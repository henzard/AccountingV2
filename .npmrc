# Required because @config-plugins/detox@11 declares peer expo@^53 but project uses expo@55.
# The plugin is functionally compatible; this flag suppresses the strict peer check.
legacy-peer-deps=true
