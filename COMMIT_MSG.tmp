fix: deep review remediation — 49 to 80 (Grade D to B)

Resolve all 4 Critical and 12 High findings from the deep review audit.

Security: authenticate notify-event edge function, fix RLS LIMIT 1,
block role self-escalation, add user_fcm_tokens RLS, Android backup
exclusion XMLs, sanitize production error boundary.

Sync: snapshot-guarded mark-synced (R2), tombstones on 7 tables (R4),
direction-independent LWW tiebreaker on 9 merge RPCs (R3).

Database: renumber duplicate 008 migration, wrap 6 repositories in
SQLite transactions, add CHECK constraints and indexes, fix schema
drift in audit cleanup function.

Architecture: fix DIP violations in 6 use cases, add ScreenErrorBoundary
on all navigators, standardize JSON error responses on edge functions.

UX/UI: fix touch targets to 44x44, add accessibility labels, inline
field validation, migrate HeroSummaryCard to theme system, extend
font token scale.

Testing: add integration test harness, core repository tests, household
isolation test, fix 21 TypeScript errors across test files.
