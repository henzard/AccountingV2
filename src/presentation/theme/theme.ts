// Re-exports for backward compatibility with code that imports `appTheme` directly.
// New code should use `useAppTheme()` from './useAppTheme' to support dark mode.
export { lightTheme as appTheme } from './useAppTheme';
