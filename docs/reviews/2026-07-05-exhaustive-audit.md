# Exhaustive whole-app audit — 2026-07-05

40 adversarially-confirmed defects (44 candidates, 4 false positives).

## Critical (3)

### [C1] supabase/functions/extract-slip/index.ts:137 (edge-functions)

**Defect:** extract-slip is non-functional in production because it expects slip_queue.status='pending', but the only production writer and the table default use 'processing' — the idempotency guard 409s and the reserve-slot RPC never matches. Tests pass only because they seed 'pending' rows.

**Failure:** User captures a real slip. CaptureSlipUseCase (src/domain/slipScanning/CaptureSlipUseCase.ts:27) creates the row with status:'processing' (also the slip_queue table default in 0001_baseline.sql:246). SlipScanFlow then invokes extract-slip. Two independent failure modes, both fatal: (a) If the row has synced to the server, step-5 guard at index.ts:137 sees status==='processing' and returns 409 'Slip already processing' (EdgeFunctionSlipExtractor maps 409 to default SLIP_OPENAI_UNREACHABLE), so extraction never runs. (b) Even if step-5 were bypassed, check_and_reserve_slip_slot (0001_baseline.sql:380-388) reserves only `WHERE status='pending'`; since no code path ever writes 'pending' (grep shows 'pending' appears only in the RPC and in test mocks — extract-slip.test.ts:47,765 etc.), the UPDATE finds nothing and returns reason 'slot_not_reserved'. Result: the slip-scan feature always fails for every user despite a green test suite.

**Verify note:** Confirmed by tracing the actual code. The only slip_queue writer, CaptureSlipUseCase.ts:27, sets status:'processing', matching the local schema default (slipQueue.ts:8) and the server table default (0001_baseline.sql:246). extract-slip's step-5 guard (index.ts:137) returns 409 'Slip already processing' whenever status==='processing', which is always the case for a freshly captured slip, so extraction never runs; EdgeFunctionSlipExtractor.mapStatus has no 409 case (cases only for 412/413/429/422/503/403) so it falls to default SLIP_OPENAI_UNREACHABLE. Even bypassing step 5, check_and_reserve_slip_slot (0001_baseline.sql:381-388) only reserves WHERE status='pending', so a 'processing' row yields slot_not_reserved → 429. No production code path ever writes 'pending' to slip_queue.status (grep hits are the unrelated pending_sync DLQ table, UI pendingDelete state, test mocks, and the RPC's own WHERE clause); migration 0000 shows slip_queue originally defaulted 'pending' but the 0008 redesign moved writers/defaults to 'processing' while the RPC and status guard kept the old 'pending' contract. This is a genuine contract mismatch that makes the slip-scan feature fail for every real user despite green tests, because the tests seed 'pending' rows. Severity Critical: the shipped feature is entirely non-functional in production.

### [C2] supabase/functions/extract-slip/index.ts:152 (edge-functions)

**Defect:** check_and_reserve_slip_slot is invoked with the service-role admin client, so auth.uid() is NULL inside the SECURITY DEFINER RPC and it returns reason:'unauthorized' — which the edge function mis-maps to a 429 'Household rate limit'.

**Failure:** adminSupabase = createAdminClient() uses only SUPABASE_SERVICE_ROLE_KEY (index.ts:329) with no forwarded user JWT, so the request carries the service_role token which has no `sub` claim. The RPC's guard `v_caller_id := auth.uid()::text; IF v_caller_id IS NULL OR v_caller_id <> p_user_id THEN RETURN 'unauthorized'` (0001_baseline.sql:349-353) therefore always returns {allowed:false, reason:'unauthorized'}. Back in index.ts:162-164 any reason other than 'user_limit' is mapped to msg='Household rate limit' with HTTP 429. Concretely, re-extracting a previously failed slip (status='failed', which passes the step-5 guard and reaches the RPC at index.ts:152) returns a misleading 429 SLIP_RATE_LIMITED_HOUSEHOLD to the user even though no rate limit was hit. The mocked test at extract-slip.test.ts:90 returns {allowed:true}, hiding this because it never exercises the real auth.uid()=NULL behavior.

**Verify note:** Confirmed real by tracing all three sites. The edge function builds `adminSupabase = deps.createAdminClient()`, and the production wiring (index.ts:329) is `createClient(supabaseUrl, supabaseServiceKey)` — service-role key only, no forwarded user JWT. At step 7 (index.ts:152-153) it calls `adminSupabase.rpc('check_and_reserve_slip_slot', {p_user_id: callerId, ...})`. PostgREST authenticates that request with the service_role JWT, whose standard Supabase payload carries `role:service_role` but no `sub` claim, so `auth.uid()` (which reads `request.jwt.claim.sub`) evaluates to NULL. Inside the SECURITY DEFINER RPC (0001_baseline.sql:348-353) the guard `v_caller_id := auth.uid()::text; IF v_caller_id IS NULL OR v_caller_id <> p_user_id THEN RETURN jsonb_build_object('allowed', false, 'reason', 'unauthorized')` therefore always fires. Back at index.ts:162-164, any reason other than 'user_limit' maps to `msg='Household rate limit'` with HTTP 429. So the guard is unsatisfiable via the only path that ever calls it. The claim understates the blast radius: this is not limited to re-extracting failed slips — the RPC is reached by every slip that passes the step-5 status guard (any status that is not completed-with-response or processing, including fresh `pending` slips), so the entire slip-extraction feature returns a spurious 429 SLIP_RATE_LIMITED_HOUSEHOLD and no slip can ever be reserved/processed in production. The unit test (extract-slip.test.ts:89-90) hard-codes `rpc: () => {allowed:true}`, so it never exercises the real `auth.uid()=NULL` path and masks the defect. The fix is to make the RPC trust `p_user_id` (since the service-role caller has already authenticated the user and verified membership/ownership) or invoke it with the caller-scoped client. I raised severity from High to Critical because this fails closed on the entire feature, not merely a mislabeled error message.

### [C3] src/presentation/hooks/useEnvelopes.ts:31 (envelopes)

**Defect:** useEnvelopes filters every envelope by exact period_start equality, which permanently excludes ALL persistent envelope types (sinking_fund, emergency_fund, savings, baby_step) from every budget period after the one they were created in — this is the production read path for BudgetScreen, DashboardScreen, ForecastScreen and SinkingFundsScreen, bypassing the correct envelopeScopeCondition() logic used by DrizzleEnvelopeRepository.listByHousehold.

**Failure:** User creates an emergency fund (or savings goal, or sinking fund) in June via AddEditEnvelopeScreen. CreateEnvelopeUseCase stores it with period_start = the June period start (e.g. 2026-06-25). Persistent types are never copied forward (StartNewPeriodUseCase explicitly excludes them, EnvelopeBalanceQuery/getEnvelopeScope) and their period_start is never updated (UpdateEnvelopeUseCase.fields omits period_start entirely). In July, BudgetPeriodEngine.getCurrentPeriod returns periodStart = 2026-07-25, and the query `and(householdId=…, eq(period_start, '2026-07-25'), isArchived=false)` matches zero of the persistent rows. Result: SinkingFundsScreen renders 'No sinking funds yet' though funded goals exist; BudgetScreen/DashboardScreen drop the emergency fund and savings envelopes from the Expenses section and from totalAllocated/totalSpent; and because calculateBudgetBalance (via useBudgetBalance) now omits those savings/expense allocations, toAssign is over-reported so the zero-based budget falsely claims unassigned money. getEnvelopeSpentCents at line 35 DOES include those persistent ids (it uses envelopeScopeCondition), but their rows are absent from `rows`, so the derived spend is silently discarded — confirming the read path is internally inconsistent. Fix: replace the period_start equality with envelopeScopeCondition(periodStart) (and drop the isArchived filter or fold it in) exactly as the repository does.

**Verify note:** CONFIRMED real defect. Read src/presentation/hooks/useEnvelopes.ts:31 — the query filters rows with `eq(envelopesTable.periodStart, periodStart)` (exact equality) plus householdId/isArchived, with no scope logic. This is the direct-db read path consumed by SinkingFundsScreen, BudgetScreen, DashboardScreen, ForecastScreen and useBudgetBalance (verified all import/call useEnvelopes, not the repository). SinkingFundsScreen passes the CURRENT period (engine.getCurrentPeriod) to the hook. CreateEnvelopeUseCase stores persistent envelopes (sinking_fund/emergency_fund/savings/baby_step) with period_start = creation-period; StartNewPeriodUseCase explicitly copies forward only 'period'-scoped types (getEnvelopeScope === 'period'), never persistent ones, and never re-stamps their period_start. Therefore in any period after creation, the equality filter matches zero persistent rows: SinkingFundsScreen shows the 'No sinking funds yet' EmptyState, and Budget/Dashboard drop emergency-fund and savings envelopes from totals, inflating toAssign in the zero-based budget. The internal inconsistency is also confirmed: getEnvelopeSpentCents (line 35) uses envelopeScopeCondition, which includes PERSISTENT_TYPES unconditionally, so it returns spend for ids whose rows are absent from `rows` — the derived spend is silently discarded. DrizzleEnvelopeRepository.listByHousehold uses the correct envelopeScopeCondition, proving the hook diverges from the intended scope rule. This manifests purely as a function of time on real SQLite, not a mock or unreachable path. Fix: replace the period_start equality with envelopeScopeCondition(periodStart) and use isNull(deletedAt), matching the repository.

## High (9)

### [H1] src/presentation/screens/debtSnowball/LogPaymentScreen.tsx:45 (money-cents)

**Defect:** Debt payment amount is parsed with `Math.round(parseFloat(amountRands) * 100)` instead of parseMoneyInput, silently mis-parsing grouped/locale-formatted amounts and writing a wrong payment to the money ledger + audit log.

**Failure:** A debt has minimumPaymentCents = 150000 (R1,500). The field is prefilled '1500.00'. User re-types the amount as '1,500' (comma thousands separator; keyboardType="numeric" permits the comma). parseFloat('1,500') === 1, so amountCents = Math.round(1\*100) = 100. LogDebtPaymentUseCase records a R1.00 payment: outstanding_balance_cents decremented by only 100, total_paid_cents +100, and the audit log stores paymentAmountCents:100 — all silently, with no error shown. Similarly '1500,50' (af-ZA decimal comma) → parseFloat('1500,50')=1500 → 150000, silently dropping the 50c.

**Verify note:** Confirmed real by tracing the actual code. At LogPaymentScreen.tsx line 44, `handleSave` computes `const amountCents = Math.round(parseFloat(amountRands) * 100)` from a free-text `TextInput` (keyboardType="numeric"), then passes it straight into `LogDebtPaymentUseCase` as `paymentAmountCents`. That use case (LogDebtPaymentUseCase.ts) writes the amount to the money ledger via `runInUnitOfWork` (decrements outstanding_balance_cents, increments total_paid_cents through a synced oplog) and records it in the audit log — so a wrong parse produces a persisted, replicated, audited wrong payment. I verified the parse failures in Node: `parseFloat('1,500') === 1` → amountCents = 100 (R1.00 recorded instead of R1,500), and `parseFloat('1500,50') === 1500` → 150000 (the 50c is silently dropped). Both results are positive integers, so the only guard (`isNaN || <= 0`) and the use case's `<= 0` check both pass — the misparse is silent with no error shown. This is not mocked or already-handled: the codebase HAS a locale-safe `parseMoneyInput` (rejects ambiguous/grouped/comma input instead of coercing) that a remediation wired into the four "add" money screens (AddTransactionScreen, AddReadingScreen, AddEditEnvelopeScreen, AddDebtScreen), but LogPaymentScreen — a fifth money-entry screen that writes to the same ledger — was missed and still uses raw parseFloat. High severity is correct: it is a silent money-cents corruption of a persisted, synced financial ledger plus audit trail, though realistically gated by whether a user enters a separator (the af-ZA decimal-comma case makes that plausible on a locale keyboard, and paste can inject grouping).

### [H2] src/presentation/screens/budgets/RolloverWizard.tsx:89 (money-cents)

**Defect:** Rollover allocation edits use a local `toCents` (`parseFloat(str.replace(',', '.'))` → Math.round(n\*100), 0 on non-finite) instead of parseMoneyInput; empty/garbage input silently becomes a R0 allocation edit and grouped input silently writes a wrong allocated_cents.

**Failure:** On the 'adjust' step a user backspaces an envelope's allocation field to '' intending to retype, then taps 'Start period'. toCents('') → parseFloat NaN → not finite → returns 0. The `edits` memo keeps it because 0 !== the original allocatedCents (e.g. 250000), so handleCommit calls repo.update(targetId, ..., { allocated_cents: 0 }) — the envelope is silently zeroed for the new period. Separately, typing '2,500' (meaning R2,500) → replace first comma → '2.500' → parseFloat 2.5 → Math.round(250) = 250 cents = R2.50 written as the allocation, with no validation error.

**Verify note:** Confirmed by tracing the code. RolloverWizard.tsx:88 defines a local toCents = parseFloat(String(str).replace(',', '.')) then Math.round(n\*100), returning 0 on non-finite — not the validating parseMoneyInput (which exists at src/presentation/utils/parseMoneyInput.ts and rejects empty/ambiguous input). The adjust-step TextInput writes the raw string directly into allocationStr[e.id] with no validation. The edits memo (line 193-199) computes editedCents: toCents(allocationStr[e.id] ?? '0'); the ?? only guards undefined, so an emptied field ('') reaches toCents('') → parseFloat NaN → returns 0, and the filter (editedCents !== allocatedCents) keeps it because 0 !== 250000. handleCommit (line 227-258) then calls repo.update(targetId, householdId, { allocated_cents: 0 }, ctx), silently zeroing the new period's envelope — a real write to the envelopes repo with no blocking validation. The grouped-input case is also real: replace(',', '.') only swaps the first comma, so '2,500' → '2.500' → parseFloat 2.5 → 250 cents (R2.50) versus parseMoneyInput's correct 250000 cents — a 1000x silent error. This is real financial-data corruption consistent with the repo's confirmed money-parsing bug pattern. Partial mitigations: the commit step renders a formatCurrency preview of each change (a careful user could notice '→ R0.00'), and the decimal-pad keyboard makes typing a literal thousands comma less common (though paste still triggers it). Severity remains High because wrong allocated_cents is committed with no validation error.

### [H3] supabase/functions/notify-event/index.ts:266 (edge-functions)

**Defect:** notify-event membership checks omit the `deleted_at IS NULL` predicate and use .single(): a removed member retains push-send access, and any caller or target who left and rejoined a household (2 rows) gets a spurious 403.

**Failure:** The canonical membership predicate private.is_household_member filters `m.deleted_at is null` (0001_baseline.sql:731). notify-event's caller check (index.ts:261-266) and target check (index.ts:278-283) query household_members by (household_id,user_id) with .single() and NO deleted_at filter — unlike extract-slip which correctly uses `.is('deleted_at', null)` + maybeSingle. Consequence A (authz bypass): a user soft-removed from household H (deleted_at set, no active row) still has exactly one matching row, so .single() returns it and the caller passes the membership gate — a removed member can keep sending pushes. Consequence B (availability): the partial unique index only enforces uniqueness on active rows (0001_baseline.sql:98-99), so a user who left and rejoined H has one soft-deleted row plus one active row = 2 matching rows; .single() then errors (PostgREST 406 'multiple rows'), data is null, and the code returns 403 Forbidden. So a rejoined caller can never send notifications, and a rejoined target can never receive them. The test at notify-event.test.ts:69 mocks single() to a lone row and never covers the multi-row / soft-deleted cases.

**Verify note:** REAL. I traced every load-bearing assertion. In supabase/functions/notify-event/index.ts the caller check (lines 260-266) and target check (278-283) both query public.household_members via the admin/service client (deps.createAdminClient(), which bypasses RLS) filtering only on household_id + user_id and terminating in .single(), with NO deleted_at predicate. The canonical membership guard private.is_household_member (0001_baseline.sql:727-736) requires `m.deleted_at is null`, and the sibling function extract-slip (index.ts:99-104) correctly uses `.is('deleted_at', null).maybeSingle()`, confirming the intended pattern and that notify-event diverges. Consequence A (authz bypass) is real: a soft-removed member still has exactly one matching (soft-deleted) row, so .single() returns it, membership is truthy, and the 403 gate passes — a removed member keeps push-send access. Consequence B (availability) is real: the uniqueness constraint is a PARTIAL unique index `... WHERE deleted_at IS NULL` (0001_baseline.sql:98-99), so a user who left and rejoined household H has one soft-deleted row plus one active row = 2 matching rows; PostgREST .single() then errors (multiple/no single row), data is null, and the code returns 403 — a rejoined caller can never send and a rejoined target can never receive. The test (**tests**/notify-event.test.ts:69) mocks single() to a single row and never exercises the soft-deleted or multi-row cases, so the bug is uncaught. Severity High is appropriate: it is a genuine authorization bypass plus a permanent functional break for any rejoined member; blast radius is bounded to push-notification sending/receipt rather than direct data exfiltration, but both effects manifest on a real Postgres/PostgREST deployment.

### [H4] src/data/sync/SyncEngine.ts:760 (sync-engine)

**Defect:** Own-device skip (line 760) combined with the unconditional, unguarded UPDATE apply in applyOne's 'update' branch (lines 800-810) causes a permanent, self-healing-proof last-write-wins divergence: a device that pulls a remote update op with a LOWER server seq than its own already-applied update overwrites its own newer value and never re-applies its own (higher-seq) op, so the replica whose write actually won on the server ends up displaying the losing value forever.

**Failure:** Envelope E ('Groceries') is synced on devices A and B. Concurrently A renames it to 'Food' (op_A, device_id=A) and B renames it to 'Petrol' (op_B, device_id=B). A pushes first, so the server assigns op_A seq=10 and op_B seq=11; the server row (LWW, highest seq) is 'Petrol'. Device A pulls [op_A(seq10,A), op_B(seq11,B)]: it skips op_A (own device) and applies op_B -> A shows 'Petrol' (correct). Device B pulls the same rows in seq order: op_A(seq10) is remote so applyOne runs UPDATE ... SET name='Food' with no updated_at/seq guard, overwriting its local 'Petrol'; op_B(seq11) is skipped (own device). B advances its cursor to 11 and never re-applies op_B. Final state: server/A = 'Petrol', B = 'Food'. The two replicas are permanently divergent and B (the write that actually won) displays the stale losing value; it self-heals only if the user edits E again.

**Verify note:** Confirmed by tracing SyncEngine.ts: line 760 skips own-device ops during pull-apply while still advancing the cursor (maxSeq includes their seq), so an own op is never recorded in oplog_applied and, since the cursor moves past it, is never re-delivered or replayed. The 'update' branch (lines ~800-810) issues `UPDATE <table> SET <fields> WHERE id = row_id AND household_id = ...` with NO updated_at/seq/version guard, so a lower-seq remote op unconditionally overwrites a higher-seq value already present locally. The header invariant R8 ('ops apply in server seq order') is the design's stated basis for convergence, but own ops are applied at local creation time rather than interleaved in seq order with concurrent remote ops — exactly the invariant the own-device skip breaks. In the described scenario device B applies its own op_B(seq11) locally ('Petrol'), then pulls [op_A(seq10), op_B(seq11)], applies op_A unconditionally (name -> 'Food'), skips op_B, and advances the cursor to 11; op_B is never replayed, leaving B='Food' while server/A='Petrol' — permanent, self-heal-proof divergence. The outcome even flips depending on whether a replica pushes-then-pulls or pulls-then-pushes, the classic signature of a broken LWW total-order guarantee. No per-row guard exists anywhere in applyOne to prevent it. This is a real multi-writer convergence bug on absolute-value update/delete fields (increment is commutative and unaffected). Severity High rather than Critical because it corrupts displayed entity fields, not money integrity, and requires concurrent edits to the same field.

### [H5] src/presentation/screens/slipScanning/SlipQueueScreen.tsx:165 (transactions)

**Defect:** Tapping a 'completed' slip in the queue navigates to SlipConfirm WITHOUT the required `extraction` param, and SlipConfirmScreen crashes dereferencing it.

**Failure:** User scans a slip, confirms it (slip.status -> 'completed'), then returns to the slip history and taps that completed row to review it. handlePress case 'completed' calls navigation.navigate('SlipConfirm', { slipId: item.id }) with NO `extraction` key. SlipConfirmScreen.tsx line 41-42 does `const { slipId, extraction } = route.params; const items = extraction.items;` -> `extraction` is undefined -> TypeError 'Cannot read property items of undefined' -> the Confirm screen white-screens/crashes. There is no code path that reloads the extraction from the DB by slipId, so every completed-slip review is unreachable. Tests pass because they always mount SlipConfirmScreen directly with a fully-populated extraction param, never via this queue tap.

**Verify note:** Real defect, fully traced. In SlipQueueScreen.tsx the `handlePress` switch `case 'completed'` calls `navigation.navigate('SlipConfirm', { slipId: item.id })` with NO `extraction` key (contrast the adjacent `case 'failed'`, which explicitly parses and passes `extraction`). In SlipConfirmScreen.tsx (line ~41-42) the screen does `const { slipId, extraction } = route.params;` immediately followed by `const items = extraction.items;` with no optional chaining or guard, so `extraction` being undefined throws a TypeError ("Cannot read property 'items' of undefined"), white-screening the Confirm screen. The stack navigator (SlipScanningStackNavigator.tsx:70-71) renders `<SlipConfirmScreen confirmSlip={confirmSlip} envelopes={envelopes} />` and injects no `extraction` prop nor any DB-hydration wrapper, so there is genuinely no code path that reloads the extraction by slipId — the missing param is never backfilled. Thus reviewing any already-completed slip via the queue tap crashes on a real device. Tests don't catch it because SlipConfirmScreen.test.tsx mounts the screen directly with a populated `extraction` param rather than exercising the queue-tap route. Note the ParamList type declares `extraction: unknown` (non-optional in the object form), so even the types expect it — the queue call site simply omits it. Severity: it crashes the screen and makes completed-slip review entirely unreachable, but it is recoverable (navigate back), non-data-corrupting, and confined to one review flow, so High is appropriate.

### [H6] src/presentation/screens/slipScanning/SlipQueueScreen.tsx:155 (transactions)

**Defect:** The 'failed' slip re-entry path passes the raw model response (data.raw_response) as `extraction`, which is NOT a SlipExtraction, producing a crash or transactions with undefined amounts.

**Failure:** A slip's extraction succeeded but the save later failed (status 'failed', rawResponseJson populated). Tapping it runs `JSON.parse(item.rawResponseJson)` and passes the result as `extraction` to SlipConfirm. But rawResponseJson = data.raw_response (EdgeFunctionSlipExtractor.ts line 72) — the raw OpenAI JSON, not the normalized SlipExtraction. SlipConfirmScreen expects `extraction.items: SlipExtractionItem[]` with camelCase `amountCents`/`suggestedEnvelopeId`. The raw response has no such `items`/`amountCents` shape, so either `extraction.items` is undefined -> `items.map` TypeError on line 47 (crash), or (if the raw JSON happens to carry an items array) every `item.amountCents` is undefined and the user confirms transactions with NaN/undefined cents. The screen is designed to be fed the parsed SlipExtraction, never the raw model string.

**Verify note:** The defect is REAL, though the claim's primary crash mechanism is misdiagnosed while its secondary one is correct. Traced end to end: (1) The edge function stores `raw_response_json = JSON.stringify(parsed)` where `parsed` is the snake_case OpenAI structured output — items with `amount_cents` / `suggested_envelope_id` (supabase/functions/extract-slip/index.ts:291,301; contract snake_case in src/data/sync/extractSlipContract.ts). The camelCase `SlipExtraction` is only ever built in-memory by EdgeFunctionSlipExtractor.extract() and is never persisted. (2) A 'failed' slip WITH rawResponseJson populated is genuinely reachable: ExtractSlipUseCase writes rawResponseJson only alongside status='completed', but ConfirmSlipUseCase.ts:206 downgrades an already-completed slip (raw still populated, never nulled) to status='failed' when the save transaction rolls back (SLIP_PARTIAL_SAVE_FAILED). (3) SlipQueueScreen.tsx:153-156 then does `JSON.parse(item.rawResponseJson)` and passes the snake_case object as `extraction`. In SlipConfirmScreen.tsx:40, `const items = extraction.items` — the parsed object DOES have an `items` array, so `items.map` does NOT throw (the claim's 'items undefined -> TypeError on line 47' prong is FALSE). But every item is snake_case, so `item.amountCents` is undefined for all items. This produces the claim's second, correct outcome: LineItemRow renders `formatCents(undefined)` = "RNaN" (LineItemRow.tsx:18,67); the guard `if (item.amountCents <= 0)` at ConfirmSlipUseCase.ts:113 evaluates `undefined <= 0` = false and is bypassed; and handleSave (SlipConfirmScreen.tsx:105-108) passes `amountCents: undefined` into the raw INSERT at ConfirmSlipUseCase.ts:155, corrupting the ledger (NULL/undefined amount_cents) or throwing a bind error. suggestedEnvelopeId and slipDate are likewise undefined. The failed-slip recovery path this code was deliberately added for is thus 100% broken whenever exercised.

### [H7] src/presentation/screens/debtSnowball/LogPaymentScreen.tsx:45 (debt-snowball)

**Defect:** Payment amount is parsed with raw parseFloat(amountRands)\*100 instead of the locale-safe parseMoneyInput used everywhere else in this subsystem, so common South African money formatting is silently coerced to a wrong cents value rather than rejected.

**Failure:** User opens Log Payment and types an extra payment of "1 500" (space thousands separator) or "1,500" (comma) — both natural en-ZA/af-ZA input on a numeric keyboard. parseFloat('1 500')===1 and parseFloat('1,500')===1, so amountCents=Math.round(1\*100)=100. The use case records a R1.00 payment (outstanding_balance_cents -= 100, total_paid_cents += 100) instead of R1,500, and the guard amountCents<=0 passes because 100>0. Debt balance and paid-to-date are silently wrong. af-ZA decimal comma is also lost: "10,50" -> parseFloat=10 -> 1000c (R10.00), dropping 50c. AddDebtScreen.tsx:42 correctly uses parseMoneyInput for the same kind of field; this screen bypasses it.

**Verify note:** Verified against the actual code. LogPaymentScreen.tsx:45 reads `const amountCents = Math.round(parseFloat(amountRands) * 100);` with only an `isNaN(amountCents) || amountCents <= 0` guard on line 46, and passes `amountCents` straight into `LogDebtPaymentUseCase` as `paymentAmountCents` (line 56). The input is `keyboardType="numeric"` (line 84) labelled in rands. parseFloat stops at the first non-numeric char, so `parseFloat('1 500')===1`, `parseFloat('1,500')===1`, and `parseFloat('10,50')===10` — each yields a positive, non-NaN cents value (100, 100, 1000) that passes the guard and is silently persisted as a wrong amount, corrupting the debt's balance/paid-to-date with no error shown. This is exactly what the locale-safe parseMoneyInput exists to prevent: I read src/presentation/utils/parseMoneyInput.ts and it explicitly disambiguates space/comma/period thousands and decimal separators for en-ZA/af-ZA and REJECTS ambiguous input rather than coercing, and AddDebtScreen.tsx uses it for the analogous balance/min-payment money fields (lines 42, 59) while this screen bypasses it. Comma-decimal ("10,50") is natural af-ZA input and paste is always possible, so the wrong-value path is reachable on a real device. High is warranted: silent, un-erroring corruption of financial ledger data.

### [H8] src/domain/debtSnowball/CreateDebtUseCase.ts:51 (debt-snowball)

**Defect:** sortOrder is assigned as the household's existing debt count (insertion order), but SnowballPayoffProjector sorts by sortOrder and both its docstring and DebtEntity.ts:17 promise 'smallest balance first (Ramsey default)'. Nothing ever aligns sortOrder to balance and there is no reorder UI, so the snowball focus follows entry order, not balance.

**Failure:** User adds a R500,000 home bond first (sortOrder=0), then a R2,000 credit card (sortOrder=1), with R3,000/month extra. SnowballPayoffProjector.project (SnowballPayoffProjector.ts:29 sorts ascending by sortOrder; line 72 gives the snowball only to focusIdx=0) pours the entire extra payment into the R500k bond while the R2,000 card sits at its minimum — the exact opposite of the Dave Ramsey snowball the feature implements. The projected payoff dates, debt-free date, and the on-screen plan are all wrong, and a user following the plan misallocates their extra payments. There is no code path or UI that ever sets sortOrder from balance, so this occurs for any household not entered in strict ascending-balance order.

**Verify note:** Confirmed real. CreateDebtUseCase.ts:51 sets sortOrder = existing debt count (insertion order): it selects count() of household debts and uses `countResult[0]?.count ?? 0`. SnowballPayoffProjector.ts:29 sorts activeDebts ascending by sortOrder, and its loop (line ~72) adds the extra snowball only to focusIdx, which starts at 0 and advances only when the current focus debt is paid off — so the whole extra payment goes to the earliest-entered debt, not the smallest balance. Both the projector docstring (line 21) and DebtEntity.ts:17 promise "smallest balance first (Ramsey default)." A grep across src shows sortOrder is written only in CreateDebtUseCase (from count) and read only for ordering in useDebts.ts:27; there is no update/reorder use case, no reorder UI, and no code path that ever derives sortOrder from balance (reorder/updateSortOrder/UpdateDebt grep returns nothing; only CreateDebtUseCase and LogDebtPaymentUseCase exist). The projection is pure deterministic domain math, so the R500k-bond-then-R2k-card scenario really does pour the extra payment into the bond and inverts the Ramsey snowball, yielding wrong payoff dates, debt-free date, and on-screen plan on a real device. High (not Critical) because it is wrong financial guidance rather than a crash/data-loss/security issue, and households that happen to enter debts smallest-first are unaffected. (Store is expo-sqlite, not Postgres, but that does not affect the logic.)

### [H9] App.tsx:392 (boot-nav)

**Defect:** The cold-start boot IIFE awaits `supabase.auth.getSession()` with no try/catch; if it rejects, `setLocalBootReady(true)` is skipped and the app is permanently stuck on the native splash with no error view and no recovery.

**Failure:** On a device where the persisted-session read fails (SecureStore/keychain error after an OS update, biometric-locked keystore, or a corrupted session entry), `supabase.auth.getSession()` at line 392 rejects. The rejection propagates out of the un-awaited `void (async () => {...})()` (started at line 386). Because only `initSessionOnce` is wrapped in try/catch (lines 402-419) — getSession is not — the rejection skips `setLocalBootReady(true)` at line 426. `localBootReady` stays false forever, so `bootDecided` (line 348) never becomes true: `SplashScreen.hideAsync()` is never called and `App` returns `null` (line 610). The user is stuck on a frozen splash indefinitely with no error UI and no way out short of reinstalling. This defeats the file's own 'degrade-on-error' boot contract, which guards `initSessionOnce` for exactly this reason but leaves the earlier `getSession()` await exposed.

**Verify note:** REAL defect, fully substantiated by tracing. At App.tsx:392 `await supabase.auth.getSession()` sits outside any try/catch inside the cold-boot IIFE (`void (async () => {...})()` at line 386, which has no `.catch` — unlike the sibling render-gate IIFE at ~line 582 that does). The only guard covers `initSessionOnce`. If getSession rejects, `setLocalBootReady(true)` (line 426, the IIFE's last statement) is skipped, so `bootDecided` (Boolean(fontError||dbError) || (fontsLoaded && dbReady && localBootReady)) stays false forever → `SplashScreen.hideAsync()` never runs and `App` returns null at the render gate; there is no error UI (only fontError/dbError render one) and no recovery. getSession genuinely can reject: SecureStorageAdapter.getItem (src/infrastructure/storage/SecureStorageAdapter.ts:5) returns SecureStore.getItemAsync unguarded; the supabase-js getItemAsync helper awaits storage.getItem with no catch around the call, and GoTrueClient \_\_loadSession/\_useSession use try/finally with no catch, so a SecureStore rejection propagates straight out of getSession(). expo-secure-store does reject on real devices (Android keystore key invalidation after biometric/OS changes, decrypt errors, corrupted entries; on iOS a bad keychain item can even survive reinstall). This is an unrecoverable hard brick that defeats the file's own documented degrade-on-error contract; the trivial fix is to wrap getSession in the same try/catch style already used for initSessionOnce. Impact is catastrophic (permanent splash lock, no in-app escape) though the trigger is a relatively uncommon keystore-level failure, which keeps it at High rather than Critical.

## Medium (18)

### [M1] src/presentation/screens/budgets/RolloverWizard.tsx:88 (write-paths)

**Defect:** RolloverWizard's `toCents` uses `parseFloat(str.replace(',', '.'))` and silently writes a WRONG integer-cents allocation to the synced envelopes repo, instead of using the codebase's locale-safe `parseMoneyInput` (which is built precisely to reject this ambiguity).

**Failure:** User is on the af-ZA/en-ZA rollover wizard and edits a copied-forward allocation to a grouped value. `toCents` only replaces the FIRST comma and lets parseFloat stop at spaces/second separators: "1 500" -> parseFloat("1 500")=1 -> Math.round(100)=100c (R1.00 instead of R1500.00); "1,500" (thousands) -> parseFloat("1.500")=1.5 -> 150c (R1.50); "1,234.56" -> replace first comma -> "1.234.56" -> parseFloat=1.234 -> 123c. handleCommit (line 244) then calls repo.update(targetId, householdId, { allocated_cents: editedCents }) on the synced envelopes repo, persisting and syncing the corrupted allocation with no error shown. parseMoneyInput.ts exists specifically to reject these strings but is bypassed here.

**Verify note:** Real defect, substantiated by tracing the code. RolloverWizard.tsx:88-91 defines toCents as parseFloat(String(str).replace(',', '.')) — it replaces only the first comma and parseFloat halts at the first unparseable char, so grouped/ambiguous af-ZA/en-ZA amounts are silently mis-parsed: "1,500"→"1.500"→1.5→150c, "1,234.56"→"1.234.56"→1.234→123c, "1 500"→1→100c. That value flows through the edits useMemo (line 196) with no validation and no error path, and handleCommit (line 240-245) writes it directly to the synced envelopes repo via repo.update(targetId, householdId, { allocated_cents: editedCents }, ctx), persisting and replicating a corrupted allocation with nothing shown to the user. The codebase ships parseMoneyInput.ts precisely to accept these grouped forms and REJECT ambiguous ones (docstring lines 4-30, DECIMAL_RE/GROUPED_RE logic), and it is bypassed here — the auto-memory even flags this as a confirmed cross-screen bug. The input is keyboardType="decimal-pad" prefilled with a dot-decimal from fromCents, so the common values (1500, 1500.00, 1500,00) parse correctly and only grouped/multi-separator or pasted inputs corrupt, which lowers the trigger frequency; combined with silent corruption of synced financial data this lands at Medium (arguably High given it is money data that replicates to other devices without any error).

### [M2] src/presentation/screens/auth/onboarding/IncomeStep.tsx:13 (money-cents)

**Defect:** Onboarding monthly income uses `toCents` (`parseFloat(str.replace(',', '.'))` → Math.round(n\*100)) instead of parseMoneyInput, silently mis-parsing grouped income and cascading a wrong figure into the entire generated budget.

**Failure:** User enters monthly income as '25,000' (R25,000) — or pastes '25,000.00'. `str.replace(',', '.')` replaces only the first comma → '25.000' (or '25.000.00'), parseFloat → 25, Math.round(25\*100)=2500. setMonthlyIncomeCents(2500) stores R25.00. AllocateEnvelopesStep then divides incomeCents (2500) across categories, so the user's whole starting budget is built on R25 instead of R25,000, with no error surfaced.

**Verify note:** Confirmed by reading the source. IncomeStep.tsx defines a local `toCents(str) { const n = parseFloat(str.replace(',', '.')); if (isNaN(n)) return 0; return Math.round(n*100); }` and `handleNext` feeds its result straight into `useAppStore.getState().setMonthlyIncomeCents(cents)`. `String.replace(',', '.')` replaces only the FIRST comma, so '25,000' becomes '25.000' → parseFloat → 25 → 2500 cents (R25.00), and '25,000.00' becomes '25.000.00' → parseFloat stops at the second dot → 25 → 2500. The project ships a safe alternative, `src/presentation/utils/parseMoneyInput.ts`, which explicitly handles thousands grouping (space/comma/period) and REJECTS ambiguous input instead of coercing — IncomeStep bypasses it (grep confirms AllocateEnvelopesStep.tsx and RolloverWizard.tsx carry the same unsafe `replace(',', '.')`, corroborating the memory note about 4 screens). The cascade is real: AllocateEnvelopesStep.tsx:43 reads `monthlyIncomeCents`, line 47 does `base = Math.floor(incomeCents / categories.length)` and splits the whole starting budget across categories, so a mis-parsed R25 poisons every envelope with no error surfaced at entry. Two caveats keep this Medium rather than High: (1) the field uses `keyboardType="decimal-pad"`, which on-device discourages typing thousands separators, so the primary trigger is pasting a grouped string (e.g. '25,000.00') or space-grouped ZA formatting via paste — a real but narrower vector than free typing; and (2) the downstream AllocateEnvelopesStep visibly renders "We've split your R{fromCents(incomeCents)}" (line 112), so the drastic R25.00 figure is shown to the user before commit, giving a chance to catch it. It is nonetheless a genuine correctness defect that silently corrupts money for grouped/pasted input where the intended safe parser was available and used elsewhere.

### [M3] src/presentation/screens/auth/onboarding/AllocateEnvelopesStep.tsx:25 (money-cents)

**Defect:** Envelope allocation amounts use the same unsafe `toCents` (`parseFloat(str.replace(',', '.'))` → Math.round(n\*100)) rather than parseMoneyInput, mis-parsing grouped input; the exact-total gate turns a legitimate grouped entry into a spurious 'off by' rejection or, in compensating cases, a wrong CreateEnvelopeUseCase allocatedCents.

**Failure:** User types '1,000' (R1,000) into an allocation field. toCents('1,000') → replace first comma → '1.000' → parseFloat 1 → 100 cents. totalAllocatedCents is now short by 99,900c, so handleNext fails with 'off by RX' even though the user entered a valid, unambiguous amount — blocking onboarding. In a compensating scenario where several fields mis-parse but still net exactly incomeCents, CreateEnvelopeUseCase is called with the wrong per-envelope allocatedCents and the wrong values are persisted silently.

**Verify note:** Confirmed real. AllocateEnvelopesStep.tsx:23-27 defines a local toCents using parseFloat(String(str).replace(',', '.')) → Math.round(n\*100), instead of the app's own locale-safe parseMoneyInput (src/presentation/utils/parseMoneyInput.ts), which is already the standard in 4 sibling screens. The two parsers agree on plain digits and 1-2 decimal inputs but diverge whenever a separator is followed by exactly 3 digits: toCents('1,000'/'1.000') → 100 cents (R1.00) while parseMoneyInput → 100000 cents (R1000), a 1000x error. On a real en-ZA/af-ZA device the decimal-pad's separator key is a comma and users commonly type grouped amounts (e.g. '2.500' for R2500), so this input is genuinely reachable and mis-parses. Because the exact-total gate (line 63) and the CreateEnvelopeUseCase persistence loop (line 84) both use the SAME toCents, the realistic manifestation is a spurious 'off by RX' rejection that blocks first-run onboarding; the fully-silent wrong-allocatedCents persistence only occurs in the contrived compensating case where mis-parsed values net exactly to incomeCents (and even then the on-screen 'To assign R0' matches what is saved). Medium is appropriate: it can block the money-splitting onboarding step and produce wrong envelope amounts for a plausible-but-specific input pattern, though decimal-pad limits the input surface and the gate/persist self-consistency caps the silent-corruption risk.

### [M4] supabase/migrations/0001_baseline.sql:1171 (server-rpc-rls)

**Defect:** sync_row_state queries the 'households' table with a `t.household_id = p_household_id` predicate, but public.households has NO household_id column (a household IS its own scope). Since 'households' is in this RPC's allowlist (line 1162), any call for that table raises SQLSTATE 42703 `column "household_id" does not exist`. This is the exact bug class 0002 fixed in apply_one_op (BUG 1 / IMPORTANT-2) but left unpatched here — 0002 only redefines apply_one_op and sync_push, never sync_row_state.

**Failure:** User changes their household payday day (UpdateHouseholdPaydayDayUseCase produces a `households` update op). The op transiently fails and is dead-lettered. In SyncHealthScreen the user taps 'Discard', which calls SyncEngine.discardDeadLettered -> transport.rowState(op.household_id, op.table_name='households', op.row_id) -> server RPC sync_row_state(H,'households',H). The EXECUTE `SELECT to_jsonb(t) FROM public.households t WHERE t.id=%L AND t.household_id=%L` throws `column household_id does not exist` (uncaught — no EXCEPTION block in sync_row_state). The RPC errors, discardDeadLettered re-throws (SyncEngine.ts:459/898), and the households op can NEVER be discarded — it is permanently stuck in the DLQ.

**Verify note:** Confirmed by tracing the code. public.households (0001 lines 59-68) has no household_id column (PK id IS the household scope), yet sync_row_state (0001 line 1171) unconditionally executes `SELECT to_jsonb(t) FROM public.%I t WHERE t.id=%L AND t.household_id=%L`, and 'households' is explicitly in the function's allowlist c_tables (line 1162), so a households call passes the allowlist gate and hits the broken EXECUTE, raising uncaught SQLSTATE 42703 (no EXCEPTION block). This is the identical bug class 0002 documents and fixes as BUG 1 / IMPORTANT-2 in apply_one_op by special-casing v_table='households' (0002 lines 184-193, 209-228), but 0002 only redefines apply_one_op and sync_push and never touches sync_row_state. Client reachability is real: SyncEngine.discardDeadLettered (SyncEngine.ts:436-456) passes op.table_name straight to transport.rowState with no filter, the production transport forwards it into rpc('sync_row_state') and throws on error (line 898), and an existing test (syncEngine.test.ts:397) proves a rowState throw leaves the op permanently dead-lettered. So any households DLQ entry can never be discarded. I downgrade from High to Medium because the trigger is narrow: it only fires on the manual Discard action for a households op that has already landed in the DLQ, and post-0002 households ops normally apply, so getting one into the DLQ requires an uncommon prior rejection; impact is a permanently stuck op, not data loss or a security hole.

### [M5] src/data/sync/SyncEngine.ts:811 (sync-engine)

**Defect:** The same own-device-skip + unconditional-overwrite mechanism applies to the 'delete' branch (lines 811-817), which stamps deleted_at unconditionally with no 'deleted_at IS NULL' guard. On a concurrent double-delete of the same row, each replica ends with the OTHER device's tombstone timestamp, violating R4's stated guarantee that 'replicas converge on the same tombstone'.

**Failure:** Row R exists on devices A and B. Both soft-delete R concurrently: A stamps deleted_at=tA (op_A, seq=10), B stamps deleted_at=tB (op_B, seq=11), with tA != tB. Device A pulls: skips its own op_A, applies op_B -> deleted_at=tB. Device B pulls: applies op_A (remote, seq10) -> deleted_at=tA, then skips its own op_B -> deleted_at stays tA. Final tombstone: A holds tB, B holds tA. The R4 'same tombstone on every replica' property is broken; any logic that reads or compares deleted_at across devices (e.g. LWW tie-breaks, audit/display) sees inconsistent values.

**Verify note:** Confirmed by tracing the code. In applyPulledBatch (SyncEngine.ts ~line 763) own-device ops are skipped (`if (row.device_id === this.deviceId) continue;`), and the delete branch in applyOne (lines 810-817) runs `UPDATE ... SET deleted_at = ${payload.deleted_at} WHERE id=... AND household_id=...` with NO `deleted_at IS NULL` guard and NO seq/timestamp comparison (a Grep for any such guard found nothing). So the apply is pure arrival-order LWW with own-ops skipped. In the concurrent double-delete trace: Device A skips its own op_A and applies op_B last -> deleted_at=tB; Device B, having already applied its own op_B (seq11) locally, then unconditionally applies the LOWER-seq remote op_A (seq10) -> deleted_at=tA, and skips op_B, never restoring the winner. Result: A holds tB, B holds tA, permanently, violating R4's documented 'replicas converge on the same tombstone' invariant (which the code comment itself asserts). It is a genuine convergence/LWW-regression defect that manifests on real offline devices. Severity Medium rather than High/Critical because the deletion state itself still converges everywhere (the row is a tombstone on all replicas) so harm is limited to the deleted_at timestamp value (audit/display, and potential future LWW tie-break/undelete races); the triggering scenario (two offline devices deleting the same row) is also narrow. Not a false positive/style nit because it breaks an explicitly documented invariant via the same unconditional-overwrite mechanism.

### [M6] src/data/sync/SyncEngine.ts:818 (sync-engine)

**Defect:** The increment-apply branch (lines 818-836) faithfully replays only the incremented money columns, but debts.is_paid_off is maintained server-side by a BEFORE UPDATE trigger (referenced in LogDebtPaymentUseCase.ts:68-70) with NO client-side equivalent in the local SQLite schema. On the origin device is_paid_off is set by the local raw UPDATE (LogDebtPaymentUseCase.ts:90); on every OTHER device it is never touched, so debts.is_paid_off permanently diverges from the balance after a pulled payment.

**Failure:** Debt D has outstanding_balance_cents=5000, is_paid_off=0 on devices A and B. On A the user logs a 5000c payment: the local statement sets outstanding_balance_cents=0 AND is_paid_off=1, and two increment ops are appended (outstanding_balance_cents -5000 floor_zero, total_paid_cents +5000) — neither op carries is_paid_off. The server's BEFORE-UPDATE trigger sets is_paid_off=true server-side. Device B pulls the two increment ops: applyOne sets outstanding_balance_cents=0 but, lacking any client trigger, leaves is_paid_off=0. B's debts.is_paid_off is now permanently stale (0 while balance is 0). Consumers that read the raw column without also testing balance==0 (DrizzleDebtRepository.ts:56 surfaces row.isPaidOff directly; a 'Paid off' UI badge) show D as still active on B. (Impact is limited only because SnowballPayoffProjector.ts:28 and BabyStepEvaluator.ts:88/124 defensively also check outstandingBalanceCents===0, masking the divergence for those two callers.)

**Verify note:** The claim is confirmed end-to-end. LogDebtPaymentUseCase.ts:86-93 runs a raw local UPDATE that sets outstanding_balance_cents, total_paid_cents, AND is_paid_off = (MAX(0, balance - applied) = 0) on the ORIGIN device only, then appends exactly two `increment` ops (lines 100-129) for the two money columns — neither carries is_paid_off. The code comment (lines 61-72) explicitly documents that a former third `update` op carrying is_paid_off was intentionally removed and the flag is now derived server-side by a Postgres BEFORE-UPDATE trigger. SyncEngine.ts:818-836 (the increment-apply branch) faithfully replays only `UPDATE <table> SET <field> = MAX(0, <field> + delta)` for the single money column and never touches is_paid_off; grep confirms is_paid_off/isPaidOff appear nowhere in src/data/sync/, and the pull path only handles insert/update/delete/increment ops (no full-row snapshot pull). The local SQLite schema (schema/debts.ts:14, migration 0000 line 33) declares is_paid_off as a plain integer column defaulting to false with NO trigger — the server trigger has no client equivalent. DrizzleDebtRepository.rowToEntity surfaces row.isPaidOff verbatim with no read-time re-derivation. Therefore a non-origin device that pulls the two increment ops lands at balance=0 but is_paid_off=false permanently. This is user-visible: SnowballDashboardScreen.tsx:42/64 shows "$0.00 remaining" with a chevron instead of "PAID OFF" with a checkmark, and DebtDetailScreen.tsx:111 still offers the "Log Payment" button on a $0 debt — while BabyStepEvaluator.ts:88/124 and SnowballPayoffProjector.ts:28 defensively also test outstandingBalanceCents===0 and thus mask it. It is a genuine permanent cross-device divergence, but the synced balance is correct everywhere and the impact is confined to a display flag / UI affordance (no money corruption), so Medium (leaning Low) is the appropriate severity rather than anything higher.

### [M7] src/presentation/screens/slipScanning/SlipQueueScreen.tsx:148 (transactions)

**Defect:** Tapping a 'processing' slip navigates to SlipProcessing with only { slipId }, omitting the householdId/createdBy/frameLocalUris that screen requires, so it kicks off a brand-new scan with undefined frames.

**Failure:** The app is killed mid-scan, leaving a slip stuck at status 'processing'. User reopens the queue and taps it. handlePress case 'processing' calls navigation.navigate('SlipProcessing', { slipId: item.id }). SlipProcessingScreen.tsx destructures householdId/createdBy/frameLocalUris from route.params (all undefined) and its mount effect calls startScan({ householdId: undefined, createdBy: undefined, frameLocalUris: undefined }). SlipScanFlow.start -> CaptureSlipUseCase.execute reads `input.frameLocalUris.length` on undefined -> TypeError thrown from execute; SlipScanFlow.start does not wrap it, and SlipProcessingScreen's `startScan(...).then(...)` has no .catch, so the promise rejects unhandled, progress never advances, and the user is left on a permanent spinner rather than resuming/inspecting the stuck slip. It also does not resume the existing slip — the screen only knows how to start a fresh capture.

**Verify note:** Confirmed real by tracing the full chain. SlipQueueScreen.tsx:148 navigates to 'SlipProcessing' with only { slipId }, but SlipProcessingScreen.tsx:78 destructures { householdId, createdBy, frameLocalUris } from route.params (all undefined) and its mount effect (line 116) calls startScan with them. neither useSlipScanner.start (lines 25-33) nor SlipScanFlow.start wraps the call in try/catch; SlipScanFlow.start first emits onProgress({stage:'capturing'}) then awaits CaptureSlipUseCase.execute, whose line 16 reads input.frameLocalUris.length on undefined and throws a TypeError. That rejection propagates to the .then() at SlipProcessingScreen.tsx:116, which has no .catch, so it becomes an unhandled rejection; progress stays at 'capturing' (the throw bypasses the flow's explicit 'failed' onProgress calls), so the UI never shows the error/retry branch and the user is stuck on a permanent spinner. The screen also has no logic to resume an existing slip. The stuck-'processing' precondition is genuinely reachable because CaptureSlipUseCase persists the slip at status 'processing' before upload/extract, so an app kill or hang mid-scan leaves exactly that DB state, and SlipQueueScreen renders such slips as tappable — real device/real DB behavior, not mocked. Medium is the right severity: it needs an uncommon interrupted-scan state and neither loses data nor hard-crashes the app, but when hit it is an unrecoverable functional dead-end.

### [M8] src/presentation/screens/meters/AddReadingScreen.tsx:91 (meter-readings)

**Defect:** The meter reading value is parsed with raw parseFloat, which silently truncates comma-decimal input in the app's own en-ZA/af-ZA locale, storing a wrong reading_value.

**Failure:** This app is explicitly built for en-ZA/af-ZA users (the reason parseMoneyInput exists for the cost field). On a comma-decimal numeric keyboard a user enters a water reading of '12,5' kL. handleSave does `parseFloat('12,5')` which returns 12 (JS parseFloat stops at the comma). isNaN(12) is false and 12>0, so the guard passes and reading_value is stored as 12 instead of 12.5. The ',5' is silently dropped. Every subsequent consumption delta and unit-rate on the dashboard/RateHistory is then computed from the truncated value. The cost field on the same screen is protected by parseMoneyInput, but the reading value is not — the same class of locale-ambiguous input produces a silently-wrong number here. (checkAnomaly at line 57 has the identical parseFloat, affecting the anomaly preview.)

**Verify note:** Confirmed by tracing the actual code. In AddReadingScreen.tsx, handleSave (line 88) does `const value = parseFloat(readingValue)` and checkAnomaly (line 55) does `const value = parseFloat(valueStr)` — both raw parseFloat. JS parseFloat('12,5') returns 12 (parsing stops at the comma), isNaN(12) is false and 12 > 0, so the guard passes and 12 is stored as the reading. The locale premise is substantiated, not assumed: the sibling cost field on the SAME screen uses parseMoneyInput, whose header comment explicitly states it is a "Locale-safe money input parser (en-ZA / af-ZA)" that treats "1,50" as 1.50 and whose "hard rule" is to "never SILENTLY coerce an ambiguous string into a wrong number." The reading TextInput uses keyboardType="numeric" (same as the cost input), so a device in a comma-decimal ZA locale can present a comma decimal key, making '12,5' a real user input path. The impact is real downstream data corruption, not cosmetic: the truncated readingValue is persisted (LogMeterReadingUseCase writes reading_value) and then differenced in UnitRateCalculator.ts:22 (`current.readingValue - previous.readingValue`) and AnomalyDetector.ts:33/40 to compute consumption deltas and unit rates. So the same class of locale-ambiguous input the app deliberately guards against for money is silently mishandled for the meter reading. Severity Medium is appropriate rather than High/Critical because it requires a comma-decimal locale keyboard plus a fractional entry, and many meter readings (electricity kWh, odometer km) are whole numbers, narrowing real-world frequency; but it is a genuine silent wrong-value bug, not a false positive.

### [M9] src/presentation/screens/auth/onboarding/MeterSetupStep.tsx:55 (meter-readings)

**Defect:** Onboarding seeds an 'Opening baseline' reading with reading_value: 0, so the first real reading's consumption and unit-rate are computed against 0 and are wildly wrong for every onboarded meter.

**Failure:** During onboarding the user toggles Electricity on. MeterSetupStep inserts a baseline row with reading_value: 0. Later the user logs their actual meter face value, e.g. 45230 kWh with cost R900. UnitRateCalculator.calculate(current=45230, previous=0) yields consumptionUnits = 45230 - 0 = 45230 kWh and unitRateCents = round(90000 / 45230) = 2c, so MeterReadingCard/RateHistory display '45230.0 kWh' consumption and 'R0.02/kWh' instead of the real ~R2.50/kWh. Because meter readings are cumulative absolute values (not deltas from zero), subtracting a 0 baseline makes the first billing period's consumption equal to the entire meter face value and the rate off by orders of magnitude. This happens for every user who enables a meter in onboarding, not an edge case.

**Verify note:** Confirmed real by tracing the actual code (via Grep, since a Read hook truncated files to line 1). MeterSetupStep.tsx:55 inserts a genuine meter_readings row with reading_value: 0 / notes 'Opening baseline' for each meter enabled in onboarding; the in-file comment (lines 10-15) explicitly states it inlines the write to bypass LogMeterReadingUseCase's readingValue<=0 rejection so it can seed this zero baseline. UnitRateCalculator.calculate (line 22) computes consumptionUnits = current.readingValue - previous.readingValue and (line 30) unitRateCents = round(costCents/consumptionUnits), rejecting only consumption <= 0 — so a large positive delta against a 0 baseline passes and produces wrong numbers. RateHistoryScreen.tsx:36-37 pairs each reading with readings[index+1], and useMeterReadings orders desc(readingDate), so the onboarding-dated baseline is the oldest row and becomes 'previous' for the first real reading; MeterDashboardScreen (limit 2, same desc order) does the same. Nothing filters the zero baseline out. Therefore the first real reading (e.g. 45230 kWh @ R900) yields consumption 45230 and rate ~2c instead of ~R2.50/kWh, for every onboarded meter, self-correcting only from the second real reading. Kept at Medium: it corrupts a displayed/derived metric for the first period only and is not data loss, a crash, or a security issue.

### [M10] src/domain/households/AcceptInviteUseCase.ts:95 (households-auth)

**Defect:** On invite accept, if RestoreService fails both attempts the use case silently substitutes paydayDay=25 / name='My Household' and never persists a local `households` row (only a `household_members` row), which both corrupts the joiner's budget period and strands them on the create/join screen on the next cold start.

**Failure:** A user enters a valid invite code while offline/flaky: `join_household_via_invite` succeeds, the local household_members row is inserted (line 79), but both `restoreService.restoreHousehold(...)` calls (lines 82,88) reject and return null. The summary falls back to `paydayDay: restored?.paydayDay ?? 25` and `name: 'My Household'` (lines 94-95); JoinHouseholdScreen writes `setPaydayDay(25)` into appStore even if the real household pays on, say, day 1 — every budget-period boundary the joiner sees is wrong until something re-reads it. Worse, no local `households` row is ever inserted. If the app is killed before a pull runs, the next cold start calls EnsureHouseholdUseCase: it finds the membership (line 35) but the households lookup returns nothing so `if (hh)` (line 41) is false, it falls through the legacy check (no id==userId row) and returns `{ success:false, code:'no_household' }` (line 126). RootNavigator then shows the create/join choice screen to a user who is already a household member; tapping 'Create Household' mints a second, orphaned household.

**Verify note:** Confirmed by tracing the code. AcceptInviteUseCase.ts:79 inserts only a local household_members row; the local households row is written solely inside RestoreService.restoreHousehold (RestoreService.ts:79-89), which returns null at line 75 BEFORE that insert when the households fetch fails. Both restore attempts are wrapped in .catch(()=>null) (AcceptInviteUseCase.ts:81-90), so on a transient/offline failure after the RPC succeeds, restored is null yet the use case still returns createSuccess with fabricated name='My Household' and paydayDay=25 (lines 94-95). JoinHouseholdScreen.tsx:67-68 then writes setPaydayDay(25) into appStore and marks onboarding complete, so every BudgetPeriodEngine computation the joiner sees in-session is wrong (e.g. real payday day 1) with no re-read. Because no local households row exists, the next cold start's EnsureHouseholdUseCase finds the membership (line 35) but not the household (line 41), misses the legacy id==userId check, and returns no_household (line 126); since appStore has no persist middleware, householdId is null and RootNavigator (lines 126-127) shows the create/join screen to an existing member, where Create Household mints a second orphan. The claim is real. Severity stays Medium (not higher) because it needs a specific window (RPC succeeds then both household fetches fail twice) and App.tsx's fire-and-forget initSessionRemote/restore self-heals the households row and householdId on the next online boot, making permanent stranding contingent on staying offline; the unmitigated part is the silent in-session paydayDay=25 budget corruption plus a narrow orphan-household window.

### [M11] src/presentation/screens/settings/SettingsScreen.tsx:174 (boot-nav)

**Defect:** The 'Privacy — Slip scanning consent' Settings item calls `navigate('SlipConsent')`, but 'SlipConsent' is not a route in the Settings/Tab/Root navigator hierarchy (it lives inside the SlipScanning modal's internal stack), so the button is a dead no-op and the consent-management screen is unreachable.

**Failure:** User opens Settings → Slip scanning → taps 'Privacy — Slip scanning consent'. `navigation.navigate('SlipConsent')` is issued on the SettingsStack navigator. 'SlipConsent' exists only inside `SlipScanningStackNavigator` (rendered under the root 'SlipScanning' modal); the RootNavigator's routes (RootNavigator.tsx:121-165) are Auth/Main/CreateHouseholdFlow/Onboarding/ResetPassword/HouseholdPicker/CreateHousehold/ShareInvite/JoinHousehold/SlipScanning — no 'SlipConsent'. React Navigation bubbles the unmatched action up to the root and leaves it unhandled (dev warning, production silent no-op). The user can never reach the advertised consent-management/withdrawal screen. Correct call: `navigate('SlipScanning', { screen: 'SlipConsent' })`.

**Verify note:** Confirmed by tracing the navigation tree. In SettingsScreen.tsx:173-174 the "Privacy — Slip scanning consent" item calls `(navigation as unknown as { navigate })('SlipConsent')`. 'SlipConsent' is not a route in RootNavigator — its Stack.Screens are ResetPassword/Auth/CreateHouseholdFlow/Onboarding/Main/HouseholdPicker/CreateHousehold/ShareInvite/JoinHousehold/SlipScanning (RootNavigator.tsx:121-165), and RootStackParamList (types.ts:87) only has `SlipScanning`. 'SlipConsent' exists solely as a nested screen inside SlipScanningStackNavigator.tsx:59, which is mounted only when the root 'SlipScanning' modal is active. While the user is in Settings (Main tab), that stack is unmounted, so React Navigation cannot resolve the 'SlipConsent' name — the action bubbles to the root unhandled (dev warning, production silent no-op), leaving the consent-management screen unreachable from this button. This is corroborated by the sibling "Slip history" item two rows up (SettingsScreen.tsx:163) which correctly targets the root route `navigate('SlipScanning')`, proving the author knew the root-route pattern but got the consent row wrong; the `as unknown as { navigate: (s: string) => void }` cast bypassed TypeScript so the compiler never flagged the invalid route name. The claim's proposed fix `navigate('SlipScanning', { screen: 'SlipConsent' })` is correct. Severity Medium is appropriate: no crash or data loss, but a privacy/consent-withdrawal control is a dead no-op, which is a real UX and consent-compliance gap (the consent screen remains reachable via the scanning flow itself, so it's not fully orphaned).

### [M12] src/presentation/screens/slipScanning/SlipProcessingScreen.tsx:149 (boot-nav)

**Defect:** On slip-extraction failure, the 'Log manually' action calls `navigation.getParent()?.navigate('Transactions', { screen: 'AddTransaction' })`, but the parent (root stack) has no 'Transactions' route — the tabs live under 'Main' — so the button does nothing and the user is stranded on the failed-processing modal.

**Failure:** Slip OCR extraction fails; the user taps 'Log manually' (handleLogManually, line 149). `navigation` belongs to the inner SlipScanningStackNavigator, so `getParent()` returns the root stack, whose routes do not include 'Transactions' (it is a tab inside the 'Main' MainTabNavigator). `navigate('Transactions', {screen:'AddTransaction'})` is therefore unhandled (dev warning / production no-op): the SlipScanning modal stays open and no manual-entry screen opens. To actually reach the Add Transaction screen the call must go through Main, e.g. `getParent()?.navigate('Main', { screen: 'Transactions', params: { screen: 'AddTransaction' } })`.

**Verify note:** Verified against the actual navigation tree. SlipProcessingScreen (SlipProcessingScreen.tsx:148-150) lives inside SlipScanningStackNavigator (SlipScanningStackNavigator.tsx), which RootNavigator.tsx registers directly as the 'SlipScanning' screen on the Root Stack. Therefore this screen's navigation object is the SlipScanning stack and navigation.getParent() returns the Root Stack. The Root Stack's routes are Main, HouseholdPicker, CreateHousehold, ShareInvite, JoinHousehold, SlipScanning (plus conditional Auth/Onboarding) — there is no 'Transactions' route there. 'Transactions' is a Tab.Screen inside Main -> MainTabNavigator (MainTabNavigator.tsx:55), and 'AddTransaction' is inside that tab's TransactionsStackNavigator (line 28). React Navigation resolves an unknown NAVIGATE route by bubbling UP to parent navigators, not by descending into descendant navigators, so getParent()?.navigate('Transactions', {screen:'AddTransaction'}) is unhandled by the Root Stack: a dev-mode 'action was not handled by any navigator' warning and a silent no-op in production. The 'Log manually' recovery button on the failed-extraction screen thus does nothing, stranding the user. The claim's proposed fix (route through 'Main': navigate('Main', {screen:'Transactions', params:{screen:'AddTransaction'}})) is correct. This manifests on a real device, not just in mocks. Severity Medium is appropriate — no crash or data loss, but a user-facing error-recovery path is completely broken.

### [M13] src/domain/shared/BudgetPeriodEngine.ts:25 (rollover-period)

**Defect:** getPeriodForDate builds period boundaries with new Date(Date.UTC(y, m, paydayDay)) / Date.UTC(y, endMonth, paydayDay-1) with no clamp for short months, so paydayDay 29/30/31 overflows into the next month. The client UI clamps payday to 1-28 (PaydayStep.tsx:31, UpdateHouseholdPaydayDayUseCase.ts:20), but the Postgres CHECK permits 1-31 (supabase/migrations/0001_baseline.sql:67) and the DB->store load paths do NOT clamp (App.tsx:205 & 239, JoinHouseholdScreen.tsx:68, HouseholdPickerScreen.tsx:30), so a payday_day of 29-31 is reachable via join/restore/import.

**Failure:** A member joins a household whose payday_day is 31 (allowed by the server CHECK and set by another client/data import); setPaydayDay(31) flows into the engine. On 2026-03-01, getPeriodForDate(31, Mar 1): day=1<31 so startMonth=Feb(1) and startDate = Date.UTC(2026,1,31) overflows to 2026-03-03 — a period that 'starts' in the FUTURE relative to today. isDateInPeriod(Mar 1) is false, differenceInDays goes negative, and isNewPeriodWithin returns false (daysSinceStart<0), so rollover never fires and February's period (Jan 31 -> Mar 2 per the endDate overflow) is mis-bounded. Every 29/30/31 payday is broken in February and other short months.

**Verify note:** Confirmed by tracing the code. BudgetPeriodEngine.getPeriodForDate builds boundaries with new Date(Date.UTC(startYear, startMonth, paydayDay)) and Date.UTC(endYear, endMonth, paydayDay-1) with no clamp for short months. I reproduced the exact algorithm in Node: paydayDay=31 on 2026-03-01 yields startDate=2026-03-03 (Feb 31 overflows into March) — a period starting in the future; isNewPeriodWithin computes daysSinceStart=-2, so the >=0 guard makes rollover silently never fire and isDateInPeriod(Mar 1) is false. The reachability chain holds: server CHECK in 0001_baseline.sql:67 permits payday_day 1–31, the store setter appStore.ts:73 does not clamp, and every DB->store load path (App.tsx:205/239, JoinHouseholdScreen.tsx:44, HouseholdPickerScreen.tsx:30) passes the raw DB value straight through; getCurrentPeriod is used across BudgetScreen/BabyStepsScreen/SlipScanningScreen so corrupt bounds affect core budgeting, not just the rollover prompt. The only weakening factor is that current client write paths clamp to 1–28 (PaydayStep), so a 29–31 value requires out-of-band data (direct SQL, import, or a differing client) — a latent robustness gap rather than an actively-triggered bug, which matches Medium: reliable silent corruption when it fires but gated behind non-standard data. A prior repo observation independently flagged the same schema drift.

### [M14] src/domain/budgets/StartNewPeriodUseCase.ts:132 (rollover-period)

**Defect:** execute() copies envelopes forward by calling repo.insert(row, ctx) once per source envelope inside a forEach (line 153). createSyncedRepo.insert opens its OWN runInUnitOfWork transaction per call, so N envelopes = N separate transactions. This is exactly the non-atomic multi-write anti-pattern the codebase's own insertRowWithinUow primitive was built to prevent (see createSyncedRepo.ts doc: 'Looping calls to createSyncedRepo(...).insert would instead open N separate transactions — item 1 could commit while item 2 fails').

**Failure:** Rolling 10 envelopes forward; the app is backgrounded/killed (or one insert throws a DB error) after 4 inserts have committed. The new period is left with 4 of 10 envelopes and no period_ack written. Because the only recovery is the dashboard's 2-day rollover window (finding #1), if the user next opens the app on day 3+ the rollover never re-fires and the period is permanently left with a partial envelope set.

**Verify note:** Confirmed by tracing the code. StartNewPeriodUseCase.execute (src/domain/budgets/StartNewPeriodUseCase.ts:132-155) calls repo.insert(row, ctx) inside a forEach, and createSyncedRepo.insert (src/data/uow/createSyncedRepo.ts:342-344) wraps each call in its own runInUnitOfWork -> its own db.transaction, so N envelopes = N independent transactions. This is precisely the anti-pattern the file's own insertRowWithinUow doc (lines 199-213) says it exists to prevent. Because the writes are not atomic, if an insert throws (constraint/disk-full/corruption) or the process is killed mid-loop, some inserts commit while the rest do not, and RolloverWizard.tsx:248 never writes period*ack*${toPeriodStart} (it runs only after the whole loop + edits succeed). Recovery is gated: DashboardScreen.tsx:72 only auto-shows the wizard when isNewPeriodWithin(paydayDay, 2) (BudgetPeriodEngine.ts:49-58, within 2 days of period start) with no manual re-trigger, so on day 3+ a partial period is never healed. Not higher severity because: execute() is idempotent/self-healing (lines 117-134 skip existing deterministic target ids and insert only the remainder), so any re-fire completes the rollover and permanence hinges on the separate 2-day-window defect; runInUnitOfWork is synchronous so the loop has no await yield points (backgrounding alone cannot interrupt it -- only a native process kill or a DB throw); and each per-row transaction is internally consistent (row + its oplog op together), so only completeness of the envelope set is at risk, not money values or sync integrity. Net: a real atomicity defect that violates an explicit documented invariant, correctly rated Medium.

### [M15] supabase/functions/notify-event/index.ts:199 (notifications)

**Defect:** The notify-event edge function has zero callers anywhere in the app, so the entire remote push path is dead code.

**Failure:** Grep across the whole repo for supabase.functions.invoke shows the only edge function ever invoked is 'extract-slip' (src/infrastructure/slipScanning/EdgeFunctionSlipExtractor.ts:48). Nothing ever calls invoke('notify-event'), and the only reader of user_fcm_tokens is this handler. registerFcmToken (App.tsx:253) faithfully writes device tokens on every login, but no code path — no envelope-overspend alert, no shared-transaction notify, no server trigger/cron — ever hits notify-event. Result: a household member logs a shared transaction expecting the partner to get a push; no push is ever sent to anyone, ever. The remote-notification feature is completely non-functional despite full token registration, membership/IDOR checks, rate limiting, and FCM v1 machinery being built.

**Verify note:** The claim is substantiated. Grepping the whole repo for `supabase.functions.invoke` (and `functions.invoke`) returns exactly one production call site — `src/infrastructure/slipScanning/EdgeFunctionSlipExtractor.ts:48` invoking `'extract-slip'`. Every reference to `notify-event` in actual code (`.ts`/`.sql`) lives only in test files (`notify-event-handler.test.ts`, `edge-function-auth.test.ts`, `security-audit-findings.test.ts`, and the Deno `__tests__`); the sole non-test `functions.invoke('notify-event', …)` occurrence is inside a planning doc (`docs/superpowers/plans/2026-04-17-system-completion.md:1145`), never wired into src. I also confirmed there is no server-side trigger path: the only `pg_cron` job in `0001_baseline.sql` is `cleanup-old-slip-images` (line 713), and although `pg_net` is enabled, no migration issues a `net.http_post`/trigger to the edge function. Meanwhile `FcmTokenRegistrar.registerFcmToken` faithfully writes device tokens to `user_fcm_tokens`, and `notify-event/index.ts` (auth at line ~204, membership/IDOR checks, rate limiting via `check_and_reserve_notify_send`, FCM v1 machinery, MAX_SENDS_PER_HOUR at line 193) is fully built — but nothing ever calls it, so no push is ever emitted for shared transactions or envelope alerts. So the remote-push feature is genuinely non-functional (real defect, not mocked-only or already-handled). I downgrade High→Medium: it is a complete functional gap in a secondary notification feature with no data-loss, crash, or accounting-correctness impact, and it is already acknowledged in the repo's own docs as unfinished/planned work (envelopeWarningEnabled called "decorative", notification-outbox proposed as future work), making it a missing-wiring gap rather than a high-severity regression.

### [M16] src/infrastructure/notifications/FcmTokenRegistrar.ts:12 (notifications)

**Defect:** FCM token refresh is never handled — no messaging().onTokenRefresh() subscription exists anywhere, so a rotated token is never written back.

**Failure:** registerFcmToken is only called from initSessionRemote (App.tsx:253), i.e. once per SIGNED_IN / session-restore. A grep for onTokenRefresh/onMessage/setBackgroundMessageHandler returns nothing in the codebase. FCM rotates the device token periodically (app update, restore, GMS refresh). Scenario: user stays logged in for weeks; FCM rotates the token mid-session; the user_fcm_tokens row still holds the old (now-invalid) token. Every push to that user is sent to a dead token (FCM returns UNREGISTERED and the row gets pruned), and the user silently stops receiving notifications until the next full cold-start sign-in re-registers.

**Verify note:** Confirmed real by tracing the code. FcmTokenRegistrar.ts:12-29 fetches the FCM token via messaging().getToken() exactly once and upserts it; it registers no listeners. Its only caller is registerFcmToken(userId) at App.tsx:253, inside initSessionRemote (App.tsx:218), whose own doc comment states it runs once per SIGNED_IN/session-restore bootstrap — so the token is only ever captured at that boot moment. A codebase-wide grep for onTokenRefresh/onMessage/setBackgroundMessageHandler returns no source hits (the only matches are in a review markdown doc noting the very same gap). FCM genuinely rotates device tokens (reinstall, restore, GMS refresh), so a rotation occurring during a long-lived logged-in session is never written back; the user_fcm_tokens row keeps the stale token and pushes silently fail (FCM UNREGISTERED) until the next cold-start sign-in re-registers. This manifests on a real device, not just in mocks. Kept at Medium: it is a silent, self-healing degradation of a non-critical notification path affecting only long-lived sessions, which is why it sits at the low end of Medium rather than High.

### [M17] src/presentation/screens/settings/SettingsScreen.tsx:45 (notifications)

**Defect:** Sign-out never deletes the user_fcm_tokens row nor calls messaging().deleteToken(), so on a shared device the previous user's row keeps the current device's token — a cross-user notification leak.

**Failure:** handleSignOut only calls supabase.auth.signOut() (line 45); App.tsx's auth listener only resets local stores. The FCM token identifies the device install, not the user, and survives user switch unless deleteToken() is called. Scenario: user A signs in on a phone -> row {A, tokenT}. A signs out; user B signs in on the same phone -> getToken() returns the same tokenT -> upsert writes {B, tokenT}. Both rows now reference tokenT. Any push targeting A is delivered to the physical device now used by B, leaking A's private notification content to B (and stale rows accumulate forever). Currently latent only because Finding 1 means no push is ever sent.

**Verify note:** Traced the full path. SettingsScreen.tsx:45 handleSignOut calls only supabase.auth.signOut(); App.tsx's resetAllStoresOnSignOut() (lines 288-300) resets local stores only and never deletes the user_fcm_tokens row nor calls messaging().deleteToken() — grep confirms no deleteToken or table-delete exists anywhere in src. FcmTokenRegistrar.ts upserts {user_id, token} with onConflict:'user_id', and migration 0001_baseline.sql:294 defines the table PK as user_id (not token). Because FCM getToken() is per-install, after user A signs out and user B signs in on the same phone, a new row {B,tokenT} is created while the stale {A,tokenT} row persists; the notify-event edge function selects tokens WHERE user_id=A and would push A's notification to the device now used by B — a real cross-user leak, plus permanent orphaned-row accumulation on real Postgres. The claim honestly notes the leak is presently latent because the push-send path is dead (no push is dispatched today), so the live cross-user delivery only manifests once that companion bug is fixed; the underlying data-hygiene defect itself is real and observable now. Medium is the correct severity given the privacy impact but the shared-device precondition and the current latency behind the dead push path.

### [M18] supabase/migrations/0001_baseline.sql:294 (notifications)

**Defect:** user_fcm_tokens has PRIMARY KEY(user_id) and registerFcmToken upserts onConflict:'user_id', so a user can only ever have ONE token row — a second device silently overwrites the first.

**Failure:** The registrar (FcmTokenRegistrar.ts:18) upserts {user_id, token} with onConflict 'user_id'. Scenario: a user installs the app on their phone (registers {A, token_phone}) and then on a tablet (registers {A, token_tablet}); the upsert overwrites token_phone with token_tablet. The phone's token is gone from the table with no error, so the phone permanently stops receiving pushes — only the most-recently-registered device ever gets notified. The notify-event handler's loop-over-tokens and stale-token-prune logic (index.ts:293-405) implies multi-token/multi-device support that the schema PK structurally forbids.

**Verify note:** Confirmed by tracing all three cited locations. Schema at supabase/migrations/0001_baseline.sql:287-294 defines user_fcm_tokens(user_id, token, updated_at) with ADD CONSTRAINT user_fcm_tokens_pkey PRIMARY KEY (user_id) — a single row per user is structurally enforced by real Postgres. FcmTokenRegistrar.ts:16-21 upserts {user_id, token, updated_at} with { onConflict: 'user_id' }, so registering from a second device overwrites the first device's token row in place, silently and without error. The notify-event edge function (index.ts:292-296) queries .select('token').eq('user_id', userId) into an array, loops over the tokens, and (index.ts:397-405) prunes stale tokens with .delete().eq('user_id', userId).in('token', staleTokens) — array/plural machinery that plainly anticipates multiple tokens per user, which the PK makes impossible. So a phone-then-tablet login leaves only the tablet token; the phone permanently stops receiving pushes with no error. This is a genuine on-real-Postgres, on-real-device functional defect, not a mock artifact or style nit; it is even independently documented in the repo (plan-execution-status.md:233, REBUILD-COMPLETE.md:103). Severity Medium is appropriate: real user-facing loss of multi-device notification delivery, but degrades gracefully (the most-recent device still works), no crash, data loss, or security impact.

## Low (10)

### [L1] src/domain/households/EnsureHouseholdUseCase.ts:68 (write-paths)

**Defect:** Legacy-household migration path writes the owner membership (row + oplog insert op) and the household catch-up oplog insert op in TWO separate transactions, and in the reverse of the bootstrap order CreateHouseholdUseCase documents as required (household op must precede member op).

**Failure:** A user with a pre-sync legacy household (households.id == userId) opens the app. `membersRepo.insert(...)` (createSyncedRepo, transaction A) commits the household_members row + its insert op with an EARLIER seq_local; the household catch-up op is appended later in a separate `runInUnitOfWork` (transaction B) with a LATER seq_local. SyncEngine push orders by `seq_local ASC` (SyncEngine.ts:566), so the member-insert op is pushed to the server BEFORE the household-insert op for a household the server has never seen -- the exact order CreateHouseholdUseCase.ts:51 warns breaks server bootstrap/RLS. Worse, a crash between transaction A and B leaves the member row + member op committed with the household op never appended, permanently orphaning the membership on the server (household op lost).

**Verify note:** The structural claim is verified: `membersRepo.insert` at EnsureHouseholdUseCase.ts:68 resolves to `createSyncedRepo(...).insert`, which opens its own `runInUnitOfWork` (createSyncedRepo.ts:342-344), committing the member row + member op in transaction A; the household catch-up op is then appended in a SEPARATE `runInUnitOfWork` (lines 88-107) — two transactions, member-before-household, the reverse of the atomic single-transaction ordering CreateHouseholdUseCase.ts:46-51 documents as mandatory. Push draining does put the member op ahead of the household op (via the `rowid` tiebreak, since `seq_local` is NULL for all appended ops, not via `seq_local` as the claim states), and server `household_members_household_id_fkey` (0001:87) is non-deferrable, so the member op FK-violates when applied before the household exists. HOWEVER the claim overstates two things. First, ordering does NOT break bootstrap/RLS: `sync_push` Pass 1 authorizes by scanning the whole batch (0002:314-323), so the self-bootstrap is recognized regardless of op order; the only consequence is a transient FK rejection (SQLSTATE 23503), which is NOT in PERMANENT_REJECT_CODES and self-heals on the very next retry once the household op (same batch) has landed — so the common, no-crash path recovers automatically within one backoff cycle. Second, the only path to PERMANENT harm is the crash scenario: an OS kill in the microscopic window between two adjacent synchronous SQLite transactions (no `await` between them) leaves the member row committed but the household op never appended, and the early `if (membership)` return at lines 35-52 never re-appends it, so the member op FK-fails forever and dead-letters — a genuine permanent server orphan. That mechanism is real, so real=true and it does violate the sibling code's explicit atomicity invariant, but it is gated behind a one-time legacy-migration population AND a crash in a sub-millisecond window, with the non-crash case self-healing — hence Low, not Medium.

### [L2] supabase/migrations/0002_fix_household_bootstrap.sql:153 (server-rpc-rls)

**Defect:** apply_one_op writes oplog.actor_user_id directly from the client-supplied `(p_op->>'actor_user_id')::uuid` with no check that it equals auth.uid(). The op-level membership authorization (sync_push household gate + the per-op household_members guard) never validates that the recorded author matches the authenticated caller, so the audit/attribution field is forgeable. (Same in 0001 line 981.)

**Failure:** An authorized household member pushes a normal transaction insert op but sets `actor_user_id` in the wire op to a DIFFERENT member's uuid. The op applies successfully and the oplog row (fanned out to every other client via the supabase_realtime publication on public.oplog) attributes the change to the impersonated user. No privilege escalation, but any 'who changed this' attribution shown to household members is spoofable.

**Verify note:** Confirmed real by tracing the code. In private.apply_one_op (0002 lines 152-154, mirrored in 0001 line 979-981) the oplog row's actor_user_id is written verbatim from (p_op->>'actor_user_id')::uuid with no comparison to auth.uid(). The only per-op identity guard (0002 lines 131-146) fires solely when v_table='household_members' and it validates v_payload->>'user_id' (the membership row's subject), not the actor_user_id attribution field; for a normal transactions insert there is no identity check at all. The authenticated caller's uid IS available in scope (sync_push computes v_caller := auth.uid(), and apply_one_op also reads auth.uid()), so a correct fix would set/verify actor_user_id = auth.uid(), but that is not done. oplog is added to the supabase_realtime publication (0001 line 915) and household members hold SELECT via the oplog_select RLS policy, so a forged actor_user_id is genuinely fanned out to and visible by other clients. Thus an authorized member can push an otherwise-valid op with actor_user_id set to another member's uuid and the change is attributed to the impersonated user. It is not mocked, not unreachable, and not merely stylistic. Impact matches the claim: no privilege escalation or data-integrity loss (the membership guard still blocks force-joining/role changes), only spoofable who-changed-this attribution among already-trusted household members, requiring authenticated membership. That is genuinely Low severity.

### [L3] src/domain/envelopes/UpdateEnvelopeUseCase.ts:65 (envelopes)

**Defect:** UpdateEnvelopeUseCase can change envelope_type but never recomputes or writes is_savings_locked, so the stored savings-locked flag diverges depending on whether an envelope was created-as vs edited-into a savings/emergency_fund type.

**Failure:** User edits a 'spending' envelope in AddEditEnvelopeScreen and changes its type to 'savings' (the segmented control offers savings), then saves. UpdateEnvelopeUseCase builds `updated` by spreading this.current (isSavingsLocked stays false) and the written `fields` (lines 65-72) omit is_savings_locked entirely — so the row persists as envelope_type='savings' with is_savings_locked=0, whereas the identical envelope created fresh via CreateEnvelopeUseCase would be is_savings_locked=1 (line 82-83). The reverse edit (savings -> spending) leaves is_savings_locked=1 on a spending envelope. The savings-locked invariant the create path establishes is therefore silently violated through the edit path.

**Verify note:** Confirmed by tracing the code. UpdateEnvelopeUseCase.ts:65-72 builds the DB `fields` object without `is_savings_locked`, and `updated` (lines 50-63) spreads `this.current`, so the flag is never recomputed when `envelopeType` changes. CreateEnvelopeUseCase.ts:82-83,113 derives and writes `is_savings_locked` from the type (1 for savings/emergency_fund). AddEditEnvelopeScreen.tsx renders a SegmentedButtons control (~lines 233-238) that offers 'savings' and is fully editable during edit (not disabled), and on save with an existing envelope it calls UpdateEnvelopeUseCase with the new envelopeType (lines 125-129) — so a spending→savings edit really persists envelope_type='savings' with is_savings_locked=0, whereas create-as-savings persists 1. StartNewPeriodUseCase.ts:144 copies the flag forward, so the divergent value survives period rollovers. This is a genuine, reproducible data-integrity divergence on real SQLite/Postgres, not mocked-only or already handled. The one caveat that keeps it Low: I found no production branch anywhere that reads isSavingsLocked to enforce anything (BabyStepEvaluator and all consumers merely plumb it through, never conditional on it), so there is no user-visible malfunction today — it is a latent inconsistency that would only bite once some feature actually enforces the savings lock.

### [L4] src/presentation/screens/auth/onboarding/MeterSetupStep.tsx:40 (meter-readings)

**Defect:** The onboarding baseline reading is dated today, so a user who logs their first real reading the same day is blocked by the DUPLICATE_READING guard and cannot save.

**Failure:** MeterSetupStep writes the baseline with reading_date = today (format(new Date(),'yyyy-MM-dd')). If the user finishes onboarding and immediately opens AddReading to log their actual meter value the same day, LogMeterReadingUseCase.findByDate (LogMeterReadingUseCase.ts:44-54) finds the baseline row for the same householdId/meterType/readingDate and returns createFailure DUPLICATE_READING ('A electricity reading already exists for 2026-07-05'). The user cannot record their real reading until the next calendar day, and there is no meter-reading edit/delete path to remove or fix the 0 baseline.

**Verify note:** Confirmed by tracing the code. MeterSetupStep.handleNext inserts a baseline meter_readings row per enabled type with reading_value:0 and reading_date = format(new Date(),'yyyy-MM-dd') (today), via the synced repo; an inline comment states it bypasses LogMeterReadingUseCase precisely because that use case rejects readingValue<=0. AddReadingScreen.handleSave (line 39 today = format(new Date(),'yyyy-MM-dd'); line 107-118) invokes LogMeterReadingUseCase with readingDate=today. execute() calls repo.findByDate(householdId, meterType, readingDate); DrizzleMeterReadingRepository.findByDate matches solely on householdId AND meterType AND readingDate (value-agnostic), so the zero baseline row matches and execute() returns createFailure DUPLICATE_READING. A grep of src/domain/meterReadings and the Drizzle repo found no delete/update path, so the 0 baseline cannot be removed or edited. Therefore a user who enables a meter during onboarding and logs their first real reading for that meter the same calendar day is blocked until the next day. Real and reproducible against local SQLite on a device, but Low severity: narrow trigger, non-destructive, and self-resolves the following calendar day.

### [L5] src/domain/babySteps/ReconcileBabyStepsUseCase.ts:150 (baby-steps)

**Defect:** The completion branch pushes stepNumber into newlyCompleted BEFORE the `if (persisted)` guard, so when a baby_steps row is missing the step is reported as newly-completed (celebration enqueued) but is_completed is never persisted — the transition is never consumed, so it re-fires on every reconcile.

**Failure:** A device joins an existing household via invite (AcceptInvite creates the membership) and opens the dashboard before the household's baby_steps rows have synced down; the EMF/income envelopes have already synced and Step 1 already qualifies as complete. reconcile finds no persisted row (previouslyCompleted defaults false), enters the completion branch, executes newlyCompleted.push(1) at line 150, but skips the repo.update at line 153 because `persisted` is undefined. The celebration modal enqueues; on dismiss StampCelebratedUseCase returns STEP_NOT_FOUND (no row) so celebrated_at is never stamped; the next reconcile still finds no row, re-pushes newlyCompleted=[1], and (given the inert celebrated_at dedup) re-shows the modal in a loop until the rows finally sync/seed.

**Verify note:** CONFIRMED. In ReconcileBabyStepsUseCase.ts the completion branch does `newlyCompleted.push(current.stepNumber)` unconditionally (line ~148) and only guards the `repo.update` persist inside `if (persisted)` (line ~151). So when no baby_steps row exists, the step is reported as newly-completed but is_completed/completed_at are never written. The re-fire chain is real: (1) useBabySteps.reconcile enqueues a celebration for every entry in newlyCompleted; (2) the celebrationStore dedup(b) checker wired in App.tsx (bindCelebrationStore) does `rows[0]?.celebratedAt != null`, which returns false for a missing row, so it does NOT suppress the enqueue; (3) CelebrationModalHost.handleDismiss calls StampCelebratedUseCase which returns createFailure STEP_NOT_FOUND when no row exists, so celebrated_at is never stamped; (4) crucially the PRIMARY guard against re-transition is the persisted is_completed flag (previouslyCompleted), and because the update was skipped that flag stays false, so every subsequent reconcile re-detects the incomplete→complete transition and re-enqueues the modal. reconcile is event-driven (mount + foreground transition + post-toggle), so the modal re-appears on each such trigger rather than in a tight infinite loop. Reachability: rows are normally guaranteed by SeedBabyStepsUseCase, invoked in CreateHouseholdUseCase, EnsureHouseholdUseCase, and at the end of RestoreService.restoreHousehold, plus a backfill after baby_steps restore. AcceptInviteUseCase awaits restoreHousehold but has a fallback that returns success even when restore (and thus the seeder) failed on both attempts. The exact precondition (envelopes restored so Step 1 qualifies, but baby_steps restore failed and the seeder never ran) requires a partial-restore failure ordered after envelopes but at/before baby_steps — a narrow but genuinely reachable window on a joining device. It self-heals on the next app launch (EnsureHousehold seeds the membership's rows) or a successful restore, and the returned statuses/progress remain correct (isCompleted is recomputed from evaluated envelopes), so the only harm is a transient, repeating celebration modal with no persistence — no data loss or corruption. Low severity is appropriate.

### [L6] src/presentation/hooks/useBabySteps.ts:106 (baby-steps)

**Defect:** The backgrounded-completion branch that fires LocalNotificationScheduler.fireBabyStepCelebration is unreachable: reconcile() is only ever invoked while the app is active, so `isActive` (captured at line 81) is always true and the `else` branch at lines 102-108 is dead code. The advertised "notify when a Baby Step completes while backgrounded" feature never runs.

**Failure:** Every reconcile trigger — mount effect (guarded by AppState==='active', line 135), useFocusEffect in BabyStepsScreen/DashboardScreen (only while focused=active), RefreshControl pull (user-initiated=active), toggleManualStep (guarded active, line 170), and the AppState 'change' listener (fires only on background→active transition, at which point currentState==='active') — occurs with the app foregrounded. No background task, headless job, or notification handler ever calls reconcile. Result: fireBabyStepCelebration(stepNumber) is never called in production, so a user whose Step completes while the app is closed gets no push notification (the completion is instead detected as newlyCompleted on next foreground and shown as a modal).

**Verify note:** Confirmed by tracing every call site of `reconcile`. In useBabySteps.ts, `isActive` is captured at line 81 as `AppState.currentState === 'active'`, and the `else` branch (lines 102-108) that calls `scheduler.fireBabyStepCelebration(stepNumber)` at line 106 is the ONLY production caller of that method (verified via grep: the sole non-test callsite is line 106). Every path that invokes `reconcile` runs while the app is foregrounded: (1) the mount effect at line ~135 is guarded by `AppState.currentState === 'active'`; (2) the AppState 'change' listener only calls reconcile when `wasBackground && isNowActive` (nextState==='active'), at which point `currentState` is already 'active'; (3) `toggleManualStep` is guarded by `AppState.currentState === 'active'`; (4) BabyStepsScreen's `useFocusEffect` (line 53) and `RefreshControl onRefresh` (line 101) only run while the screen is focused/interactive, i.e. active. There is no BackgroundFetch/registerHeadlessTask/TaskManager registration anywhere (grep found none), and this being a React hook it requires a mounted component that only renders when the UI is active. So `isActive` is always true when reconcile executes, making the background branch dead code and the advertised "notify when a Baby Step completes while backgrounded" feature genuinely never fire in production. This is a real functional gap, not a mock artifact or style nit. Low severity is appropriate: no crash or data loss, and completions are still surfaced as a modal on next foreground via the newlyCompleted path — the user just loses the backgrounded push notification.

### [L7] src/presentation/screens/dashboard/DashboardScreen.tsx:46 (rollover-period)

**Defect:** Period boundaries are constructed at UTC midnight (Date.UTC in BudgetPeriodEngine) but converted to the period_start ISO key with date-fns format(period.startDate,'yyyy-MM-dd'), which reads LOCAL calendar fields. In any UTC-negative timezone the local date is the day before the UTC instant, so period_start is written/queried one day earlier than the intended payday. All read/write paths share this format() so a single device stays self-consistent, but different devices in different UTC offsets diverge.

**Failure:** Two household members, one at UTC+2 and one at UTC-5, both roll over the payday-25 period. The UTC+2 device formats period.startDate (2026-06-25T00:00Z) as '2026-06-25'; the UTC-5 device formats the same instant as '2026-06-24'. envelopeScopeCondition matches by exact period_start string and rolloverEnvelopeId(household, toPeriodStart, source) hashes the toPeriodStart string, so the two devices create envelopes under different period_start values and different deterministic ids — defeating the intended cross-device convergence and producing duplicated/mismatched period-scoped envelopes.

**Verify note:** Real defect, traced end-to-end. BudgetPeriodEngine.ts:25 builds period.startDate as a UTC-midnight Date via Date.UTC, but every string key is derived with date-fns format(period.startDate,'yyyy-MM-dd') (confirmed at 16 call sites including DashboardScreen.tsx:46), and date-fns format reads the host's LOCAL calendar fields. For a payday-25 boundary (2026-06-25T00:00Z) a UTC+2 device formats '2026-06-25' while a UTC-5 device formats '2026-06-24' — a genuine, well-known Date.UTC-vs-local-format off-by-one that occurs on any UTC-negative offset. That string is the exact-match scope key (useEnvelopes.ts:30 eq(periodStart), StartNewPeriodUseCase.ts:146 writes period_start) AND is hashed into the deterministic rollover id (StartNewPeriodUseCase.ts:58 uuidv5(household:toPeriodStart:sourceId)). So two household members straddling the UTC-0 line create envelopes under different period_start values and different uuidv5 ids, producing duplicated/orphaned period-scoped envelopes and defeating the intended cross-device convergence in the offline-first sync design. A single device stays self-consistent (same format() on read and write; engine's own period math uses getTime()), so the bug only manifests across devices whose UTC offsets differ in sign — a narrow trigger for this predominantly UTC+2 (South African) app, which is why Low severity is appropriate despite the data-integrity nature of the impact when it does trigger.

### [L8] src/presentation/screens/budgets/RolloverWizard.tsx:88 (rollover-period)

**Defect:** toCents() returns 0 for any non-finite parse (parseFloat of '' or garbage), so an emptied or invalid allocation field is silently coerced to R0.00 rather than rejected; separately replace(',', '.') only swaps the first comma, so a thousands-separated string is mis-parsed.

**Failure:** In the adjust step the user clears an envelope's allocation TextInput (value becomes ''). edits computes toCents('') = 0, which differs from the original allocatedCents, so it is treated as an intentional edit and handleCommit writes allocated_cents = 0 for that carried-forward envelope, zeroing the budget silently. Likewise typing '1,234.56' yields parseFloat('1.234.56')=1.234 -> 123 cents (R1.23) instead of R1234.56.

**Verify note:** Substantiated by tracing the real code path. In RolloverWizard.tsx the adjust-step TextInput (line 392) stores the raw string, so clearing an allocation field sets allocationStr[id]=''. The edits memo (line 196) calls toCents(allocationStr[id] ?? '0'); because '' is not undefined the nullish default never fires, and toCents('') = parseFloat('') = NaN → returns 0 (line 88-89). The filter editedCents !== allocatedCents then treats a non-zero carried-forward envelope as an intentional edit, and handleCommit (line 244) performs a real synced-repo/Postgres write of allocated_cents: 0, zeroing the budget. The second part is also correct: replace(',', '.') swaps only the first comma, so '1,234.56' → parseFloat('1.234.56') = 1.234 → 123 cents (R1.23). The codebase already has a safe parseMoneyInput helper (rejects empty/negative) that this local toCents bypasses, matching the recorded 'unsafe parseFloat on 4 screens' finding, so the fix is well-defined. Severity is Low rather than higher because the commit step renders a visible '→ R0.00' preview (lines 418-420) giving the user a checkpoint before the write, and the thousands-separator mis-parse is only reachable via paste on a single-separator decimal-pad keyboard; but the empty-field → R0.00 coercion is a genuine, reachable money-input validation gap on a real write path.

### [L9] src/presentation/screens/settings/NotificationPreferencesScreen.tsx:176 (notifications)

**Defect:** envelopeWarningEnabled is an inert toggle: it is rendered and persisted but no code path ever reads it to schedule or send any notification.

**Failure:** The 'Envelope overspend warning' Switch (line 176-177) writes envelopeWarningEnabled via updatePref. Grep shows envelopeWarningEnabled is referenced only in this screen toggle, the defaults (NotificationPreferences.ts:8/18), the store, and tests — never in any scheduler call or notify-event invocation. Toggling it has zero effect; a user who turns it ON believes they will be alerted when an envelope nears its limit but no alert mechanism is wired to this flag at all (and with Finding 1, the remote warning it implies is never sent regardless).

**Verify note:** Confirmed real. In NotificationPreferencesScreen.tsx:176-177 the "Envelope overspend warning" Switch reads and writes preferences.envelopeWarningEnabled via updatePref, and the flag is declared/defaulted (true) in NotificationPreferences.ts:8,18. However, the only place preferences are turned into scheduled notifications is RootNavigator.tsx (lines 91/97/100), which gates scheduler calls on eveningLogPromptEnabled, meterReadingReminderEnabled, and monthStartPreflightEnabled — there is no branch for envelopeWarningEnabled. LocalNotificationScheduler.ts exposes only scheduleEveningLogPrompt, scheduleMeterReadingReminder, and scheduleMonthStartPreflight (plus a permission helper); no envelope-warning method exists. A repo-wide grep shows envelopeWarningEnabled appears only in the toggle, the interface/defaults, the store, and tests — never in any scheduler or notify-event invocation. Thus toggling it has zero runtime effect: a user enabling it expects overspend alerts and receives none. It manifests on a real device as a misleading/dead UI control, which is a genuine functional gap but low impact (no crash, data loss, or security issue), so Low severity is appropriate.

### [L10] src/presentation/screens/settings/NotificationPreferencesScreen.tsx:53 (notifications)

**Defect:** A single shared debounce timer is used for both the evening-log hour and minute inputs, so editing both within 600ms drops the earlier field's change.

**Failure:** debouncedUpdatePref stores one timer in debounceTimer.current and, on each call, clears the previous timer and schedules updatePref(update) with only that call's partial. updatePref computes {...preferences, ...update} from the render-time preferences snapshot (setPreferences hasn't run yet because the first timer was cleared). Scenario: current prefs hour=19, minute=0. User types hour=20 (timer set with {hour:20}), then within 600ms taps the minute field and types 30 (timer cleared and replaced with {minute:30}). Only {minute:30} fires against the stale snapshot, so the saved prefs become hour=19, minute=30 — the hour=20 edit is silently lost and the evening prompt is scheduled at 19:30 instead of 20:30.

**Verify note:** Confirmed real by tracing NotificationPreferencesScreen.tsx. A single shared debounceTimer ref (line 21) backs debouncedUpdatePref (lines 50-58), which clearTimeouts the prior pending call on every invocation and schedules updatePref with only the current partial. updatePref (line 23) merges as {...preferences, ...update} from the closure's render-time snapshot. Both the hour input (line 94) and minute input (line 106) route through this same timer. In the 600ms window setPreferences never runs (the earlier timer is cleared before firing), so the preferences snapshot stays stale. Sequence hour=20 then minute=30 within 600ms clears the {hour:20} callback and fires {minute:30} against {hour:19,minute:0}, persisting and scheduling 19:30 — the hour=20 edit is silently lost. Partials never accumulate. This is a real closure/timing data-loss bug on-device (no mocks involved), but it needs sub-600ms cross-field typing and only yields a correctable wrong reminder time, so Low is the right severity.

---

## Synthesis

I'll produce the synthesis report. The task is clear and self-contained—these are confirmed defects to rank and organize. Let me write the report directly.

Note: the JSON was truncated mid-entry on the final defect (MeterSetupStep baseline reading_value: 0), but I have enough to include it.

---

# AccountingV2 — Confirmed Defect Synthesis (40 verified)

## CRITICAL (3) — feature entirely non-functional / money-scope corruption

**C1. `supabase/functions/extract-slip/index.ts:137` — Slip-scan status contract mismatch (`processing` vs `pending`)**
Every real slip is written with `status:'processing'`, but the step-5 idempotency guard 409s on `processing` and `check_and_reserve_slip_slot` only reserves `WHERE status='pending'`. Slip scanning fails for **every user**; green tests only pass because they seed `pending` rows.
_Fix:_ Align the guard and RPC to the real writer contract — reserve/guard on `processing`, not `pending`.

**C2. `supabase/functions/extract-slip/index.ts:152` — Reserve-slot RPC called with service-role client → `auth.uid()` NULL → spurious 429**
The admin client carries no user JWT, so the SECURITY DEFINER RPC's `auth.uid()` guard returns `unauthorized`, which index.ts mis-maps to a `429 Household rate limit`. No slip can ever be reserved in production.
_Fix:_ Have the RPC trust the already-authenticated `p_user_id`, or invoke it with the caller-scoped client that forwards the user JWT.

**C3. `src/presentation/hooks/useEnvelopes.ts:31` — Exact `period_start` equality drops all persistent envelopes**
`eq(period_start, periodStart)` permanently excludes sinking funds, emergency funds, savings, and baby-step envelopes from every period after creation. Sinking-funds screen shows empty, Budget/Dashboard drop the envelopes from totals, and `toAssign` is over-reported so the zero-based budget lies about unassigned money.
_Fix:_ Replace equality with `envelopeScopeCondition(periodStart)` + `isNull(deletedAt)`, matching `DrizzleEnvelopeRepository.listByHousehold`.

## HIGH (9) — silent money corruption, authz bypass, crashes, wrong guidance, brick

**H1. `src/presentation/screens/debtSnowball/LogPaymentScreen.tsx:45` — Debt payment parsed with raw `parseFloat*100`**
`'1,500'` → R1.00; `'1500,50'` → drops 50c. Writes wrong amount to the synced money ledger + audit log, silently. (Two confirmed entries point at this same line.)
_Fix:_ Use `parseMoneyInput` (already wired into the four "add" money screens; this fifth screen was missed).

**H2. `src/data/sync/SyncEngine.ts:760` — Own-device skip + unguarded UPDATE apply = permanent LWW divergence**
A device that pulls a lower-seq remote update overwrites its own newer (winning) value and never replays its own op, so the replica that won on the server displays the losing value forever.
_Fix:_ Add a seq/version guard to the update-apply, or replay own ops in server-seq order instead of skipping them.

**H3. `supabase/functions/notify-event/index.ts:266` — Membership check missing `deleted_at IS NULL` + `.single()`**
Removed members keep push-send access (one soft-deleted row still matches); anyone who left-and-rejoined has 2 rows → `.single()` errors → spurious 403 forever.
_Fix:_ Add `.is('deleted_at', null)` and use `.maybeSingle()`, matching extract-slip.

**H4. `src/presentation/screens/slipScanning/SlipQueueScreen.tsx:165` — Tapping a completed slip omits `extraction` param → crash**
`SlipConfirmScreen` dereferences `extraction.items` on undefined → TypeError, white-screen. Completed-slip review is entirely unreachable.
_Fix:_ Rehydrate the extraction from the DB by `slipId` before navigating, or guard + reload in SlipConfirm.

**H5. `src/presentation/screens/slipScanning/SlipQueueScreen.tsx:155` — Failed-slip re-entry passes raw model JSON as `extraction`**
`rawResponseJson` is snake_case OpenAI output, not a normalized `SlipExtraction`; every `item.amountCents` is undefined → "RNaN" rendered, the `<=0` guard is bypassed, and `amountCents:undefined` is inserted into the ledger. The failed-slip recovery path is 100% broken.
_Fix:_ Normalize the raw response into `SlipExtraction` before navigating, or re-run the mapper.

**H6. `src/presentation/screens/debtSnowball/LogPaymentScreen.tsx:45` — (locale variant of H1)** space/comma-grouped and af-ZA decimal-comma amounts silently coerced. Same file/line, same fix as H1 — treat as one remediation.

**H7. `src/domain/debtSnowball/CreateDebtUseCase.ts:51` — `sortOrder` = insertion order, but projector promises smallest-balance-first**
Snowball pours the entire extra payment into the earliest-entered debt (e.g. a R500k bond over a R2k card), inverting the Ramsey snowball. Payoff dates, debt-free date, and on-screen plan are all wrong. No reorder UI exists.
_Fix:_ Derive `sortOrder` from balance (or sort by balance in the projector), and/or add reorder support.

**H8. `App.tsx:392` — Cold-start `getSession()` await has no try/catch → permanent splash brick**
A SecureStore/keychain failure (post-OS-update, biometric-locked keystore, corrupt entry) rejects, skips `setLocalBootReady(true)`, and the app hangs on the native splash with no error UI and no escape short of reinstall.
_Fix:_ Wrap `getSession()` in the same try/catch degrade-on-error pattern already used for `initSessionOnce`.

## MEDIUM (10)

**M1. `src/presentation/screens/budgets/RolloverWizard.tsx:89 & :88` — local `toCents` instead of `parseMoneyInput`**
Emptied field → silent R0 allocation for the new period; `'2,500'` → R2.50; `'1,234.56'` → R1.23. Writes corrupted `allocated_cents` to the synced envelopes repo with no error. (Two confirmed entries, same component.)
_Fix:_ Use `parseMoneyInput`; block commit on invalid/empty input.

**M2. `src/presentation/screens/auth/onboarding/IncomeStep.tsx:13` — onboarding income `toCents`** grouped/pasted `'25,000'` → R25.00, poisoning the entire generated budget. _Fix:_ `parseMoneyInput`.

**M3. `src/presentation/screens/auth/onboarding/AllocateEnvelopesStep.tsx:25` — allocation `toCents`** `'1,000'` → R1.00; either blocks onboarding with a spurious "off by RX" or persists wrong per-envelope allocations. _Fix:_ `parseMoneyInput`.

**M4. `supabase/migrations/0001_baseline.sql:1171` — `sync_row_state` queries non-existent `households.household_id`** Discarding a dead-lettered `households` op throws `42703`; the op is permanently stuck in the DLQ. Same bug class 0002 fixed in `apply_one_op` but never applied here. _Fix:_ Special-case `households` (scope = `id`) as 0002 does.

**M5. `src/data/sync/SyncEngine.ts:811 — delete branch, unguarded `deleted_at` overwrite** Concurrent double-delete leaves each replica holding the _other's_ tombstone timestamp, breaking R4's "same tombstone everywhere". _Fix:_ Guard with `deleted_at IS NULL` / keep the max timestamp.

**M6. `src/data/sync/SyncEngine.ts:818 — `debts.is_paid_off` never set on non-origin devices** Server trigger has no client equivalent, so a pulled payment leaves `is_paid_off=0` at balance 0 on other devices; "PAID OFF" badge and Log-Payment affordance are wrong. _Fix:_ Re-derive `is_paid_off` from balance in the increment-apply, or add a client trigger.

**M7. `src/presentation/screens/slipScanning/SlipQueueScreen.tsx:148 — tapping a `processing` slip omits required params** Kicks off a fresh scan with `frameLocalUris:undefined` → TypeError in `CaptureSlipUseCase`, unhandled rejection, permanent spinner; never resumes the stuck slip. _Fix:_ Pass full params or route stuck slips to a resume/inspect path with a `.catch`.

**M8. `src/presentation/screens/meters/AddReadingScreen.tsx:91` (and `:57`) — reading value uses raw `parseFloat`** `'12,5'` → 12, silently truncating; corrupts every downstream consumption delta and unit rate. The cost field on the same screen is protected; the reading value is not. _Fix:_ Locale-safe numeric parse that rejects ambiguous input.

**M9. `src/presentation/screens/auth/onboarding/MeterSetupStep.tsx:55` — baseline seeded with `reading_value: 0`** First real reading is differenced against 0, so consumption = entire meter face value and unit rate is off by orders of magnitude for every onboarded meter. _Fix:_ Don't seed a 0 baseline — capture a real opening reading or mark the first reading as the baseline.

_(The provided JSON was truncated inside M9; summary reflects the confirmed portion.)_

---

## Themes (recurring root causes)

1. **Locale-unsafe money/number parsing (largest cluster: H1, H6, M1, M2, M3, M8).** Raw `parseFloat` / homegrown `toCents` with `replace(',', '.')` scattered across screens instead of the existing `parseMoneyInput`. A remediation added the safe parser to _some_ screens; every screen touching money/numbers that was missed silently corrupts data. Systemic, not incidental.

2. **Sync-apply ignores server ordering / has no per-row guards (H2, M5, M6).** The own-device-skip plus unconditional overwrite in `applyOne` breaks LWW convergence for update, delete, and derived-column cases. One mechanism, three defects.

3. **Contract drift between client, edge functions, and SQL (C1, C2, H3, M4).** Status enums (`processing` vs `pending`), auth context (service-role vs user JWT), soft-delete predicates, and scope columns diverge across the client / edge / migration layers. Fixes applied in one place (e.g. migration 0002, the notify pattern in extract-slip) weren't propagated to siblings.

4. **Tests validate the happy path with seeded/mocked state that masks the real contract (C1, C2, H3, H4, H5).** Green suites hide production-fatal bugs because fixtures seed `pending` rows, mock `{allowed:true}`, mock `.single()` to one row, or mount screens with fully-populated params — never exercising the real code path.

5. **Read-path scope logic diverges from the repository (C3).** UI hooks reimplement query scoping instead of reusing `envelopeScopeCondition`, so the direct-DB read path silently disagrees with the canonical repository.

6. **Missing error handling on async boot/navigation paths (H4, H5, H8, M7).** Unwrapped awaits and missing `.catch` turn recoverable failures into crashes, unhandled rejections, permanent spinners, or a full app brick.

## Recommended fix order

1. **C1 + C2 together** — one PR restores the entire slip-scan feature (status contract + RPC auth). They share the same file and code path.
2. **C3** — one-line scope fix restores correct budget totals and `toAssign`; highest-value, lowest-risk critical.
3. **H1/H6 (+ M1, M2, M3, M8) as a single "locale money-parse" sweep** — audit every numeric/money input for `parseFloat`/`toCents`, replace with `parseMoneyInput`. Batching prevents leaving another missed screen. Add a lint/grep guard against raw `parseFloat` in input handlers.
4. **H8** — trivial try/catch, prevents an unrecoverable brick; ship early.
5. **H3** — add `deleted_at` filter + `maybeSingle()`; closes an authz bypass and a rejoin break.
6. **H2 (+ M5, M6) as a sync-apply hardening PR** — add per-row seq/tombstone guards and derived-column handling; these share the `applyOne` mechanism.
7. **H4, H5, M7** — slip queue navigation/param + error-handling fixes (same file, related).
8. **H7** — snowball ordering (wrong financial guidance).
9. **M4, M9** — DLQ households RPC and onboarding baseline; lower frequency, isolated.

Fastest risk reduction: knock out the three Criticals plus H8 first (small, high-impact, low-blast-radius), then run the locale-parse sweep and the sync-apply hardening as two themed batches.
