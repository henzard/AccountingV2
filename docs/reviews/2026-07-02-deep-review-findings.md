=== CODE REVIEWS ===

## [C] Domain: budgets & envelopes (src/domain/budgets, src/domain/envelopes, src/domain/shared)

STRENGTHS: Money is integer cents end-to-end in the domain | BudgetBalanceCalculator (src/domain/budgets/BudgetBalanceCalculator | Consistent Result<T, DomainError> pattern (src/domain/shared/types | BudgetPeriodEngine boundary test coverage is strong: December→January year crossover, paydayDay=1, Feb 28, exact window-day boundaries for isNewPeriodWithin, and inclusive start/end for isDateInPeriod (src/domain/shared/**tests**/\*) | paydayDay is constrained to 1-28 at the household use-case level (src/domain/households/UpdateHouseholdPaydayDayUseCase | Every envelope write is scoped by householdId in the WHERE clause, and CreateTransactionUseCase re-verifies envelope ownership before mutating — good multi-tenant hygiene

- (high/VERIFIED) Envelope spentCents mutations from transactions are never enqueued for sync — remote balances go stale and LWW clobbers concurrent spends [src/domain/transactions/CreateTransactionUseCase.ts:92-104]
  detail: CreateTransactionUseCase updates envelopes.spentCents (and DeleteTransactionUseCase.ts:36 decrements it) but sets only updatedAt — no isSynced: false and no enqueuer.enqueue('envelopes', ...) call; only the transaction row is enqueued (line 121). SyncOrchestrator is pending_sync-queue driven (src/data/sync/SyncOrchestrator.ts:85), and the Supabase merge functions (supabase/migrations/016/017) upse
  fix: Short term: inside the same db.transaction, set isSynced: false on the envelope and enqueue an 'envelopes' UPDATE alongside the transaction INSERT. Long term: stop storing spentCents as a mutable synced field — derive it per period from SUM(transactions.amountCents) so sync merges are commutative (s
- (high/VERIFIED) Period rollover does not exist — the modal claims 'Your envelopes have been reset' but envelopes simply vanish at the new period [src/presentation/screens/dashboard/PeriodRolloverModal.tsx:35]
  detail: Envelopes are keyed by exact periodStart string (useEnvelopes.ts:26-32 filters eq(periodStart)). There is no copy-forward/reset use case anywhere (grep for carry-forward/clone/copy across src returns nothing; the only envelope inserts are CreateEnvelopeUseCase and the repository). When payday passes, getCurrentPeriod yields a new periodStart, the query returns zero rows, and the user's entire budg
  fix: Build a StartNewPeriodUseCase: copy forward non-archived envelopes into the new periodStart with type-aware rules (spending resets spentCents, sinking_fund/savings carry accumulated balance), run it when isNewPeriodWithin fires, and make the modal's acknowledge button actually execute it. Rename the
- (medium/VERIFIED) Domain layer purity is broken: envelope use cases import drizzle-orm, expo-crypto, and concrete data-layer classes [src/domain/envelopes/CreateEnvelopeUseCase.ts:1-8]
- (medium/VERIFIED) UTC/local timezone mismatch between BudgetPeriodEngine and screen-layer periodStart formatting [src/domain/shared/BudgetPeriodEngine.ts:10-34]
- (medium/unverified) UpdateEnvelopeUseCase invariant holes: type change to 'income' bypasses the no-spending rule, isSavingsLocked goes stale, spentCents input silently ignored, archived envelopes editable [src/domain/envelopes/UpdateEnvelopeUseCase.ts:47]
- (medium/unverified) Envelope use cases are non-atomic: write, audit log and sync enqueue are separate awaits with no transaction [src/domain/envelopes/CreateEnvelopeUseCase.ts:70-95]
- (medium/unverified) No integer or upper-bound validation on money inputs in domain use cases [src/domain/envelopes/CreateEnvelopeUseCase.ts:40]
- (low/unverified) getPercentRemaining reports 100% remaining for a zero-allocation envelope even when overspent [src/domain/envelopes/EnvelopeEntity.ts:31]
- (low/unverified) BudgetPeriodEngine silently produces overlapping/garbage periods for paydayDay outside 1-28; defense-in-depth gap [src/domain/shared/BudgetPeriodEngine.ts:25]
- (low/unverified) SinkingFundProjector: overdue isOnTrack contradicts its own comment, and sub-month targets understate urgency [src/domain/envelopes/SinkingFundProjector.ts:40]
- (low/unverified) ArchiveEnvelopeUseCase has no domain rules: archives income envelopes silently unbalancing the budget, no already-archived check, fabricated audit previousValue [src/domain/envelopes/ArchiveEnvelopeUseCase.ts:41]
  UPGRADE IDEAS:
  - (transformative) Derive envelope balances from the transaction ledger (kill stored spentCents): Make transactions the single source of truth and compute per-period spentCents as SUM(amountCents) via a SQL view or repository query. This eliminates the entire class of bugs found here: missed sync enqueues, LWW clobbering of concurrent spends by h
  - (transformative) A real 'New Month Ceremony' rollover engine: The Ramsey method's monthly zero-based budget review is currently a data-loss event. Build a StartNewPeriodUseCase with per-type carry rules (spending resets, sinking_fund and savings accumulate savedCents across periods, utility copies allocation) p
  - (major) Branded value objects: Cents, PeriodKey, and a timezone-free CalendarDate: Introduce `type Cents = number & { __brand: 'cents' }` with a smart constructor enforcing safe non-negative integers, and a CalendarDate value object ({y,m,d} with compare/add-month) used by BudgetPeriodEngine, envelope periodStart, and transactionDa
  - (major) Ports-and-adapters extraction with an in-memory test kit: Define IEnvelopeRepository, IAuditLog, IClock, IIdGenerator ports in src/domain/ports and ship an in-memory implementation of each. Use cases become constructor-injected pure orchestrations testable in microseconds without the brittle hand-rolled dri
  - (moderate) 'Safe to spend today' — turn the pure calculators into a daily pacing signal: The building blocks already exist (getRemainingCents, BudgetPeriodEngine day arithmetic, CashFlowForecaster's daily-rate idea). Compute per-envelope and whole-budget 'safe to spend today' = remaining / days left in period, render it as the dashboard
  - (moderate) Domain event stream for budget moments: Emit typed domain events (EnvelopeOverspent, BudgetBalanced, PeriodRolled, SinkingFundOnTrackChanged) from the use cases via a lightweight publisher port. Notifications, Ramsey Score, coaching nudges and the audit log all become subscribers instead o

## [C] Domain: debt snowball & baby steps

STRENGTHS: BabyStepEvaluator is a genuinely pure function (no clock, no I/O) with a strong 35-scenario table-driven test matrix covering threshold-1/exact/+1 boundaries, regression, and no-source-data cases for every step (src/**tests** colocated: src/domain/babySteps/**tests**/BabyStepEvaluator | BabyStepRules | The celebrated_at one-shot-for-life invariant is carefully designed and enforced (StampCelebratedUseCase idempotency, ReconcileBabyStepsUseCase never clears it on regression or re-completion) and is tested | SeedBabyStepsUseCase is correctly idempotent under concurrency via the unique (household_id, step_number) index + onConflictDoNothing | SnowballPayoffProjector has sensible guardrails: 600-month safety cap, a short-circuit for total payment <= 0 (with a test asserting it does not loop), and zero-interest month-count tests that verify rollover of paid-off minimums | All money is integer cents throughout; interest is applied before payment each month (standard convention); Result<T> error contract is used consistently in use cases | LogDebtPaymentUseCase shows race awareness — total_paid_cents is incremented via a SQL expression instead of read-modify-write, and payments are clamped so balance never goes negative | useBabySteps coalesces concurrent reconciles via an in-flight promise ref and correctly splits foreground (modal) vs background (notification preview) celebration paths

- (high/VERIFIED) Baby step writes are never enqueued to pending_sync — progress silently never uploads [src/domain/babySteps/ReconcileBabyStepsUseCase.ts:137 (also ToggleManualStepUseCase.ts:41, StampCelebratedUseCase.ts:47, SeedBabyStepsUseCase.ts:43)]
  detail: Every baby_steps write sets isSynced=false but no production code ever calls enqueue('baby_steps', ...). SyncOrchestrator is purely pending_sync-queue-driven (it never scans for isSynced=false rows), so manual step toggles (4/5/7), completedAt, and celebratedAt never reach Supabase. The orchestrator even has a merge_baby_step RPC route (SyncOrchestrator.ts:48) and RestoreService restores baby_step
  fix: Inject ISyncEnqueuer into all four baby-step use cases (the pattern already exists — see ReconcileEmergencyFundTypeUseCase which does enqueue) and enqueue('baby_steps', row.id, 'INSERT'|'UPDATE') after every write. Add an integration test asserting a manual toggle produces a pending_sync row.
- (high/VERIFIED) Period rollover regresses Steps 1 and 3 every month — emergency fund is modeled as a period-scoped envelope [src/domain/babySteps/ReconcileBabyStepsUseCase.ts:35-43]
  detail: Reconcile reads only envelopes where periodStart = currentPeriodStart. Envelopes are per-period rows (schema/envelopes.ts:15) and there is no copy-forward mechanism anywhere in src (no clone/copy use case exists); PeriodRolloverModal.tsx:35 literally tells the user 'Your envelopes have been reset.' So on the first reconcile after payday the EMF envelope for the new period doesn't exist (or has 0 b
  fix: Either model the EMF as a household-level persistent fund (not period-scoped), or make reconcile fall back to the most recent period that has an EMF envelope, or carry EMF/savings allocations forward automatically at rollover. Add a regression test: 'period rolls over with no new-period envelopes →
- (high/VERIFIED) Snowball order is debt-creation order, not smallest-balance-first — the core Ramsey ordering is never enforced [src/domain/debtSnowball/SnowballPayoffProjector.ts:29 (root cause: CreateDebtUseCase.ts:56-60)]
  detail: The projector sorts by sortOrder, and DebtEntity.ts:17 claims 'sortOrder: 0 = smallest balance first (Ramsey default)'. But sortOrder is assigned as COUNT(debts in household) at creation time and is never updated anywhere — there is no reorder use case, no drag-reorder UI, and no re-sort by outstanding balance. A user who enters their R120,000 car loan first and their R3,000 store card second gets
  fix: Default the projection ordering to outstandingBalanceCents ascending (tie-break by interest rate desc), keeping sortOrder only as an explicit user override once a reorder UI exists. Add a test where insertion order differs from balance order.
- (high/VERIFIED) LogDebtPaymentUseCase: balance update is a stale-snapshot lost-update, and the two writes are not transactional [src/domain/debtSnowball/LogDebtPaymentUseCase.ts:43-60]
  detail: The code comments that total_paid_cents is incremented atomically 'to avoid lost-update races' — but the very next statement writes outstandingBalanceCents computed from the caller-supplied currentDebt snapshot. Two concurrent/double-tapped payments of R500 against a R10,000 balance both compute newBalance = 9500: totalPaid correctly becomes 1000 while the balance loses one payment (ends at 9500 i
  fix: Do the whole payment as one atomic UPDATE: SET outstanding_balance_cents = MAX(0, outstanding_balance_cents - :amount), total_paid_cents = total_paid_cents + :applied, is_paid_off = (outstanding_balance_cents - :amount <= 0), inside a db.transaction() with the audit row. Clamp against the DB value,
- (medium/unverified) Projector loses the payoff-month surplus — rollover math is off by up to one full payment per debt [src/domain/debtSnowball/SnowballPayoffProjector.ts:74-80]
- (medium/unverified) extraMonthlyPaymentCents is dead code — no caller ever passes an extra payment [src/presentation/screens/debtSnowball/SnowballDashboardScreen.tsx:36]
- (medium/unverified) DebtDetailScreen projects each debt in isolation, contradicting the dashboard's snowball dates [src/presentation/screens/debtSnowball/DebtDetailScreen.tsx:64]
- (medium/unverified) Soft-deleted debts (deleted_at tombstones) are never filtered — they poison projections and permanently block Steps 2/6 [src/domain/babySteps/ReconcileBabyStepsUseCase.ts:62 (also useDebts.ts:23-27, DrizzleDebtRepository.ts:21-24)]
- (medium/unverified) Step 3 has two independent income computations that can disagree; baseline=0 with income>0 would auto-complete with target 0 [src/domain/babySteps/BabyStepEvaluator.ts:105-115]
- (medium/unverified) Households with no recorded non-bond debts can never complete Step 2, and renters can never complete Step 6 [src/domain/babySteps/BabyStepEvaluator.ts:84-85 and 121-122]
- (low/unverified) Reconcile emits newlyCompleted/newlyRegressed without persisting when the baby_steps row is missing — repeat celebrations [src/domain/babySteps/ReconcileBabyStepsUseCase.ts:124-168]
- (low/unverified) ToggleManualStepUseCase reports success when no row was updated and swallows nothing else consistently [src/domain/babySteps/ToggleManualStepUseCase.ts:35-45]
- (low/unverified) CreateDebtUseCase derives sortOrder from row count — duplicates under multi-device or after any future delete [src/domain/debtSnowball/CreateDebtUseCase.ts:56-60]
- (low/unverified) Projector clock is non-injectable and month indexing overstates dates by design ambiguity; test coverage gaps on interest paths [src/domain/debtSnowball/SnowballPayoffProjector.ts:35]
  UPGRADE IDEAS:
  - (transformative) Interactive 'what-if' payoff simulator with extra-payment slider: The projector already accepts extraMonthlyPaymentCents — expose it. A slider on the snowball dashboard that live-recomputes the debt-free date, total interest paid, and interest saved vs minimums-only as the user drags (the simulation is trivially fa
  - (transformative) Close the loop: auto-log debt payments from transactions: Today users budget in envelopes and separately hand-type debt payments — double entry that will drift. Introduce a 'debt' envelope type linked to a debtId: when a transaction posts against it, LogDebtPaymentUseCase fires automatically, the balance up
  - (major) Full amortization engine with payoff timeline visualization: Replace the flat monthly loop with a schedule-aware engine: per-debt payment due dates, daily/monthly compounding options, once-off lump-sum inputs ('13th cheque in December'), and promo-rate expiry (store accounts). Emit a month-by-month schedule (i
  - (major) Make the emergency fund a persistent household fund with contribution history: Promote the EMF out of the period-scoped envelope model into a household-level fund with a running balance and contribution ledger (deposits/withdrawals as first-class events). This fixes the monthly Step 1/3 regression structurally, enables a 'month
  - (moderate) Projection history and momentum tracking: Persist a monthly snapshot of the projected debt-free date and total outstanding. Then show trend deltas: 'Your debt-free date moved 4 months closer since January' and 'snowball grew from R1,500 to R2,300/month.' Feed the same snapshots into the Rams

## [C-] Domain: scoring, coaching & forecasting (src/domain/scoring, src/domain/coaching, src/domain/forecasting)

STRENGTHS: Pure, deterministic, well-typed calculator classes (HabitScoreCalculator, CashFlowForecaster, SpendingCoach) with explicit input/output interfaces and integer-cents money throughout — no float money anywhere | Degenerate cases are handled deliberately with rationale comments: totalDaysInPeriod=0 yields 0 logging points, totalEnvelopes=0 yields full discipline points ('nothing to overspend'), daysElapsed clamped to >=1 | CashFlowForecaster's day-count semantics are carefully documented (+1 so today counts as elapsed; daysRemaining exclusive of today) and verified in tests (daysElapsed=10, daysRemaining=20 for the canonical Apr 1–30 / Apr 10 case) | LevelAdvancementEvaluator has good boundary tests: exact thresholds (70,70,70 advances; 59,59 warns), insufficient history, empty array | The coaching intercept UX is a sound pre-commitment device: SpendingCoach

- (medium/VERIFIED) Entire gamification leveling loop is dead code — levels can never advance [src/presentation/hooks/useLevelAdvancement.ts:13]
- (medium/VERIFIED) Ramsey Score is silently capped at 80: meterReadingsLoggedThisPeriod hardcoded to false [src/presentation/screens/dashboard/DashboardScreen.tsx:97]
- (medium/VERIFIED) Logging component rewards spending frequency, not logging fidelity — a perverse incentive in a frugality app [src/domain/scoring/RamseyScoreCalculator.ts:20]
- (medium/unverified) totalDaysInPeriod hardcoded to 30 while payday periods are 28–31 days [src/presentation/screens/dashboard/DashboardScreen.tsx:94]
- (medium/unverified) '90-Day Forecast' screen title is false — the forecaster projects only to the current period end [src/presentation/screens/forecasting/ForecastScreen.tsx:53]
- (medium/unverified) Naive linear run-rate makes early-period forecasts alarmist: one day-1 lump payment flags the envelope over_budget for weeks [src/domain/forecasting/CashFlowForecaster.ts:48]
- (medium/unverified) Zero-allocation envelope with real spending reports on_track / '100% projected left' [src/domain/forecasting/CashFlowForecaster.ts:52]
- (medium/unverified) Domain layer purity violated: scoring resolvers import drizzle-orm and the data-layer schema [src/domain/scoring/resolveLoggingDays.ts:1]
- (low/unverified) Discipline component counts income and sinking-fund envelopes as 'on budget', inflating the score [src/presentation/screens/dashboard/DashboardScreen.tsx:91]
- (low/unverified) SpendingCoach content: all-negative shame-based tone, repeats allowed, currency-locked copy, no severity scaling [src/domain/coaching/SpendingCoach.ts:13]
- (low/unverified) Coach evaluates backdated transactions against current-period envelope aggregates [src/presentation/screens/transactions/AddTransactionScreen.tsx:153]
- (low/unverified) Duplicated parallel test suites for every module in the subsystem [src/domain/coaching/__tests__/spending-coach.test.ts:1]
  UPGRADE IDEAS:
  - (transformative) Score history table + real progression engine: Persist the habit score at every period rollover into a synced score_history table (householdId, periodStart, score, component breakdown). This single addition revives the entire dead gamification loop: LevelAdvancementEvaluator finally gets real rec
  - (transformative) Commitment-aware forecasting with recurring-transaction detection: Replace the naive run-rate with a two-bucket model: (1) committed spend — recurring transactions detected by payee/amount fingerprinting across periods (rent, insurance, subscriptions) projected as scheduled remainder, and (2) discretionary spend — E
  - (major) Forecast-powered coaching: replace canned quotes with a personal what-if: Wire CashFlowForecaster into the CoachingModal: when a transaction would overspend, show 'If you proceed, Groceries runs dry on the 22nd — 8 days before payday' computed from the user's own run-rate, alongside a concrete alternative ('move R450 from
  - (major) Ramsey Score v2: score the actual Ramsey journey, not spending frequency: Rebuild the formula around the methodology the app is named for: emergency-fund funded %, consecutive on-time snowball payments, sinking-fund contribution streaks, % of transactions logged within 48h (fidelity, not frequency), and envelopes finished
  - (moderate) Streaks, celebrations, and household accountability digest: Add positive-reinforcement primitives on top of existing infrastructure: logging streaks with grace tokens (miss one day without breaking the streak), end-of-period celebration screens for envelopes that survived intact, and a weekly FCM push digest

## [C-] Domain: meters, slip scanning & transactions (+ households, ports)

STRENGTHS: Slip scanning domain is the one genuinely hexagonal slice: Capture/Upload/Extract/Confirm/Cleanup/Consent use cases all depend on ports (ISlipQueueRepository, ISlipExtractor, ISlipImageCompressor, ISlipImageUploader) with solid dedicated tests for each, a typed SlipScanErrorCode union, and multi-item confirm wrapped in a single DB transaction with rollback-on-failure | Money is integer cents everywhere; no floats | Cross-household isolation is taken seriously at write time: CreateTransactionUseCase scopes envelope lookup to householdId (the CRITICAL-2 fix), DeleteTransactionUseCase scopes both delete and envelope update by householdId, and there is a dedicated household-isolation test suite | Invite code generation is correct crypto: 32-char alphabet divides 256 evenly so the modulo has zero bias, confusable characters (0/O/1/I) excluded, 48h expiry, and the actual membership write happens server-side via the join_household_via_invite RPC rather than trusting the client | Slip image retention is handled on both sides: local CleanupExpiredSlipsUseCase (30-day cutoff, nulls rawResponseJson PII) plus a server-side pg_cron job in supabase/migrations/006_slip_scanning | Audit logging on every mutating transaction/meter/household use case with previousValue/newValue payloads

- (high/VERIFIED) DeleteTransactionUseCase corrupts envelope balance on double-delete or stale entity [src/domain/transactions/DeleteTransactionUseCase.ts:27-40]
  detail: execute() never verifies the transaction row exists (or that any row was actually deleted) before decrementing envelopes.spentCents by the caller-supplied tx.amountCents. A double-tap delete, a retry, or deleting a transaction that a sync pull already removed deletes 0 rows but still subtracts amountCents from the envelope — permanently understating spent and overstating available budget. It also
  fix: Inside the DB transaction, SELECT the row by (id, householdId); if absent, return a NOT_FOUND failure without touching the envelope. Use the fetched row's amountCents/envelopeId for the decrement. Only enqueue DELETE when a row was deleted.
- (medium/VERIFIED) AcceptInviteUseCase strands the user in a broken half-joined state when restore fails [src/domain/households/AcceptInviteUseCase.ts:80-98]
- (medium/VERIFIED) '3-month rolling average' anomaly math ignores time entirely — false alarms on any irregular cadence [src/domain/meterReadings/AnomalyDetector.ts:31-52]
- (medium/VERIFIED) Meter rollback/typo — the most extreme anomaly — is silently swallowed and poisons future averages [src/domain/meterReadings/AnomalyDetector.ts:42-49]
- (high/VERIFIED) Audit + sync enqueue sit outside the DB transaction in every write use case — rows can become permanently unsynced [src/domain/transactions/CreateTransactionUseCase.ts:89-121]
  detail: The pattern is: commit DB transaction → await audit.log → await enqueuer.enqueue. If audit.log throws (or the app dies between commit and enqueue), the transaction/reading/household exists locally with isSynced=false but no pending_sync row, and the use case returns a rejected promise after the data is already committed — the UI shows an error for a write that happened. Same shape in DeleteTransac
  fix: Move the pending_sync insert (and ideally the audit insert — both are local tables) inside the same db.transaction as the entity write. Wrap CreateHousehold's household+member+seed writes in one transaction. As a backstop, add a periodic sweep that enqueues any row with isSynced=false lacking a pend
- (high/VERIFIED) ConfirmSlipUseCase has no idempotency or status guard — double confirm duplicates transactions [src/domain/slipScanning/ConfirmSlipUseCase.ts:42-64]
  detail: execute() never loads the slip to check it exists or that status is not already 'completed', and never validates items is non-empty. A double-tap on the confirm button, or a retry after the success response is lost, re-runs the loop and creates a full duplicate set of transactions against the envelopes (each also re-incrementing spentCents). An empty items array 'succeeds' and marks the slip compl
  fix: Fetch the slip inside the transaction, fail with SLIP_ALREADY_CONFIRMED when status === 'completed' (or when any transaction with this slipId already exists), and reject empty item lists.
- (medium/unverified) Household membership rules exist only as data — no role checks, no leave/remove/transfer use cases [src/domain/households/UpdateHouseholdPaydayDayUseCase.ts:22-36]
- (medium/unverified) Ports layer is half-fiction: three exported ports have zero domain consumers while use cases import Drizzle directly [src/domain/transactions/CreateTransactionUseCase.ts:1-8]
- (medium/unverified) Duplicate-reading check ignores vehicleId — a second vehicle cannot log an odometer reading on the same day [src/domain/meterReadings/LogMeterReadingUseCase.ts:46-56]
- (medium/unverified) CleanupExpiredSlipsUseCase aborts the whole retention batch on first failure [src/domain/slipScanning/CleanupExpiredSlipsUseCase.ts:20-24]
- (medium/unverified) AcceptInvite enqueues an INSERT push for a row the server already owns [src/domain/households/AcceptInviteUseCase.ts:77-78]
- (low/unverified) No transactionDate/readingDate validation — garbage dates flow into grouping and duplicate logic [src/domain/transactions/CreateTransactionUseCase.ts:38-41]
- (low/unverified) Transactions can be created against archived envelopes [src/domain/transactions/CreateTransactionUseCase.ts:56-64]
- (low/unverified) Transaction lifecycle has no update path — 'edit' can only be delete+recreate, losing identity and slip linkage [src/domain/transactions/TransactionEntity.ts:3-16]
  UPGRADE IDEAS:
  - (transformative) Time-normalized, robust, money-aware anomaly engine: Replace the last-3-deltas mean with consumption-per-day normalization, median/MAD (robust to one bad reading) over a true trailing 90-day window, seasonal baselining (compare against the same month last year — electricity in a Highveld winter is not
  - (major) Derive envelope balances from the transaction ledger instead of mutating spentCents: Every bug class found in this review (double-delete drift, double-confirm duplication, edit-by-recreate) exists because spentCents is a denormalized counter mutated in lockstep with transaction writes. Make the transaction table the single source of
  - (major) Finish the hexagon and make it enforceable: Complete the migration the ports/ directory started: all domain use cases take required port dependencies (repo, audit, enqueuer, clock, idGenerator), a composition root in application/ wires Drizzle adapters, and an eslint/dependency-cruiser rule fa
  - (transformative) Slip line-items as a price-intelligence and auto-categorization asset: SlipExtractionItem already carries description, amountCents, quantity and a suggestedEnvelopeId — but items are flattened into plain transactions and the intelligence is discarded. Persist line items, learn envelope assignments from the user's confir
  - (major) Real household collaboration: roles, exits, attribution, approvals: Build the membership rules the schema pretends to have: owner/member (or owner/admin/member/viewer) permissions enforced by a domain MembershipPolicy, LeaveHousehold / RemoveMember / TransferOwnership use cases with matching server RPCs, per-member a
  - (moderate) Point the slip-scanning pipeline at the meter itself: The compress→upload→extract infrastructure already exists for receipts. Add a 'scan meter' mode: photograph the dial/LCD, OCR the value, prefill the reading with monotonicity validation against the last reading before saving. This eliminates the typo

## [C+] Data: local schema & migrations (src/data/local, drizzle.config.ts)

STRENGTHS: Money is stored as integer cents everywhere (amount_cents, allocated_cents, outstanding_balance_cents, etc | The custom migration runner in src/data/local/db | Timestamps are consistently ISO-8601 UTC TEXT across every table — no epoch/ISO mixing | Hot query paths are indexed with sensible composite indexes: transactions(household_id, transaction_date), envelopes(household_id, period_start, is_archived), meter_readings(household_id, meter_type), plus a correct unique index on baby_steps(household_id, step_number) | There is a dedicated schema-alignment test (tombstone-schema | camelCase/snake_case conversion is centralized in one tiny, testable module (src/data/sync/rowConverters

- (medium/VERIFIED) Migration 0007 is invalid SQL on any non-empty table — works today only by accident [src/data/local/migrations/0007_household_members_updated_at.sql:1]
- (medium/VERIFIED) Tombstone/soft-delete schema (migration 0009) is completely dead code — deletes are still hard everywhere [src/data/local/migrations/0009_soft_delete_tombstones.sql:1]
- (medium/VERIFIED) drizzle-kit metadata is corrupted: 5 of 10 migrations have no snapshot, journal timestamps are fabricated and non-monotonic [src/data/local/migrations/meta/_journal.json:20]
- (high/VERIFIED) No migration test executes SQL against a real SQLite engine — every test mocks the database [src/data/local/__tests__/db-migration-runner.test.ts:30]
  detail: db-migration-runner.test.ts, db.test.ts, and db.migrationChecksum.test.ts all mock expo-sqlite with jest.fn() stubs; tombstone-schema.test.ts only string-matches the SQL file. Nothing ever parses or executes the migration SQL, which is exactly why the invalid 0007 (non-constant default) and the destructive 0008 shipped unnoticed. The runner logic is tested; the migrations themselves — the risky pa
  fix: Add a Jest suite using better-sqlite3 (node-only devDependency) that (1) applies all migrations in order to an empty DB, (2) applies them to a DB pre-seeded with rows in every table at each intermediate version, and (3) diffs the resulting sqlite_master schema against the Drizzle TS schema (drizzle-
- (medium/unverified) Migration 0008 drops slip_queue with user data in it; orphaned pending_sync rows then dead-letter [src/data/local/migrations/0008_slip_scanning.sql:5]
- (medium/unverified) Zero foreign key constraints in the entire local schema, and PRAGMA foreign_keys is never enabled [src/data/local/schema/transactions.ts:9]
- (medium/unverified) Local schema has no CHECK constraints while remote Postgres does — locally-valid rows can wedge in the sync DLQ [src/data/local/schema/households.ts:7]
- (low/unverified) pending_sync has no indexes and no dedupe constraint — duplicate queue entries for the same record [src/data/local/schema/pendingSync.ts:3]
- (low/unverified) Two abandoned database files left on user devices after 'rename to force clean schema' [src/data/local/db.ts:7]
- (low/unverified) Schema convention drift: household_members lacks is_synced/created_at; slip_queue, audit_events, user_consent lack deleted_at; sync special-cases by string compare [src/data/local/schema/householdMembers.ts:3]
  UPGRADE IDEAS:
  - (transformative) Real-SQLite migration proving harness with seeded upgrade paths: A Jest suite backed by better-sqlite3 that (1) replays the full migration chain on an empty DB, (2) replays it from every historical version N with realistic fixture data present, and (3) asserts the final schema is byte-identical to what drizzle-kit
  - (transformative) Copy-migrate-swap: zero-brick database upgrades: Before running new migrations, copy accountingv2-v3.db to a temp file, run the migration chain on the copy, run PRAGMA integrity_check plus a row-count sanity diff, then atomically swap files. On any failure the app keeps running on the old schema (f
  - (transformative) Finish tombstone sync: delete-wins LWW with undo: Complete the half-built deleted*at story end-to-end: repositories soft-delete and filter, merge*\* RPCs get deleted_at with direction-independent LWW (extending migration 016's tiebreaker), delete_sync_row is retired, and RestoreService propagates tom
  - (major) Derive envelope spent_cents instead of hand-maintaining it: envelopes.spent_cents is a denormalized running total mutated by every transaction use-case (e.g. DeleteTransactionUseCase decrements it manually) — a drift bug waiting to happen across sync/restore/conflict paths. Replace with SQLite triggers on tra
  - (major) Schema contract test + drizzle-kit drift gate in CI: Two cheap automated gates: (a) a conformance test that walks every exported table and asserts the canonical synced-table column contract (timestamps, is_synced, deleted_at, household scoping) plus presence of an index on every column used in a WHERE
  - (moderate) Connection hardening: WAL, foreign_keys, busy_timeout, PRAGMA optimize: db.ts opens the database with all SQLite defaults. Explicitly set journal_mode=WAL (crash-safe, concurrent-read-friendly for the sync engine reading while the UI writes), foreign_keys=ON (once FKs land), busy_timeout to stop SQLITE_BUSY surprises bet
  - (moderate) Local data lifecycle: audit pruning and legacy DB cleanup: audit_events grows forever on-device (remote has cleanup in migration 015; local has nothing) and two abandoned pre-rename database files sit on old installs containing unencrypted financial history. Add a startup housekeeping task: prune synced audi

## [D+] Data: sync engine (src/data/sync, src/data/remote, supabase merge RPCs)

STRENGTHS: Per-table SECURITY DEFINER merge RPCs with server-side household membership checks give defense-in-depth; direct DML on sync tables is revoked (019), and deletes route through an allowlisted delete_sync_row RPC instead of raw PostgREST DML | Retry machinery is genuinely solid: exponential backoff capped at 60s, backoff window enforced in the fetch query itself (lastAttemptedAt as next-allowed-time), DLQ on 10 retries, and the enqueuer coalesces duplicate (table, record) entries without resetting retry state (src/data/sync/PendingSyncEnqueuer | Deterministic LWW tiebreaker migration (016) shows real thought about direction-independent merges; audit_events are correctly immutable first-write-wins (ON CONFLICT DO NOTHING) | The N+1 prefetch batching in SyncOrchestrator (rowCache via inArray) and the reentrancy guard are sensible performance/safety touches | Unusually honest engineering culture: docs/known-gaps/lww-data-loss | Test volume is real: backoff timing, DLQ thresholds, RPC routing, error branches all have dedicated suites

- (high/VERIFIED) Edits made during an active sync run are silently lost (queue-entry erasure race) [src/data/sync/SyncOrchestrator.ts:106-135]
  detail: syncPending prefetches row snapshots for the whole 100-item batch BEFORE the sequential network loop (lines 106-126), pushes the stale snapshot, then unconditionally deletes the pending_sync row (line 135) and sets isSynced=true (lines 228-233). PendingSyncEnqueuer dedupes by (table, record) and only updates `operation` on an existing queue row (PendingSyncEnqueuer.ts:25-34). So if the user edits
  fix: Make completion conditional: capture the local row's updatedAt at push time and DELETE the pending row only WHERE the row is unchanged (or version the pending row and delete WHERE version = captured). Same guard for the isSynced=true update. Alternatively re-read the row immediately before each push
- (critical/VERIFIED) Migration 019 breaks invite creation AND invite acceptance in production (RPC arg name mismatch + revoked INSERT) [src/domain/households/AcceptInviteUseCase.ts:51-53]
  detail: 019 defines `join_household_via_invite(p_invite_code text)` (supabase/migrations/019_batch1_security_hardening.sql:12) but the client calls `rpc('join_household_via_invite', { invite_code: ... })`. PostgREST resolves functions by named arguments, so this returns PGRST202 (function not found) — joining a household is dead. Separately, 019:265 runs `REVOKE INSERT ON public.invitations FROM authentic
  fix: Rename the client arg to p_invite_code (or the SQL param to invite_code), add a contract test that parses the SQL signature against client call sites, and either re-grant INSERT on invitations (RLS still constrains it) or move invite creation behind a SECURITY DEFINER RPC. Verify against a real data
- (high/VERIFIED) Deletes cannot propagate across devices: hard delete + LWW upsert = resurrection and ghost rows (tombstones are dead schema) [src/data/sync/SyncOrchestrator.ts:185-191]
  detail: DELETE ops hard-delete on the server via delete_sync_row (019:246), and DeleteTransactionUseCase.ts:28-32 hard-deletes locally. Migration 0009 added deleted_at columns to 7 local tables but NOTHING writes them (grep: deletedAt appears only in schema files and a gap-documenting test), and the remote schema has no deleted_at at all — the merge RPC composite types would silently drop the field anyway
  fix: Implement the full tombstone pipeline: soft-delete locally (SET deleted_at), add deleted_at to remote tables and merge RPCs (LWW on the tombstone like any field), filter deleted rows in queries, purge tombstones after a retention window, and add orphan purge / tombstone application to RestoreService
- (high/VERIFIED) Restore clobbers dirty local rows then sync pushes the stale data back (restore-before-sync ordering) [src/data/sync/RestoreService.ts:170-194]
  detail: restoreTable() upserts every remote row with onConflictDoUpdate over ALL columns except id/isSynced — no updated_at comparison, remote unconditionally authoritative. App.tsx:91-93 runs restore in parallel with local init and BEFORE syncPending (App.tsx:127). Sequence: offline edit (isSynced=false, pending row) → boot → restore overwrites data columns with stale remote values → syncPending then pus
  fix: Skip conflict-update for rows with isSynced=false (or where local updated_at > remote updated_at), and order boot as push-then-pull. At minimum implement the documented low-effort fix: onConflictDoNothing for dirty rows.
- (medium/VERIFIED) LWW compares client wall-clock TEXT timestamps; server-written rows use a different format, making comparisons semantically wrong [supabase/migrations/016_lww_direction_independent_tiebreaker.sql:45-46]
- (medium/VERIFIED) isSynced=true is set even when the merge RPC's LWW guard rejected the push — permanent silent divergence [src/data/sync/SyncOrchestrator.ts:225-233]
- (high/unverified) Sync is only triggered at app boot and on network reconnect — writes during a steady online session never push; no pull at all mid-session [App.tsx:127-181]
  detail: The only two syncPending call sites are initSession (App.tsx:127) and networkObserver.onConnected (App.tsx:179). There is no after-write trigger, no periodic timer, no foreground/AppState trigger. A user who opens the app in the morning and logs transactions all day with stable connectivity accumulates everything in pending_sync until the next app restart or connectivity flap — maximizing every LW
  fix: Trigger a debounced syncPending after every enqueue (the reentrancy guard already dedupes), add an AppState foreground trigger and a periodic timer while online, and loop syncPending until the fetched batch is empty. Add a lightweight incremental pull (updated_at > cursor) or Supabase Realtime subsc
- (high/unverified) DLQ silently discards user data; 7-day age rule dead-letters long-offline changes on their FIRST failure [src/data/sync/SyncOrchestrator.ts:142-151]
  detail: Items are dead-lettered after 10 retries OR when `Date.now() - createdAt >= 7 days`. The age check uses queue-entry creation time, so a change enqueued during an extended offline period (vacation, rural connectivity) that is older than 7 days gets dead-lettered after a single transient failure on reconnect. Nothing consumes deadLetteredAt: no UI, no re-drive API, no notification — grep shows the o
  fix: Drop the age-based DLQ trigger (or make it 'age since first ATTEMPT'), classify errors (permanent → DLQ immediately with user notification; transient → retry forever with capped backoff), and build a DLQ inbox surface with retry/discard plus a Sentry/monitoring hook on every dead-letter event.
- (high/unverified) RestoreService has no pagination — device restore silently truncates at the PostgREST max-rows cap (default 1000) [src/data/sync/RestoreService.ts:163-166]
  detail: restoreTable() issues a single `.select('*').eq('household_id', ...)` per table with no .range()/.limit() paging. Supabase caps responses at max-rows (default 1000). An established household easily exceeds 1000 transactions or audit_events; on reinstall/new-device restore, everything beyond the cap silently never arrives, and since errors are also swallowed (`if (error || !data) return;` line 168)
  fix: Page through results with .range() in a loop keyed on created_at/id until short page; propagate per-table errors up so the boot flow can retry or warn instead of silently proceeding with partial data.
- (high/unverified) delete_sync_row lets any household member hard-delete other members' membership rows, and user_households is never cleaned up [supabase/migrations/019_batch1_security_hardening.sql:208-252]
  detail: delete_sync_row allowlists 'household_members' and authorizes by checking the CALLER is a member of the row's household — not that the row belongs to the caller. Any 'member' can therefore delete the owner's household_members row (SECURITY DEFINER bypasses RLS). Worse for sync correctness: the user_households mirror is populated by an INSERT-only trigger (005:71-89), so a deleted member's user_hou
  fix: In delete_sync_row, special-case household_members: require row.user_id = caller (self-leave) or caller is owner; add a DELETE trigger on household_members that removes the matching user_households row.
- (medium/unverified) merge_household_member (019) regressed the LWW guard and silently demotes roles with no client feedback [supabase/migrations/019_batch1_security_hardening.sql:130-145]
- (medium/unverified) PendingSyncTable is dead code with retry semantics that contradict the orchestrator [src/data/sync/PendingSyncTable.ts:44-54]
- (medium/unverified) Module-level isSyncRunning lock silently drops sync requests with an ambiguous result [src/data/sync/SyncOrchestrator.ts:63-77]
- (low/unverified) Redundant post-join push overwrites server-authored membership fields with client-clock values [src/domain/households/AcceptInviteUseCase.ts:69-78]
- (medium/unverified) Test suite is mock-chain and source-string based; zero coverage of the real race/ordering behavior [src/__tests__/sync/soft-delete-gaps.test.ts:34-95]
  UPGRADE IDEAS:
  - (transformative) Server-sequenced oplog sync (replace state-snapshot LWW): Move from 'push whole row, LWW on updated_at' to an append-only change log: each local mutation records an operation (table, id, changed fields, base version); the server assigns a monotonic sequence number on ingest and clients pull 'everything afte
  - (transformative) Supabase Realtime household channels for live collaboration: Subscribe each device to postgres_changes filtered by household_id and apply incoming rows through the same LWW/merge logic locally. Two spouses budgeting together would see envelope balances, transactions, and debt payments update within seconds — t
  - (major) Delta-based money fields (CRDT counters) in merge RPCs: spent_cents, outstanding_balance_cents, and allocated adjustments should sync as server-applied increments (SET spent_cents = spent_cents + delta) instead of absolute LWW overwrites — the pattern total_paid_cents already uses. Concurrent spending by
  - (major) Sync health center: visible queue, DLQ inbox, conflict toasts: A settings screen showing pending/failed/dead-lettered items per record with retry & discard actions, a 'last synced' badge in the app header, and a toast when a merge conflict resolves against the user ('Your envelope edit was superseded by Alicia's
  - (major) Server-stamped hybrid logical clocks: Merge RPCs stamp updated_at server-side (or maintain an HLC combining server time + device counter) and RETURN the authoritative winning row, which the client applies locally. Eliminates the entire clock-skew and mixed-timestamp-format class of bugs,
  - (moderate) Two-device simulation test harness with the real SQL: A Jest harness running two Drizzle/SQLite 'devices' against pglite executing the actual merge\_\* migration SQL, driven by a property-based interleaving fuzzer (edit/sync/restore/delete/reconnect in random orders, asserting convergence and no data loss
  - (moderate) Sync-on-write with smart batching and background tasks: Debounced auto-push after every enqueue (300ms coalescing window), AppState foreground trigger, and expo-background-task for periodic push/pull while the app is closed. Combined with drain-until-empty batching, the pending queue stays near-zero at al

## [C-] Data: repositories & audit (src/data/repositories, src/data/audit)

STRENGTHS: Consistent household scoping on core repos — every findById/update/delete on debts, envelopes, transactions, meter readings filters by (id, householdId), a good tenant-isolation habit (e | Atomic SQL increment for money: DrizzleDebtRepository | DrizzleSlipQueueRepository and DrizzleUserConsentRepository integrate the sync outbox at the repository level with injectable ISyncEnqueuer — the right shape for the whole layer | PendingSyncEnqueuer is thoughtfully written: dedupes per (table, record), upgrades the operation in place, and deliberately preserves retryCount/lastAttemptedAt so exponential backoff is not reset (documented in a WHY comment) | Money is integer cents everywhere, timestamps are ISO strings, and row-to-entity mappers are explicit and thin | Hot local queries are backed by composite indexes (envelopes household+period+archived, transactions household+date, meter_readings household+type in migration 0006) | CreateTransactionUseCase and DeleteTransactionUseCase correctly wrap the transaction-row + envelope spentCents pair in db

- (high/VERIFIED) Outbox enqueue is never atomic with the domain write — crash window loses sync permanently [src/data/repositories/DrizzleSlipQueueRepository.ts:40-51 (also AuditLogger.ts:29-37, DrizzleUserConsentRepository.ts:455-473)]
  detail: Every write path does `await db.insert/update(...)` followed by a separate `await enqueuer.enqueue(...)`, even though both statements hit the same SQLite database. The same pattern repeats in the use cases (write -> commit -> audit.log -> enqueue). If the app crashes or the enqueue throws between the two statements, the row is persisted with isSynced=false but no pending_sync entry exists. SyncOrc
  fix: Wrap entity write, audit insert, and pending_sync insert in a single db.transaction() everywhere (trivial since they share one SQLite connection), and/or add a startup reconciliation sweep that re-enqueues any row with isSynced=false that has no live pending_sync entry.
- (medium/VERIFIED) Audit trail records no actor — cannot answer 'who did this' in a multi-member household app [src/data/audit/AuditLogger.ts:12-19 (schema: src/data/local/schema/auditEvents.ts:3-12)]
- (medium/VERIFIED) Repository layer is largely dead code — presentation and use cases bypass it and query Drizzle directly [src/data/repositories/DrizzleEnvelopeRepository.ts:1 (and DrizzleTransactionRepository.ts:1)]
- (medium/VERIFIED) Debt payment is split across two non-transactional writes with a stale-snapshot balance [src/domain/debtSnowball/LogDebtPaymentUseCase.ts:52-61 (repo: src/data/repositories/DrizzleDebtRepository.ts:56-61)]
- (medium/unverified) DrizzleEnvelopeRepository.update silently drops targetAmountCents and targetDate [src/data/repositories/DrizzleEnvelopeRepository.ts:115-130]
- (medium/unverified) Dirty-flag discipline is inconsistent: DrizzleDebtRepository.update trusts the caller for isSynced/updatedAt [src/data/repositories/DrizzleDebtRepository.ts:48-54]
- (medium/unverified) SlipQueue get/update are not household-scoped, unlike every other repository [src/data/repositories/DrizzleSlipQueueRepository.ts:53-71]
- (medium/unverified) Slip rowToDomain JSON.parse is unguarded — one corrupt row breaks the whole slip queue [src/data/repositories/DrizzleSlipQueueRepository.ts:15 (rowToDomain), used by listByHousehold:76-88]
- (low/unverified) Unbounded queries and missing indexes on growing tables; audit_events grows forever with no retention [src/data/repositories/DrizzleMeterReadingRepository.ts:205-216]
- (low/unverified) Consent changes are not audited, and setSlipScanConsent is check-then-act [src/data/repositories/DrizzleUserConsentRepository.ts:455-473]
- (low/unverified) rowToEntity mappers use unchecked `as` casts on enum-like fields [src/data/repositories/DrizzleDebtRepository.ts:68 (also EnvelopeRepository.ts:145, MeterReadingRepository.ts:247, SlipQueueRepository.ts:276)]
- (low/unverified) Test suite is 100% fluent-chain mocks — SQL semantics are untested [src/data/repositories/__tests__/DrizzleEnvelopeRepository.test.ts:688-701]
  UPGRADE IDEAS:
  - (transformative) Transactional Unit-of-Work: one commit for write + audit + outbox: Since entity tables, audit_events, and pending_sync all live in the same SQLite file, build a single UnitOfWork.commit({ writes, audit, sync }) API that wraps them in ONE db.transaction(). Repositories and use cases stop hand-sequencing insert -> aud
  - (transformative) Actor-aware audit trail surfaced as a Household Activity feed: The audit trail already captures previous/new values for every money move but is write-only — no screen reads it. Add actor + source to audit_events, then ship an in-app timeline: 'Sipho spent R450 from Groceries', 'Anele paid R2,000 toward FNB card
  - (major) Undo and time travel powered by the existing prev/new audit JSON: Because every audit event stores previousValue and newValue, you are one inverse-apply function away from 'Undo' on destructive actions (deleted transaction, archived envelope, fat-fingered payment) and a month-end 'what changed this period' replay.
  - (major) Schema-generated household-scoped repository factory: Seven repos hand-roll the same CRUD with drifting conventions (some enqueue, some set isSynced, one drops fields). Build createSyncedRepo(table, { scope: 'householdId' }) that derives find/insert/update/softDelete from the Drizzle table type and auto
  - (major) Real-SQLite integration test harness with migration replay: Stand up a Jest project that opens in-memory better-sqlite3, replays the actual migration files, and runs repository + use case tests against real SQL — including property-based tests for rowConverters (camel/snake round-trip) and household-isolation
  - (moderate) Reactive queries: replace manual reload() hooks with live queries: useEnvelopes/useTransactions manually reload on mount and expose reload() that screens must remember to call after mutations — and nothing refreshes when SyncOrchestrator pulls remote changes. Adopt drizzle's live query support (or a lightweight tabl

## [C] Infrastructure (src/infrastructure: crypto, logging, monitoring, notifications, network, storage, slipScanning + server-side push half in supabase/functions)

STRENGTHS: earlyCrashLog | Clean ports-and-adapters discipline in slipScanning: EdgeFunctionSlipExtractor / SupabaseSlipImageUploader / ExpoSlipImageCompressor / SlipImageLocalStore all implement domain ports (ISlipExtractor etc | Supabase session tokens are kept in expo-secure-store (SecureStorageAdapter) rather than plain AsyncStorage — the right default for auth material | The extract-slip edge function has real defense in depth: auth, household membership check, consent gate (412), slip ownership + cross-household check, idempotency/status guard, payload size cap, and advisory-lock rate limiting — and there are regression tests (security-audit-findings | SyncOrchestrator has a single-flight guard (isSyncRunning), so the somewhat noisy NetworkObserver reconnect events cannot cause duplicate concurrent pushes | Every infrastructure module has a matching test file; FcmTokenRegistrar tests cover both failure paths (getToken throw, upsert error), and Logger tests cover circular/unserialisable data

- (high/VERIFIED) Server push uses the FCM Legacy HTTP API, which Google shut down in June 2024 — every push notification send fails [supabase/functions/notify-event/index.ts:103]
  detail: The notify-event function POSTs to https://fcm.googleapis.com/fcm/send with an `Authorization: key=<FCM_SERVER_KEY>` legacy server key (lines 96-113). The legacy HTTP API was deprecated in June 2023 and fully removed in June/July 2024; in 2026 this endpoint returns 404 for all callers, and legacy server keys can no longer be provisioned in the Firebase console. Every send silently fails (`res.ok`
  fix: Migrate to FCM HTTP v1 (`https://fcm.googleapis.com/v1/projects/<project-id>/messages:send`) with OAuth2 service-account credentials (google-auth JWT flow works in Deno), one message per token with the v1 payload shape, and prune tokens from user_fcm_tokens when v1 returns UNREGISTERED/INVALID_ARGUM
- (high/VERIFIED) FCM token row is never deleted on sign-out — a previous user's financial notifications are delivered to whoever uses the device next [src/presentation/screens/settings/SettingsScreen.tsx:45]
  detail: registerFcmToken (src/infrastructure/notifications/FcmTokenRegistrar.ts:16-21) upserts {user_id, token} keyed on user_id, but sign-out (SettingsScreen.tsx:45 just calls supabase.auth.signOut(); App.tsx:233-242 only resets stores and clears the Crashlytics userId) never deletes the row or calls messaging().deleteToken(). The FCM token identifies the device installation, not the user. Scenario: user
  fix: Before supabase.auth.signOut() resolves the session, delete the caller's user_fcm_tokens row (RLS policy fcm_own_token permits it) and call messaging().deleteToken() so the token rotates. Also register an unregister path in FcmTokenRegistrar to keep the logic in one place.
- (medium/VERIFIED) 429 user-vs-household rate-limit discrimination can never work in production — supabase-js FunctionsHttpError.message is a generic constant [src/infrastructure/slipScanning/EdgeFunctionSlipExtractor.ts:17]
- (medium/unverified) One FCM token per user (PK user_id) — multi-device users only receive pushes on the last device that opened the app; no onTokenRefresh handling [src/infrastructure/notifications/FcmTokenRegistrar.ts:18]
- (medium/unverified) recordError leaks context between crash reports via global Crashlytics attributes [src/infrastructure/monitoring/crashlytics.ts:14]
- (medium/unverified) Supabase session persisted through expo-secure-store exceeds the documented 2048-byte Android limit, and adapter has no error handling [src/infrastructure/storage/SecureStorageAdapter.ts:5]
- (medium/unverified) NetworkObserver: auto-sync never fires when isInternetReachable stays null, contradicting syncStore's semantics; onConnected callbacks can never be removed [src/infrastructure/network/NetworkObserver.ts:16]
- (medium/unverified) envelopeWarningEnabled preference is decorative — nothing in the app sends envelope warnings or calls notify-event [src/infrastructure/notifications/NotificationPreferences.ts:8]
- (medium/unverified) Non-Error objects thrown in slip pipeline — no stack traces, breaks instanceof handling and Crashlytics reporting [src/infrastructure/slipScanning/SupabaseSlipImageUploader.ts:48]
- (medium/unverified) Compressor silently returns empty base64; fixed width 1600 upscales small images [src/infrastructure/slipScanning/ExpoSlipImageCompressor.ts:11]
- (medium/unverified) earlyCrashLog unhandled-rejection capture is a silent no-op on Hermes [src/infrastructure/monitoring/earlyCrashLog.ts:139]
- (medium/unverified) Month-start payday notification silently skips months when paydayDay is 29-31 [src/presentation/navigation/RootNavigator.tsx:98]
- (medium/unverified) Logging layer is barely adopted and has no PII policy; errors across the codebase are swallowed with empty catches [src/infrastructure/logging/Logger.ts:26]
- (low/unverified) NotificationPreferencesRepository.load trusts stored JSON without validation [src/infrastructure/notifications/NotificationPreferencesRepository.ts:13]
- (low/unverified) src/infrastructure/crypto is an empty directory — the advertised crypto layer does not exist [src/infrastructure/crypto/.gitkeep]
- (low/unverified) Unknown/auth failures from extract-slip are mislabeled as 'OpenAI unreachable' [src/infrastructure/slipScanning/EdgeFunctionSlipExtractor.ts:30]
  UPGRADE IDEAS:
  - (transformative) Rebuild push on FCM HTTP v1 with a notification outbox + preference sync: Replace the dead legacy-API notify-event with a proper pipeline: a notifications_outbox table written by database triggers (envelope threshold crossed, debt paid off, partner logged a transaction), a scheduled edge function that drains it via FCM HTT
  - (transformative) On-device OCR pre-pass so slip scanning works offline and costs ~10x less: Add an ML Kit / Vision text-recognition adapter behind the existing ISlipExtractor port that extracts merchant, date, and total locally in <1s with zero network. Use the OpenAI edge function only as an enhancement pass (line items + envelope suggesti
  - (major) Unified telemetry layer: breadcrumbs, scrubbing, and a sync-health dashboard: Promote Logger into a real telemetry facade: ring-buffer breadcrumbs (last 50 events attached to every crash report), automatic PII scrubbing, category-based levels (sync, ocr, notif, auth), and Crashlytics + optional self-hosted Sentry sinks. Then s
  - (major) Background sync + edge-detected network service: Replace the NetworkObserver/syncStore duo with one NetworkService that edge-detects offline→online transitions and drives both the banner and sync, then add expo-background-task/WorkManager so pending writes and slip uploads flush even when the app i
  - (major) Encrypt the ledger: SQLCipher + SecureStore-held key, filling the crypto/ directory: Financial data deserves at-rest encryption: derive a per-install AES key stored in Android Keystore via SecureStore, use it for (a) the oversized Supabase session blob (fixing the 2048-byte issue) and (b) op-sqlite/SQLCipher database encryption. Add
  - (moderate) Time-intelligent notifications with deep links: Make the three local notifications smart: payday trigger computed per-month with last-day clamping and timezone awareness; evening prompt suppressed on days the user already logged (schedule next-day only, reschedule on each log); every notification

## [C+] UI: dashboard, budgets & envelopes screens

STRENGTHS: Money is handled in integer cents end-to-end with domain helpers (getRemainingCents/getPercentRemaining/isOverBudget in src/domain/envelopes/EnvelopeEntity | Dashboard empty state is genuinely good: EmptyState + a visible '+ New envelope' CTA (DashboardScreen | testIDs are consistently applied across every interactive element (add-transaction-fab, view-budget-link, budget-balance-banner, envelope-save | BudgetBalanceBanner is a well-executed zero-based-budgeting status component: three distinct semantic states (balanced/left-to-assign/overcommitted) with icon, color, and a breakdown row, backed by a memoized domain calculator | Accessibility intent is present in most touchables: dashboard envelope rows compose a full sentence label including over/under state and % used (DashboardScreen | Design tokens (spacing/radius/fontSize) are used consistently instead of magic numbers, and EnvelopeFillBar has a pleasant clamped, eased fill animation | Rollover acknowledgement is persisted per-period via AsyncStorage key so the modal fires exactly once per period (DashboardScreen

- (medium/VERIFIED) Ramsey Score on dashboard is permanently wrong: meter readings hardcoded to false and period length hardcoded to 30 [src/presentation/screens/dashboard/DashboardScreen.tsx:92-99]
- (medium/VERIFIED) Dashboard 'Budget'/'Spent'/ring % aggregates include income envelopes, inflating totals [src/presentation/screens/dashboard/DashboardScreen.tsx:87-91]
- (high/VERIFIED) Envelope type selector is missing 'sinking_fund' (and 'emergency_fund'/'baby_step'), breaking the sinking-fund create/edit flow [src/presentation/screens/envelopes/AddEditEnvelopeScreen.tsx:211-221]
  detail: SegmentedButtons only offers income/spending/savings/utility, but EnvelopeType has 7 values and two live navigation paths preselect the missing ones: SinkingFundsScreen.tsx:74 navigates with preselectedType 'sinking_fund' and BabyStepsScreen.tsx:78 with 'emergency_fund'. Result: the segmented control renders with NO selection, the sinking-fund target fields (lines 223-246) appear with no visible e
  fix: Either add the missing types to the control (or a two-row chip group), or when type is sinking_fund/emergency_fund render a read-only type chip with explanatory text instead of an unmatched SegmentedButtons value.
- (medium/unverified) ~700 LOC of dead dashboard components still shipped and tested: HeroSummaryCard, EnvelopeTile, RamseyScoreBadge, BabyStepsCard [src/presentation/screens/dashboard/components/HeroSummaryCard.tsx:77]
- (medium/unverified) Primary 'Add Transaction' action lives in the FlatList footer and scrolls off-screen [src/presentation/screens/dashboard/DashboardScreen.tsx:195-250]
- (medium/unverified) Every screen focus and pull-to-refresh blanks the envelope list to a skeleton [src/presentation/screens/dashboard/DashboardScreen.tsx:328]
- (medium/unverified) Query error states are silently swallowed on both Dashboard and Budget screens [src/presentation/screens/dashboard/DashboardScreen.tsx:50]
- (medium/unverified) BudgetScreen envelope cards ripple but do nothing, and the screen has no add/edit path or empty-state CTA [src/presentation/screens/budgets/BudgetScreen.tsx:83]
- (medium/unverified) Dark-mode text at 0.28-0.40 alpha and an 18px dismiss target fail accessibility minimums [src/presentation/screens/dashboard/components/BabyStepsBar.tsx:39-40]
- (low/unverified) Envelope lists render in arbitrary DB order — no sorting anywhere [src/presentation/hooks/useEnvelopes.ts:23-32]
- (low/unverified) Amount and target-date inputs accept garbage silently: toCents() coerces invalid text to 0, date is a free-text field [src/presentation/screens/envelopes/AddEditEnvelopeScreen.tsx:23-27]
- (low/unverified) Two competing theming systems: Paper theme vs hardcoded PULSE palette + useColorScheme [src/presentation/screens/dashboard/DashboardScreen.tsx:101-108]
- (low/unverified) BabyStepsBar highlights the wrong 'current' segment when steps complete out of order [src/presentation/screens/dashboard/components/BabyStepsBar.tsx:66]
- (low/unverified) ListHeader/ListFooter memoized as elements with exhaustive-deps disabled — fragile and partially stale-prone [src/presentation/screens/dashboard/DashboardScreen.tsx:181-192]
  UPGRADE IDEAS:
  - (transformative) Reactive local-first data layer (live queries instead of poll-on-focus): Replace the manual useEnvelopes reload-on-focus pattern with Drizzle/expo-sqlite live queries (useLiveQuery) or a Zustand slice updated by the sync engine's change events. Every screen (dashboard ring, budget banner, envelope rows) updates instantly
  - (transformative) Envelope quick-actions bottom sheet: spend, move money, see recent transactions: Tapping an envelope on the dashboard currently jumps to an edit form — the least common intent. Replace with a bottom sheet showing remaining amount, a mini spending-pace sparkline, the last 5 transactions, and three actions: 'Spend from this envelop
  - (transformative) Spending-pace intelligence: 'safe to spend today' and on-pace indicators: The data is already local: allocatedCents, spentCents, days elapsed/remaining. Compute per-envelope burn rate vs linear pace and surface (a) a daily safe-to-spend number as the dashboard hero instead of a static remaining total, and (b) a subtle pace
  - (major) Interactive 'assign every rand' budget builder: Upgrade BudgetScreen from a read-only report into the primary budgeting workspace: inline steppers/drag handles on each envelope's allocation, with the BudgetBalanceBanner's 'to assign' figure animating live as you adjust, celebratory state when it h
  - (major) Period rollover wizard replacing the passive modal: PeriodRolloverModal just says 'envelopes have been reset'. Replace with a 3-step wizard at period start: (1) review last period — overspent envelopes, score, wins; (2) carry forward or adjust each allocation (copy last period's envelope set with per-
  - (moderate) Unified PULSE design system + accessibility pass: Promote the P palette into the theme layer, delete the four dead components, define semantic tokens (text.muted, surface.tile, status.danger) with WCAG-validated dark/light values, add Dynamic Type support (allowFontScaling with maxFontSizeMultiplier
  - (moderate) Score delta and streak gamification on the ring: Once the score inputs are fixed, make the number motivating: animate the ring on mount with Reanimated, show a delta chip ('+6 vs last week'), track a logging streak ('12-day logging streak') with haptic celebration at milestones, and make the score

## [C-] UI: transactions, slips & meters screens

STRENGTHS: Clean dependency-injection at the slip stack boundary (SlipScanningScreen | Excellent slip-scan error taxonomy: ERROR_COPY in SlipProcessingScreen | Offline is a first-class state in SlipCaptureScreen: shutter disabled, visible banner with accessibilityLiveRegion, and a one-tap 'Log manually' fallback to the offline-first manual form | AddTransactionScreen has real product thought: double-tap save guard via isSaving ref, auto-select when only one envelope exists, envelope balance shown inline in the picker with overspend coloring, and the SpendingCoach overspend intercept modal — genuine Ramsey-methodology value | Consent gating for AI photo processing (SlipConsentScreen) with clear retention/revocation copy — privacy UX done properly | Pockets of real accessibility care: LineItemRow composes descriptive accessibilityLabels including confidence state, live regions on error containers, accessibilityViewIsModal on the envelope sheet | Money handled as integer cents end-to-end in the UI layer; consistent theme-token styling with intentional, documented exceptions for camera chrome

- (high/VERIFIED) Slip scanning is unreachable: the stack's initial route (SlipQueue) has no button to start a scan [src/presentation/screens/slipScanning/SlipQueueScreen.tsx:163-190]
  detail: Every entry point to slip scanning (AddTransactionScreen.tsx:297 'Scan slip' button, SettingsScreen.tsx:154) navigates to the 'SlipScanning' modal, whose stack has SlipQueue as the first screen (SlipScanningStackNavigator.tsx:52). SlipQueueScreen renders only a FlatList — there is no FAB, header button, or any control that navigates to SlipCapture. The empty state even says 'Tap the camera button
  fix: Add a FAB (icon='camera') to SlipQueueScreen that navigates to SlipConsent-or-SlipCapture (check consent locally first), and/or make SlipCapture the initial route with the queue reachable via header button. Add a navigation-level integration test that walks AddTransaction → Scan slip → shutter visib
- (medium/VERIFIED) 'Undo' on frame delete is cosmetic — the frame is deleted anyway after 3 seconds [src/presentation/screens/slipScanning/SlipCaptureScreen.tsx:98-110]
- (high/VERIFIED) Tapping a completed slip in the queue crashes: SlipConfirm is navigated to without the extraction param [src/presentation/screens/slipScanning/SlipQueueScreen.tsx:153-154]
  detail: handlePress for status 'completed' calls navigation.navigate('SlipConfirm', { slipId: item.id }) with no extraction. SlipConfirmScreen.tsx:41-42 does `const { slipId, extraction } = route.params; const items = extraction.items;` — extraction is undefined, so this throws 'Cannot read property items of undefined' and crashes the screen. The param type in SlipScanningStackNavigator.tsx:22 even allows
  fix: Either load the persisted extraction from the repo by slipId inside SlipConfirmScreen when the param is missing, or navigate completed slips to a read-only detail view. At minimum, guard route.params and render a fallback instead of crashing.
- (medium/VERIFIED) Tapping a processing slip re-triggers a scan with undefined frames [src/presentation/screens/slipScanning/SlipQueueScreen.tsx:136-138]
- (high/VERIFIED) Silent money misparse: amounts with thousands separators or spaces save the wrong value [src/presentation/screens/transactions/AddTransactionScreen.tsx:28-32]
  detail: toCents() does parseFloat(randStr.replace(',', '.')). Typing '1,234.56' becomes '1.234.56' → parseFloat → 1.234 → saves R1.23. '1 000' → parseFloat('1 000') → 1 → saves R1.00. Both pass the amountCents > 0 validation, so the user gets a success toast while a wrong amount is written to the ledger — the worst failure mode for a budgeting app. decimal-pad keyboards on some Android IMEs emit commas/sp
  fix: Sanitize input on change (strip everything except digits and one decimal separator), or parse with an explicit locale-aware routine that rejects ambiguous input with an inline error. Add unit tests for '1,50', '1,234.56', '1 000', '12.345', '-5'.
- (medium/VERIFIED) SlipConfirm save failure is completely silent [src/presentation/screens/slipScanning/SlipConfirmScreen.tsx:112-121]
- (high/unverified) OCR results cannot be corrected: no editing of amounts, descriptions, or removal of bogus line items [src/presentation/screens/slipScanning/SlipConfirmScreen.tsx:200-222]
  detail: The confirm screen renders extraction items read-only (LineItemRow only lets you pick an envelope). If the OCR misreads R89.99 as R899.90, hallucinates an item, or merges two lines, the user's only options are to save wrong data or abandon the whole slip and type everything manually. There is also no reconciliation check that sum(items) equals extraction.totalCents — a mismatch (common with OCR) i
  fix: Make amount and description tappable/editable, allow swipe-to-remove and add-item, and show a live 'items total vs slip total' reconciliation row that blocks/warns on mismatch. This is the difference between a demo and a usable scanner.
- (high/unverified) Invalid cost input in AddReading produces NaN cents passed to the use case [src/presentation/screens/meters/AddReadingScreen.tsx:96]
  detail: costCents = costRands.trim() ? Math.round(parseFloat(costRands) \* 100) : null. Input like 'abc', '1,5' (→150? no: parseFloat('1,5')=1 → R1.00 silently), or '12.3.4' yields NaN or a silently wrong value; NaN flows into LogMeterReadingUseCase and the DB. Note also the inconsistency: AddTransactionScreen at least attempts comma normalization, this screen does not. Additionally handleSave has no try/c
  fix: Validate costRands with the same sanitizer as the amount field (shared util), show inline HelperText on invalid input, and wrap the save in try/catch/finally.
- (medium/unverified) Cancel during slip processing cannot actually cancel anything [src/presentation/screens/slipScanning/SlipProcessingScreen.tsx:81,116-124]
- (medium/unverified) Escaped en-dash renders literally: users see 'This usually takes 5–15 seconds' [src/presentation/screens/slipScanning/SlipProcessingScreen.tsx:173]
- (medium/unverified) Daily scan counter counts frames, not slips, and burns quota on abandoned captures [src/presentation/screens/slipScanning/SlipCaptureScreen.tsx:87-92]
- (medium/unverified) Camera controls are invisible to screen readers [src/presentation/screens/slipScanning/SlipCaptureScreen.tsx:204-242]
- (medium/unverified) Failed-slip retry parses rawResponseJson unvalidated and passes it as SlipExtraction [src/presentation/screens/slipScanning/SlipQueueScreen.tsx:140-151]
- (medium/unverified) Meter readings cannot be backdated and the form shows no previous-reading context [src/presentation/screens/meters/AddReadingScreen.tsx:38,90-116]
- (medium/unverified) Transactions are immutable in the UI: no edit, no detail view, delete-only with no undo [src/presentation/screens/transactions/TransactionListScreen.tsx:148-169]
- (low/unverified) Validation errors surface as transient snackbars, and the iOS date spinner dismisses on first tick [src/presentation/screens/transactions/AddTransactionScreen.tsx:142-151,271-282]
- (medium/unverified) Screen tests are shallow render-assertions; every bug above ships through them [src/presentation/screens/slipScanning/__tests__/SlipCaptureScreen.test.tsx:63-116]
  UPGRADE IDEAS:
  - (transformative) Capture-first transaction logging: make the camera the default entry: Invert the flow: the Transactions FAB opens straight into the camera (with a 'type instead' escape), OCR runs, and logging a grocery trip becomes snap → glance → confirm, ~3 taps total. Combine with merchant→envelope memory (learn that 'Checkers' = G
  - (transformative) Offline capture queue with background OCR: Today offline users hit a red banner and must type manually — in an offline-first app. Instead: always allow capture, persist frames locally as a queued slip, and auto-upload/extract when connectivity returns, finishing with a push notification ('You
  - (major) Editable, reconciling confirm screen that learns: Upgrade SlipConfirm into a true review surface: inline-editable amounts/descriptions, swipe-to-remove, add-missing-item, split one line across two envelopes, and a live reconciliation bar (items total vs printed slip total, with the delta highlighted
  - (major) Sub-10-second manual entry: smart keypad, payee autocomplete, contextual defaults: Replace the generic form with an amount-first big keypad screen (calculator-style, comma-safe), payee autocomplete from transaction history, and a default envelope predicted from payee/time-of-day/most-used. Recent-payee chips above the keypad ('Chec
  - (major) Meter photo OCR + reading reminders + consumption trends: The app already has camera + OCR infrastructure; point it at the electricity/water/odometer dials: snap the meter, auto-read the digits, prefill the value with the last reading shown for sanity. Add cadence-based reminders ('You usually read electric
  - (moderate) Universal undo instead of confirm dialogs: Replace Alert-confirm deletes (transactions) and the broken frame-undo with one consistent pattern: optimistic action + 5-second snackbar undo backed by the tombstone/soft-delete machinery the sync layer already has. Faster for confident users, safer
  - (moderate) Multi-slip batch mode for weekly shoppers: Envelope budgeters often reconcile a week of receipts at once. Add gallery import (multi-select) and a batch pipeline: scan N slips sequentially, queue all extractions, then a single triage screen to confirm each. Pairs naturally with the existing Sl

## [C+] UI: debt, baby steps, forecasting & sinking funds screens

STRENGTHS: Baby Steps visual identity is genuinely crafted: seven bespoke SVG seal glyphs (StepSealMark | Accessibility effort is real, not token: DebtPayoffBar exposes accessibilityRole=progressbar with accessibilityValue (DebtPayoffBar | Money handling is disciplined: integer cents end-to-end, formatCurrency with en-ZA grouping, tabular-nums on numeric labels (chip dates, progress labels) so digits align | Domain logic is cleanly separated and defensively written: SnowballPayoffProjector has a 50-year cap and unpayable-debt short-circuit; LogDebtPaymentUseCase clamps overpayment and increments total_paid atomically; screens mostly stay thin | Empty states on SnowballDashboard and SinkingFunds have specific, actionable, localized copy (car service, school fees, holiday), and useFocusEffect reload keeps data fresh across the stack consistently | useBabySteps is a thoughtful hook: in-flight promise coalescing, AppState foreground/background reconcile paths, celebration queue with dedup, regression toasts with canonical copy

- (medium/VERIFIED) Dead CTA button: 'Add debt account' on Baby Steps 2/6 does nothing in production [src/presentation/screens/babySteps/BabyStepsScreen.tsx:124-129]
- (low/VERIFIED) '90-Day Forecast' title is false — forecast only covers the current ~30-day payday period [src/presentation/screens/forecasting/ForecastScreen.tsx:53]
- (low/VERIFIED) SevenDotPath computes node positions from window width but is rendered inside a padded card — step 7 dot overflows/clips [src/presentation/screens/babySteps/components/SevenDotPath.tsx:147]
- (medium/VERIFIED) Sinking fund 'on track' math is semantically wrong: total saved passed as monthly contribution [src/presentation/components/envelopes/SinkingFundCard.tsx:27-33]
- (medium/unverified) Forecast over-budget rows render an empty bar — the most critical rows get the least visual weight [src/presentation/screens/forecasting/ForecastScreen.tsx:84]
- (medium/unverified) CelebrationModal is unreadable in dark mode: hardcoded light overlay with theme-dependent text colors [src/presentation/screens/babySteps/CelebrationModal.tsx:37]
- (medium/unverified) ForecastScreen has no empty state — the gap is even documented as a TODO in its own test [src/presentation/screens/forecasting/ForecastScreen.tsx:58-70]
- (medium/unverified) Comma decimal input silently corrupts amounts in ZA-localized money fields [src/presentation/screens/debtSnowball/AddDebtScreen.tsx:41-43]
- (medium/unverified) AddDebtScreen crams 5 long SegmentedButtons on one row — cramped/truncated on phones [src/presentation/screens/debtSnowball/AddDebtScreen.tsx:88-93]
- (medium/unverified) Out-of-order Baby Step completion renders contradictory UI: same step in 'Completed' chips AND 'Coming Up' [src/presentation/screens/babySteps/BabyStepsScreen.tsx:62-66]
- (medium/unverified) Snowball debt rows are unlabeled tap targets for screen readers [src/presentation/screens/debtSnowball/SnowballDashboardScreen.tsx:47-50]
- (medium/unverified) No celebration when a debt is paid off — the emotional peak of the snowball method is a generic toast [src/presentation/screens/debtSnowball/LogPaymentScreen.tsx:60-62]
- (low/unverified) Future Baby Steps hidden entirely from screen readers [src/presentation/screens/babySteps/BabyStepsScreen.tsx:150-151]
- (low/unverified) CelebrationModalHost fabricates the completion date as 'now' [src/presentation/screens/babySteps/CelebrationModalHost.tsx:28]
- (low/unverified) 'Increase payments to project a date' points at a feature that does not exist; projector's extra-payment support is dead code [src/presentation/screens/debtSnowball/components/PayoffProjectionCard.tsx:44]
- (low/unverified) Presentation layer queries Drizzle directly, bypassing the hook/repository pattern [src/presentation/screens/debtSnowball/DebtDetailScreen.tsx:30-35]
- (low/unverified) Test suite is smoke-level exactly where the subsystem is weakest [src/presentation/screens/debtSnowball/__tests__/SnowballDashboardScreen.test.tsx:41-46]
  UPGRADE IDEAS:
  - (transformative) Debt-free mountain: interactive payoff timeline chart: The projector already simulates month-by-month balances but throws them away, keeping only payoff dates. Capture the full simulation series and render a stacked area chart of total debt declining to zero, one band per debt, with milestone markers whe
  - (transformative) What-if snowball slider: 'add R500/mo → debt-free 14 months sooner': SnowballPayoffProjector.project() already accepts extraMonthlyPaymentCents — it is pure, fast, and completely unused. Add a slider/stepper on the dashboard that re-runs the projection live as the user drags, animating the debt-free date and showing m
  - (major) Debt payoff celebrations with shareable win cards: Reuse the existing celebration architecture (queue store, modal host, seal marks, spring animation, reduce-motion handling) for debt events: debt paid off (broken-chain seal, amount destroyed, new snowball size rolling to the next debt), 25/50/75% mi
  - (major) Honest multi-period forecast with per-envelope sparklines and 'safe to spend today': Make the 90-day title true: project across the next 3 payday periods using per-envelope recurring allocations, weekday-weighted spend rates (weekend grocery spikes), and outlier damping for one-off purchases instead of naive spent/daysElapsed. Each r
  - (major) Real sinking fund ledger with countdown rings: Fix the data model so sinking funds have a cumulative balance and contribution history across periods (contributions ledger), then upgrade the card to a circular countdown ring (reuse CurrentStepHero's ProgressRing) showing percent saved, months left
  - (moderate) Interest-saved odometer: Run the projector once with actual payment history and once with minimums-only, and surface the difference as a continuously-growing 'Interest you will never pay: R8 431' counter on the snowball dashboard. Every extra payment visibly ticks it up. Che
  - (moderate) Progress-positive color language on DebtPayoffBar: The current red (<50%) → amber (≥50%) → green (100%) scale paints a user who just started their journey red — the color of failure — for months. Ramsey psychology celebrates every payment. Use a single brand hue with intensity/length encoding progres

## [C+] UI: auth, onboarding, household & settings screens

STRENGTHS: Consistent visual system: every screen uses theme tokens (spacing/radius), Paper components, and dark-mode-aware colors — no hardcoded hex values anywhere in these screens | Genuine accessibility effort on auth screens: accessibilityLabel, accessibilityLiveRegion, announceForAccessibility on sign-in failure (LoginScreen | Clean layering discipline: screens invoke domain use cases returning Result<T> (CreateHouseholdUseCase, AcceptInviteUseCase, CreateEnvelopeUseCase) instead of inlining SQL/HTTP | Nearly every screen has a test file (17 test files, ~2,300 lines), including targeted edge-case tests like PaydayStep | Thoughtful inline comments explaining non-obvious ordering decisions (FinishStep | Invite-joiner path smartly skips the budget wizard (JoinHouseholdScreen | HouseholdPickerScreen

- (high/VERIFIED) No password reset / forgot-password flow exists anywhere in the app [src/presentation/screens/auth/LoginScreen.tsx:123]
  detail: LoginScreen offers only Sign In and a link to SignUp. A repo-wide grep for resetPasswordForEmail/ForgotPassword/forgot returns zero hits. A user who forgets their password is permanently locked out of their financial data with no recovery path — for a personal-finance app this is a support ticket and churn machine.
  fix: Add a 'Forgot password?' link that calls supabase.auth.resetPasswordForEmail with a deep-link redirect, plus a ResetPassword screen handling the recovery token. This is table stakes for any auth surface.
- (high/VERIFIED) Switching households never restores that household's data locally [src/presentation/screens/household/HouseholdPickerScreen.tsx:21]
  detail: handleSelect just swaps householdId/paydayDay in the store and resets to Main. RestoreService is only ever invoked inside AcceptInviteUseCase (join path). If a household in availableHouseholds was created/joined on another device, or its remote data has advanced, switching to it shows an empty or stale local SQLite dataset with zero indication anything is missing. Worse: RootNavigator's per-(user,
  fix: On household switch, run RestoreService (with a progress UI) before landing on Main, and derive 'onboarding complete' from remote/local data presence (e.g. household has envelopes) rather than a device-local AsyncStorage flag.
- (medium/VERIFIED) Onboarding envelope creation is not crash-safe or idempotent — duplicates envelopes on re-run [src/presentation/screens/auth/onboarding/AllocateEnvelopesStep.tsx:83]
- (medium/VERIFIED) NaN payday day bypasses domain validation and poisons the household [src/presentation/screens/household/CreateHouseholdScreen.tsx:34]
- (medium/unverified) Envelopes are created BEFORE the payday question, anchored to the wrong budget period [src/presentation/screens/auth/onboarding/AllocateEnvelopesStep.tsx:81]
- (medium/unverified) Household modal screens render with no header, title, or back button [src/presentation/navigation/RootNavigator.tsx:126]
- (medium/unverified) ShareInviteScreen burns a new single-use invite code on every mount [src/presentation/screens/household/ShareInviteScreen.tsx:22]
- (medium/unverified) Signing up with an already-registered email shows a misleading 'Check your email' screen [src/presentation/screens/auth/SignUpScreen.tsx:58]
- (medium/unverified) NotificationPreferences numeric inputs fight a 600ms-debounced store — keystrokes revert and fields cannot be cleared [src/presentation/screens/settings/NotificationPreferencesScreen.tsx:90]
- (medium/unverified) Join flow: uncaught markOnboardingComplete rejection can strand a joiner in the budget wizard [src/presentation/screens/household/JoinHouseholdScreen.tsx:66]
- (medium/unverified) No biometric/app-lock protection for a financial-data app [src/presentation/screens/settings/SettingsScreen.tsx:57]
- (medium/unverified) No account deletion path — Google Play compliance gap and settings discoverability holes [src/presentation/screens/settings/SettingsScreen.tsx:203]
- (low/unverified) MeterSetupStep swallows insert failures and asserts householdId non-null [src/presentation/screens/auth/onboarding/MeterSetupStep.tsx:30]
- (low/unverified) Progress indicator appears on only 3 of 8 onboarding steps; layout duplicated by hand [src/presentation/screens/auth/onboarding/AllocateEnvelopesStep.tsx:102]
- (low/unverified) Monthly income is asked, used once, then discarded — never persisted [src/presentation/screens/auth/onboarding/IncomeStep.tsx:32]
- (low/unverified) FinishStep has no test; sign-out and Wi-Fi toggle lack failure handling [src/presentation/screens/auth/onboarding/FinishStep.tsx:16]
  UPGRADE IDEAS:
  - (transformative) First win in 90 seconds: collapse onboarding to 3 questions + log-first-transaction finale: The current funnel honestly advertises '12 minutes' across 8 steps, two of which are pure reading (ScoreIntro) or deferrable (MeterSetup), and one duplicates a question already answered (payday). Cut to: income → pick categories (with a smart default
  - (transformative) Biometric app lock + privacy screen: A budgeting app holding a family's complete financial picture, on Android where devices are shared, has zero access protection today. Add expo-local-authentication gating cold start and resume-from-background (configurable grace period), PIN fallback
  - (major) Deep-link + QR invites with household preview: Today inviting someone requires them to install the app, sign up, create-or-skip a household, find 'Join a Household' in Settings, and hand-type a 6-character code. Replace with a shareable https link (Supabase edge function + Android App Links) that
  - (major) Household Hub: members, roles, switching, and safe data restore: Promote household from four disconnected modal screens to a first-class hub: member list with roles and pending invites (revocable), rename/leave household, payday editing, per-household color/emoji identity. Put a household avatar in the main app he
  - (major) Settings v2: search, sync status, data export, account self-service: Settings is currently a thin list missing the things users actually look for. Add: a sync status row (last synced, pending count, manual sync button — the app is offline-first yet sync state is invisible); data export to CSV for the tax-season crowd;
  - (moderate) Resumable onboarding state machine with funnel analytics: Persist wizard progress (current step + collected answers) to storage so an app kill resumes where the user left off instead of restarting at Welcome and re-creating envelopes. Model steps as a single ordered config array driving both the navigator a
  - (moderate) Native pickers and live previews for notification preferences: Replace the raw 'Hour (0-23)' / 'Minute (0-59)' text fields with the platform time picker and the day field with a calendar-style day selector, add a 'Send test notification' button so users trust the setup, and show the next scheduled fire time unde

## [C+] UI platform: components, theme, navigation, stores, hooks

STRENGTHS: Boot crash infrastructure is genuinely excellent: early crash handler installed before imports (App | Token system is real and disciplined where adopted: src/presentation/theme/tokens | Zustand store design is small and focused: seven single-purpose stores, atomic selectors used almost everywhere (useAppStore((s) => s | useBabySteps is a well-engineered hook: coalesced in-flight reconcile via useRef promise, AppState foreground/background split (modal vs notification), injectable deps for tests, and dedup delegated to the store | Navigation is fully typed: per-stack ParamLists with CompositeScreenProps for tab+stack screens (navigation/types | Test breadth is strong: nearly every component, store, hook, and navigator has a test file, including accessibility-relevant assertions; accessibilityRole/Label/LiveRegion usage across components (OfflineBanner, HeroSummaryCard, dashboard rows) is above average

- (medium/VERIFIED) Dashboard ignores the in-app theme preference — theme source split across the app [src/presentation/screens/dashboard/DashboardScreen.tsx:41]
- (high/VERIFIED) Cold start blocks first paint on a full remote restore, and initSession re-runs on every auth event [App.tsx:203-246]
  detail: App renders a blank View until sessionRestored=true, which is only set after `await initSession(...)` — and initSession awaits RestoreService.restore(userId), which fetches household_members from Supabase and re-downloads/upserts every table for every household on EVERY cold start (src/data/sync/RestoreService.ts:34-56), not just reinstalls. On a slow/flaky network the user stares at an unbranded
  fix: Gate first paint only on local resolution (EnsureHouseholdUseCase); run restore/sync in the background after the navigator mounts. Filter onAuthStateChange by event type (run initSession only on SIGNED_IN, skip TOKEN_REFRESHED/INITIAL_SESSION when already initialized). Show a branded splash instead
- (medium/VERIFIED) The 'PULSE' dashboard redesign forked the design system [src/presentation/screens/dashboard/components/HeroSummaryCard.tsx:16-45]
- (medium/unverified) Presentation hooks query SQLite directly with Drizzle, bypassing domain/repository layers [src/presentation/hooks/useEnvelopes.ts:23-33]
- (medium/unverified) Seven dead components are shipped and maintained with tests; ScreenErrorBoundary is never mounted [src/presentation/components/shared/ScreenErrorBoundary.tsx:1]
- (medium/unverified) Dead store state: syncStore's sync status fields and appStore.currentPeriod are never written [src/presentation/stores/syncStore.ts:8-10]
- (medium/unverified) Gamification level system is dead in the UI: userLevel never advances and never hydrates [src/presentation/hooks/useLevelAdvancement.ts:13]
- (medium/unverified) Dashboard Ramsey score is computed with hardcoded wrong inputs [src/presentation/screens/dashboard/DashboardScreen.tsx:92-99]
- (medium/unverified) NavigationContainer is unthemed and has no deep-linking config [src/presentation/navigation/RootNavigator.tsx:125]
- (low/unverified) Inconsistent hook contracts: some auto-load, some require manual reload, with mismatched initial loading state [src/presentation/hooks/useDebts.ts:16]
- (low/unverified) Duplicate and overlapping test files inflate the suite [src/presentation/components/shared/CurrencyText.test.tsx:1]
- (low/unverified) Stale module documentation in useAppTheme.ts misdescribes the codebase [src/presentation/theme/useAppTheme.ts:1-6]
- (low/unverified) Currency formatting duplicated with divergent output formats [src/presentation/utils/currency.ts:6]
- (low/unverified) RootNavigator subscribes to the entire notificationStore [src/presentation/navigation/RootNavigator.tsx:40]
- (low/unverified) Font family string literals scattered instead of typography tokens [src/presentation/screens/dashboard/DashboardScreen.tsx:387]
  UPGRADE IDEAS:
  - (transformative) Reactive local-first data layer (kill the reload() treadmill): Replace the four copy-paste query hooks with Drizzle's useLiveQuery (or TanStack Query keyed on table+household+period with an invalidation bus fired from repositories/SyncOrchestrator). Every screen then updates instantly when a transaction is added
  - (transformative) Skeleton-first cold boot with a visible sync story: Render the navigator from local SQLite immediately (fonts + db only), replace the blank white View with a branded splash, and move RestoreService/syncPending into a background job that publishes into the already-existing-but-dead syncStore fields (pe
  - (major) One design language: promote PULSE into the token system: The PULSE dashboard is visually the most ambitious part of the app — frosted glass, arc gauges, Fraunces hero numerals — but it lives as a fork. Promote its palette into tokens.ts as proper semantic light/dark tokens, add typography tokens (families
  - (major) Notification deep-linking and celebration continuity: Wire a linking config + notification-response handler so FCM/local notifications land somewhere meaningful: baby-step celebration -> BabySteps screen with the celebration modal firing, meter reminder -> AddReading with the right meterType param, even
  - (major) Resurrect the gamification loop end-to-end: The Ramsey Score and level system are the app's differentiator but are currently half-dead: score computed with hardcoded false meter input, useLevelAdvancement never called, userLevel never hydrated or persisted. Build the loop properly: compute the
  - (moderate) Per-screen error boundaries with graceful retry: Mount the existing-but-unused ScreenErrorBoundary around every stack screen via a shared screen wrapper, styled with the token system, offering 'Try again' (remount) and 'Report' (share via the existing earlyCrashLog/Crashlytics plumbing). Today one

## [D+] Backend: Supabase (migrations, RLS, edge functions)

STRENGTHS: Defense-in-depth write path: all client writes are routed through SECURITY DEFINER merge\_\* RPCs with explicit household-membership checks, and migration 019 revokes direct INSERT/UPDATE/DELETE on every sync table from `authenticated` — PostgREST table-level DML is genuinely closed off | extract-slip edge function is layered well: auth -> membership -> consent -> slip ownership -> idempotency -> 5MB payload cap -> atomic advisory-lock rate limit (check_and_reserve_slip_slot eliminates the TOCTOU window) -> prompt-injection defense that allowlists suggested_envelope_id against the household's real envelope IDs -> per-call OpenAI cost tracking | Storage RLS on slip-images is solid: private bucket, per-household folder paths validated with a strict UUID regex (007) that kills path-traversal including URL-encoded variants | PII lifecycle discipline: pg_cron job deletes slip images and nulls raw_response_json after 30 days, with per-image error isolation and audit logging of failures (015) | extract-slip has real dependency-injected unit tests (748 lines) covering auth, consent, rate-limit, and OpenAI failure branches | The LWW tiebreaker redesign in 016 (strictly-greater + deterministic id tiebreak) shows genuine thought about direction-independent conflict resolution, and it is documented in-file

- (critical/VERIFIED) Invite acceptance is broken: RPC parameter name mismatch (invite_code vs p_invite_code) [src/domain/households/AcceptInviteUseCase.ts:51]
  detail: supabase/migrations/019_batch1_security_hardening.sql:12 defines join_household_via_invite(p_invite_code text), but AcceptInviteUseCase calls supabase.rpc('join_household_via_invite', { invite_code: ... }). PostgREST resolves RPCs by named arguments, so this call returns PGRST202 'could not find function ... (invite_code)' 100% of the time. Since 019 also dropped hm_insert and revoked INSERT on ho
  fix: Change the client to pass p_invite_code (or add a migration renaming the SQL parameter). Add at least one integration test against a real local Supabase (supabase start + supabase db reset) that exercises the invite round-trip, since mocked rpc() tests cannot catch name/signature drift.
- (critical/VERIFIED) Invite creation is broken: 019 revokes INSERT on invitations but the client still inserts directly [supabase/migrations/019_batch1_security_hardening.sql:265]
  detail: Line 265 executes REVOKE INSERT, UPDATE, DELETE ON public.invitations FROM authenticated, but CreateInviteUseCase (src/domain/households/CreateInviteUseCase.ts:36-45) still creates invites via supabase.from('invitations').insert(...). Every invite creation now fails with 'permission denied for table invitations'. There is no create-invite RPC anywhere in supabase/migrations to replace it, and the
  fix: Add a create_invitation(p_household_id) SECURITY DEFINER RPC that verifies owner role, generates the code server-side, and returns it; or re-grant INSERT and rely on the (fixed — see tautology finding) inv_insert policy. Then delete the dead policy or the dead client path so there is exactly one ble
- (high/VERIFIED) Migration chain cannot apply on a fresh database: 010 duplicates 008_user_preferences with non-idempotent CREATE POLICY [supabase/migrations/010_user_preferences.sql:13]
  detail: 008_user_preferences.sql and 010_user_preferences.sql are byte-for-byte the same table + policies. CREATE TABLE IF NOT EXISTS is idempotent, but CREATE POLICY up_select/up_insert/up_update has no DROP POLICY IF EXISTS guard, so applying 010 after 008 fails with 42710 duplicate_object and aborts the migration run. Any fresh environment (new developer, CI, disaster recovery, `supabase db reset`) die
  fix: Delete or empty 010 (or wrap its policies in DROP POLICY IF EXISTS). Adopt the Supabase CLI timestamped naming convention to prevent prefix collisions, and add a CI job that runs `supabase db reset` against the full migration chain on every PR — that single check would have caught this and the 017 p
- (high/VERIFIED) inv_insert RLS policy is a tautology: unqualified household_id binds to the inner table [supabase/migrations/019_batch1_security_hardening.sql:92]
  detail: The policy's EXISTS subquery reads `WHERE hm.household_id = household_id`. Inside the subquery, the unqualified household_id resolves to hm.household_id (innermost scope wins in Postgres), making the predicate hm.household_id = hm.household_id — always true. The policy therefore checks 'caller is an owner of ANY household', not 'owner of THIS invitation's household'. Today this is latent because I
  fix: Qualify the outer column: `WHERE hm.household_id = invitations.household_id`. Add a behavioral RLS test (two users, two households) proving a non-owner-of-target cannot insert an invitation for it.
- (high/VERIFIED) Removing a household member never revokes access: user_households is only synced on INSERT [supabase/migrations/005_security_and_sync_correctness.sql:85]
  detail: All RLS policies and merge-RPC membership checks key off user_households, which is populated exclusively by the AFTER INSERT trigger tr_household_members_sync_user_households (005:85-89). There is no AFTER DELETE/UPDATE mirror. When a member is removed via delete_sync_row('household_members', id) (019:208-252), the household_members row disappears but the user_households mapping persists forever —
  fix: Add an AFTER DELETE trigger on household_members that deletes the matching user_households row (and an AFTER UPDATE trigger to mirror role changes). Add a test: remove member, assert their SELECT on the household's transactions returns zero rows.
- (high/VERIFIED) delete_sync_row lets any member kick the owner and destroy audit history [supabase/migrations/019_batch1_security_hardening.sql:223]
  detail: delete_sync_row authorizes purely on 'caller is a member of the row's household', then deletes with SECURITY DEFINER privileges. Two consequences: (1) household_members is in the allowlist, so a plain 'member' can delete the owner's membership row (or anyone else's) — a privilege inversion with no role check; (2) audit_events is in the allowlist, so any member can erase audit trail rows, directly
  fix: Remove audit_events from the allowlist entirely. For household_members, require the caller to be deleting their own row OR be an owner. For data tables, add a remote tombstone table (mirroring local migration 0009) consulted by the merge RPCs so deletes are not resurrected by stale peers.
- (high/unverified) notify-event uses the shut-down FCM legacy API and has no input validation or rate limiting [supabase/functions/notify-event/index.ts:103]
  detail: The function POSTs to https://fcm.googleapis.com/fcm/send with `Authorization: key=...` — the FCM legacy HTTP API that Google deprecated in June 2023 and shut down in mid-2024. Every send now fails (res.ok false), the function reports {sent: 0}, and push notifications are silently dead. Also: req.json() at line 42 is unwrapped (malformed body -> unhandled exception -> opaque 500); title/body are f
  fix: Migrate to FCM HTTP v1 (OAuth2 service-account token, projects/{id}/messages:send). Wrap body parsing, validate userId/householdId as UUIDs and cap title/body length (~200/1000 chars), and add a simple per-user rate limit (e.g., reuse the advisory-lock counter pattern from check_and_reserve_slip_slo
- (high/unverified) LWW conflict resolution compares TEXT timestamps in three inconsistent formats [supabase/migrations/008_phase2_data_integrity.sql:103]
  detail: All updated_at columns are TEXT and every merge RPC does lexicographic comparison (EXCLUDED.updated_at > existing.updated_at). Clients write ISO-8601 ('2026-07-02T10:00:00.000Z'), but server-side writers use NOW()::text ('2026-07-02 10:00:00.123+00' — check_and_reserve_slip_slot 008:103, cleanup_old_slip_images 015:52) and (NOW() AT TIME ZONE 'UTC')::text ('2026-07-02 10:00:00.123' — join_househol
  fix: Standardize: have all server-side writers emit to_char(NOW() AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'), or better, migrate updated_at to timestamptz and compare natively (client ISO strings cast cleanly). Add a CHECK constraint enforcing the ISO format on TEXT columns in the interim.
- (medium/unverified) 30-bit invite codes with unthrottled brute-force oracle [src/domain/households/CreateInviteUseCase.ts:16]
- (medium/unverified) Security 'tests' are regex assertions over migration text, not behavior [src/__tests__/security/019-batch1-security.test.ts:17]
- (medium/unverified) extract-slip: failed-slip retry returns a misleading 429, and image array elements are unvalidated [supabase/functions/extract-slip/index.ts:151]
- (medium/unverified) Ownerless-household bootstrap in merge_household_member [supabase/migrations/019_batch1_security_hardening.sql:121]
- (low/unverified) Migration idempotence and lint debt across the chain [supabase/migrations/014_check_constraints.sql:6]
  UPGRADE IDEAS:
  - (transformative) Ephemeral-database CI with behavioral RLS test suite: Every defect class found here (parameter drift, phantom columns, duplicate policies, tautological RLS) is catchable by one pipeline: `supabase start` + `supabase db reset` in CI, then a pgTAP/Jest suite that creates two real users in two households a
  - (transformative) Replace hand-rolled per-table LWW RPCs with a single versioned sync protocol: Ten nearly-identical merge\_\* functions drifted apart four times in four migrations. Replace them with one generic `sync_push(p_table, p_rows jsonb)` driven by a column-metadata table, using timestamptz + a per-row monotonically-increasing server vers
  - (major) Household roles and lifecycle as a first-class server capability: Right now roles are a string column enforced ad hoc in three RPCs, member removal leaks access forever, and there's no ownership transfer. Build a proper capability layer: owner/admin/member/viewer roles, RPCs for invite-create, member-remove (cleans
  - (major) Event-driven push notifications via database webhooks + FCM v1: Instead of the client calling notify-event (dead legacy FCM, spammable, requires the sender's device to be online), emit notifications server-side: pg triggers on domain events (envelope overspent, debt paid off, invite accepted, slip completed) writ
  - (major) Slip pipeline v2: storage-first, queue-processed, cost-governed: Base64-in-JSON caps quality at 5MB and blocks the client for the full 30s OpenAI round trip. Move to: client uploads images straight to the slip-images bucket (RLS already solid), inserts a queue row, and a pgmq/cron-driven worker edge function proce
  - (moderate) Household-scoped Realtime sync channels: Supabase Realtime with RLS-authorized postgres_changes subscriptions per household would let a spouse's transaction appear on your phone in seconds instead of at next poll. The RLS SELECT policies needed to authorize the channels already exist. Combi

## [C+] Quality: tests, e2e & CI

STRENGTHS: CI PR gate is genuinely strict: Prettier check, tsc --noEmit, ESLint --max-warnings 0, Jest with enforced coverage thresholds (80% lines / 60% branches in jest | Domain-layer tests are real behavioral tests: BabyStepEvaluator/ReconcileBabyStepsUseCase integration lifecycle (src/**tests**/integration/baby-steps-progression | SyncOrchestrator tests (src/data/sync/SyncOrchestrator | fast-check property-based testing is present and partly real (src/domain/**tests**/financial-calculations | Zero snapshot tests across 239 test files — deliberate avoidance of the classic vanity-coverage trap | E2E infrastructure exists and runs on a real Android emulator in CI with KVM acceleration, logcat capture, and hermetic network blacklisting (ci | Pre-commit hook runs lint-staged + incremental tsc ( | 2023 tests run in ~2

- (medium/VERIFIED) Security test suite is largely self-testing theater — mocks the exact thing it claims to verify [src/__tests__/security/household-isolation.test.ts:157]
- (medium/VERIFIED) 12 test files assert production behavior by grepping source code as strings [src/presentation/__tests__/accessibility.test.tsx:22]
- (medium/VERIFIED) E2E journeys do not perform their named journeys — zero authenticated app coverage [e2e/journeys/addEnvelope.e2e.ts:38]
- (medium/VERIFIED) Play Store publish is not gated on e2e — CD races CI on every master push [.github/workflows/cd.yml:30]
- (medium/unverified) Suite is not green cross-platform: 2 tests fail locally with default 5s timeouts, dragging coverage below the gate [src/presentation/screens/debtSnowball/__tests__/DebtDetailScreen.test.tsx:163]
- (medium/unverified) CI push trigger `branches: ['*']` never matches slash-named branches [.github/workflows/ci.yml:5]
- (medium/unverified) No test anywhere executes real SQL — SQLite, Drizzle, and all 19 migrations are stubbed [jest.config.js:15]
- (medium/unverified) E2E specs globally disable Detox synchronization then assert immediately — boot race baked in [e2e/journeys/login.e2e.ts:27]
- (low/unverified) ESLint and typecheck coverage gaps outside src/; 1.4MB of crash logs committed to scripts/ [scripts/crash50794.log]
- (low/unverified) One property-based test reimplements the formula it claims to verify [src/domain/__tests__/financial-calculations.test.ts:305]
  UPGRADE IDEAS:
  - (transformative) Real-database test tier: better-sqlite3 + actual migrations: Add a Jest project that opens an in-memory better-sqlite3 database via drizzle-orm/better-sqlite3, applies the real migration journal (all 19+ migrations), and runs repository, sync-queue, tombstone, and rollover tests against genuine SQL. This retir
  - (transformative) Hermetic full-stack e2e: local Supabase + authenticated Detox/Maestro journeys: Run `supabase start` (Postgres + GoTrue + PostgREST in Docker) in the e2e job, point the app at it via EXPO_PUBLIC_SUPABASE_URL, seed a test household, and write the journeys the file names already promise: sign up → onboard → create envelope → add t
  - (major) Single gated release train: check → e2e → Test Lab → publish: Collapse ci.yml and cd.yml into one pipeline where Play publishing hard-depends on unit tests, Detox e2e, and a non-optional Firebase Test Lab run, with a reusable 'quality-gate' workflow so the PR gate and release gate can never drift. Add concurren
  - (major) Mutation testing to expose and kill vanity coverage: Run StrykerJS over src/domain and src/data/sync on a weekly scheduled workflow. Mutation score will immediately quantify what this review found qualitatively: the tautological security tests and string-grep tests kill ~0 mutants. Use the surviving-mu
  - (major) Property-test the invariants that guard user money and sync: Extend the existing fast-check foundation from one file to the system's core invariants: envelope allocate/spend/rollover never creates or destroys cents (conservation property across BudgetPeriodEngine rollovers), toSupabaseRow/fromSupabaseRow round
  - (moderate) Flake-resistant test ergonomics: sharding, retry telemetry, and a quarantine lane: Shard Jest across 2-4 CI workers with --shard, cache the Jest transform cache keyed on the lockfile, and add jest-junit output feeding a flake dashboard (GitHub summary or Buildkite-style retry annotations). Add a @quarantine tag convention: known-fl

## [C] App shell & performance

STRENGTHS: The early-crash observability stack is genuinely excellent for a RN app: installEarlyCrashHandler + captureBoot (src/infrastructure/monitoring/earlyCrashLog | initSession parallelizes local household resolution and remote restore via Promise | Migration integrity is enforced at boot with djb2 checksums and orphan detection (src/data/local/db | Hermes is enabled (android/gradle | Dependency surface is lean for a 52k LOC app; versions are current for Expo SDK 55, TypeScript is strict, and the single patch-package patch documents exactly why it exists

- (high/VERIFIED) First interactive render is gated on an un-timed remote network call [App.tsx:258]
  detail: App renders a bare blank <View> until fontsLoaded && dbReady && sessionRestored. sessionRestored is only set after supabase.auth.getSession() AND await initSession() complete (App.tsx:213-222), and initSession awaits restoreService.restore(userId) — a Supabase network call with no timeout or AbortController anywhere in src/data/sync/RestoreService.ts. expo-splash-screen is not a dependency, so the
  fix: Add expo-splash-screen and keep the splash up through boot; wrap restore in a hard timeout (e.g. Promise.race 3-5s) and never block sessionRestored on the network — resolve the household from local DB first, run restore/sync fully in the background, and show a skeleton or spinner instead of an empty
- (medium/VERIFIED) initSession failure leaves the app on a blank screen forever [App.tsx:213-222]
- (high/VERIFIED) initSession runs twice at boot and re-runs on every token refresh; awaited Supabase calls inside onAuthStateChange risk deadlock [App.tsx:225-232]
  detail: supabase.auth.getSession().then() runs initSession, and onAuthStateChange — which Supabase fires with INITIAL_SESSION on subscribe, plus SIGNED_IN and hourly TOKEN_REFRESHED — also awaits initSession(session.user.id) with the \_event parameter ignored. Result: duplicate remote restore + duplicate syncPending races on every cold start, and a full restore + EnsureHousehold + sync cycle on every token
  fix: Filter events (only run initSession on SIGNED_IN; skip INITIAL_SESSION/TOKEN_REFRESHED), guard with an in-flight/last-user-id check, and defer the body with setTimeout(…, 0) per Supabase guidance so the callback returns synchronously.
- (medium/VERIFIED) Non-fatal errors and any unhandled rejection trigger the full-screen 'Previous boot crashed' gate on next launch [src/infrastructure/monitoring/earlyCrashLog.ts:122]
- (low/REFUTED) 16 KB page-size non-compliance is deferred, not solved — Play Store updates at risk [android/app/build.gradle:103-106]
- (medium/unverified) newArchEnabled=false is dead configuration on React Native 0.83 [app.config.ts:36]
- (medium/unverified) R8 minification and resource shrinking disabled in release builds [android/app/build.gradle:73]
- (medium/unverified) react-native-vector-icons bundles every icon font and the package is deprecated [android/app/build.gradle:204]
- (medium/unverified) No over-the-air update capability at all [app.config.ts:30-73]
- (medium/unverified) patch-package patch pinned to firebase 24.0.0 while the dependency range floats (^24.0.0) [patches/@react-native-firebase+app+24.0.0.patch]
- (medium/unverified) Comment claims index.ts wraps module evaluation in try-catch — it does not; import-time crashes escape the early handler [App.tsx:3-5]
- (low/unverified) NetworkObserver.onConnected callbacks can never be removed — leak on remount [src/infrastructure/network/NetworkObserver.ts:10-12]
- (low/unverified) Fatal-crash persistence relies on a fire-and-forget AsyncStorage write racing process death [src/infrastructure/monitoring/earlyCrashLog.ts:41-47]
- (low/unverified) Dual source of truth: committed android/ directory plus app.config.ts config plugins [android/app/build.gradle:110-112]
- (low/unverified) Zero test coverage for the App.tsx boot orchestration itself [App.tsx:88-137]
  UPGRADE IDEAS:
  - (transformative) Instant-on boot: render the budget before the network exists: The app is offline-first with all data in local SQLite, yet boot waits on Supabase. Invert it: keep the native splash up via expo-splash-screen, resolve household + envelopes from SQLite synchronously-fast, and paint the last-known budget screen in u
  - (major) OTA updates with crash-aware auto-rollback: Adopt expo-updates (fingerprint runtime versions, preview/production channels in eas.json) and wire it into the existing earlyCrashLog: if a persisted fatal crash record exists at startup, call Updates.fetchUpdateAsync/rollback to the embedded bundle
  - (major) Boot performance telemetry and CI budgets: Instrument the boot phases that already exist as gates (bundle eval → migrations → fonts → session → first navigator frame) with performance.now() markers reported to Crashlytics custom keys, so cold-start regressions are visible per release in the f
  - (major) Testable BootController state machine: Replace the ad-hoc App.tsx effects (two competing auth entry points, module-scope singletons, unbounded gating booleans) with an explicit boot state machine — idle → migrating → resolvingHousehold → ready | degraded(error) — living in domain/boot wit
  - (moderate) New Architecture + SDK 36 modernization pass: Do one deliberate native-health sprint: confirm and embrace New Architecture (it is already running on RN 0.83 whether intended or not), verify 16 KB alignment of every .so and move to targetSdk 36, enable R8 + resource shrinking + icon-font pruning

=== RESEARCH ===

## Competitor teardown 2025-2026: YNAB, EveryDollar, Monarch Money, Actual Budget, Goodbudget — features, pricing, envelope-budgeting UX, and user sentiment

- YNAB's price has more than doubled since going subscription ($50 → $109/yr, $14.99/mo in 2026) and price increases are the single most common complaint on Reddit and in reviews. Yet retention among engaged users is ~75% at 12 months and users report ~$6,000 saved in year one — proving people will pa [https://www.fincomparelab.com/guides/ynab-pricing/ and https://envelopebudgeting.com/articles/best-budgeting-apps]
- YNAB's most-loved UX mechanic is Targets + the Underfunded view + one-tap Auto-Assign: every category can have a goal, the app shows exactly which envelopes are underfunded, and a single button distributes available dollars by due-date priority. Users request even more flexibility (partial/percentag [https://support.ynab.com/en_us/underfunded-a-guide-BJwPhQO09 and https://github.com/toolkit-for-ynab/toolkit-for-ynab/issues/3151]
- YNAB's most-hated UX mechanic is its credit card handling: spending on a card silently moves money from the spending envelope into a per-card 'Credit Card Payment' category. New users take 2-4 weeks to understand it and many quit over it. Simpler envelope apps just decrement the category regardless [https://www.moneymindedme.com/blog/ynab-credit-card-handling and https://www.thepennyhoarder.com/budgeting/ynab-review/]
- EveryDollar — the official Ramsey app and this project's most direct competitor — relaunched in January 2026 with: Margin Finder (guided ~15-min questionnaire surfacing forgotten subscriptions/overpaid bills; claimed avg $3,015 of margin found), Debt-Free Date Calculator (live-updating snowball payo [https://www.globenewswire.com/news-release/2026/01/08/3215300/0/en/RAMSEY-SOLUTIONS-RELAUNCHES-EVERYDOLLAR-TO-HELP-USERS-ACHIEVE-20-BILLION-IN-FINANCIAL-TRANSFORMATION-PER-YEAR-BY-2030.html]
- EveryDollar's weaknesses: the free tier is manual-entry only (bank sync, Paycheck Planning, Margin Finder and coaching are paywalled at $17.99/mo or $79.99/yr), it is less flexible than YNAB (no true rollover envelopes in the YNAB sense), and Android users specifically report crashes and glitches wh [https://lendedu.com/blog/everydollar-review/ and https://www.nerdwallet.com/finance/learn/everydollar-app-review]
- EveryDollar's Paycheck Planning (Premium) is a differentiator users value: it maps upcoming bills/spending against specific paydays so users can pace spending between paychecks — addressing the reality that most people budget per-paycheck, not per-month. YNAB handles this poorly and its community ke [https://everydollar.help.ramseysolutions.com/hc/en-us/articles/11667520933773-Paycheck-Planning]
- Monarch Money ($99.99/yr Core, new $199/yr Plus tier in 2026) wins on couples/household collaboration — unlimited equal-partner access to shared budgets and goals is called its strongest differentiator. But its Trustpilot is 2.2/5, driven not by price but by unreliable bank connections and support: [https://www.thepennyhoarder.com/budgeting/monarch-money-review/ and https://envelopebudgeting.com/articles/monarch-money-review]
- Monarch's Flex Budgeting is the notable 2025-2026 budgeting-UX innovation: categories are grouped into Fixed / Non-monthly / Flex buckets and the user manages just ONE flexible-spending number instead of dozens of envelope amounts. It is explicitly pitched as 'the one-number budget that actually sti [https://help.monarch.com/hc/en-us/articles/32125337244052-Using-Flex-Budgeting and https://www.monarch.com/blog/flex-budgeting-simplify-your-spending-with-just-one-number]
- Actual Budget (free, open-source, local-first with CRDT sync — architecturally the closest cousin to this app's offline-first SQLite+Supabase design) validates local-first envelope budgeting at scale. Its killer features: a rules engine that AUTO-CREATES rules from user behavior (rename a payee or c [https://actualbudget.org/docs/budgeting/rules/ and https://archive.jlongster.com/using-crdts-in-the-wild and https://getfinny.app/blog/best-budget-apps-reddit-recommends-2026]
- Goodbudget proves a manual-entry, privacy-first envelope app still has a market ($10/mo or $80/yr Premium): manual entry is reframed as intentional mindfulness, and its usable free tier (20 envelopes, 1 account) is a strong acquisition funnel. Its stated weaknesses — dated UI, weak trend reporting, [https://www.nerdwallet.com/finance/learn/goodbudget-app-review and https://www.fincomparelab.com/guides/goodbudget-pricing/]
- The central UX tension across all five apps is automation vs. awareness: bank-sync automation reduces friction but weakens behavior change; manual entry builds mindfulness but causes churn. The winning envelope-UX patterns are (a) envelope balance visible at the moment of purchase decision ('dining [https://envelopebudgeting.com/articles/best-budgeting-apps and https://envelopebudgeting.com/articles/best-envelope-budgeting-app]
  RECOMMENDATIONS:
  - (transformative) Ship a live Debt-Free Date on the home screen with a what-if slider: EveryDollar's January 2026 relaunch made the Debt-Free Date Calculator its hero feature; this app already has the debt snowball domain logic in src/domain. Leapfrog it: compute the projected debt-free date locally (no premium paywall, works offline), pin it to the dashboard, recalculate instantly on
  - (transformative) Build YNAB-style Underfunded + one-tap Auto-Assign, but paycheck-aware: Copy YNAB's most-loved mechanic — per-envelope targets, an Underfunded view, and one tap to distribute available money by priority — but fix the gap both YNAB and EveryDollar users complain about: fund per PAYCHECK, not per month. On payday (detectable from recurring income transactions), prompt 'As
  - (major) Add an Actual-style self-learning rules engine feeding off slip scanning: Actual Budget auto-creates categorization rules from user behavior: categorize or rename a payee once and it applies forever. Implement the same in the sync/repository layer: when a user categorizes a scanned slip or manual transaction, silently create a payee→envelope rule (with an undoable 'Always
  - (major) Keep credit-card spending simple — never adopt YNAB's payment-category model: YNAB's per-card 'Credit Card Payment' envelope is its single biggest onboarding killer (2-4 weeks to understand, a top reason users quit). Design decision for this app: a card purchase just decrements the spending envelope, and card balances are tracked as debts in the snowball — which matches Ramse
  - (major) Build a 'Margin Finder' onboarding that mines transactions for leaks: EveryDollar's Margin Finder (guided questionnaire, claimed avg $3,015 found) is a marketing weapon. This app can do better with data instead of a questionnaire: after 1-2 months of transactions (or an initial CSV/statement import), run local detection of recurring payees, duplicate subscriptions, an
  - (major) Make couples/household budgeting the headline, with sync reliability as the proof: Monarch's #1 differentiator is frictionless equal-partner household access, yet its 2.2/5 Trustpilot is driven by sync/connection failures. This app already has multi-household with invite codes AND a local-first architecture (like Actual's CRDT design) that keeps working offline. Invest in: real-ti
  - (moderate) Add streaks and daily bite-sized lessons feeding the Ramsey Score: EveryDollar 2026 added daily lessons + streaks as Premium retention mechanics. This app already has Ramsey Score gamification and a coaching module: add a check-in streak (log/categorize transactions, review envelopes), 60-second daily lessons drawn from the baby-steps content, and make streaks cont
  - (moderate) Price as the anti-YNAB: generous free tier, cheap premium, no sync tax: The market's paywall pattern is: bank sync and planning features cost $80-199/yr (YNAB $109, EveryDollar Premium $79.99, Monarch $99.99-199, Goodbudget $80), while free tiers are crippled (Goodbudget: 1 account, 20 envelopes; EveryDollar: no sync, no paycheck planning). Since this app is manual+OCR-
  - (moderate) Offer an optional Flex-style 'one-number' mode on top of envelopes: Monarch's Flex Budgeting (Fixed / Non-monthly / Flex buckets, one discretionary number) is the notable 2025-2026 innovation for users who abandon fine-grained envelopes. Add a simplified view mode: tag envelopes as Fixed, Sinking (maps to existing sinking funds), or Flex, and show a single 'Safe to

## Fintech mobile UI/UX trends 2026 — applied to AccountingV2 (Expo/React Native Paper envelope-budgeting app)

- In 2026 fintech UX is judged by decision confidence, not visual modernity: the best dashboards answer three questions instantly — where money stands, what needs attention now, and what to do next — and actively reduce decision fatigue by hiding what doesn't matter and nudging the next best action. D [https://www.onething.design/post/top-10-fintech-ux-design-practices-2026 ; https://www.gitnexa.com/blogs/modern-fintech-ui-ux-trends ; https://www.uxpin.com/studio/blog/dashboard-design-principles/]
- Money typography has hard rules: tabular lining figures (fontVariant tabular-nums), always show trailing zeros (54.00 never 54.3) so numbers don't jump, use the true minus sign U+2212 (not hyphen), real Unicode currency symbols (not 'USD' text), and locale-aware formatting via Intl.NumberFormat. Fon [https://medium.com/design-bootcamp/the-elements-of-fintech-typography-part-1-readable-money-b6c1226acbde]
- Copilot Money is the consensus design benchmark among budgeting apps: colors, emoji/custom category icons, and elegant graphs give at-a-glance financial health; it feels fast and responsive versus 'clunky data-heavy apps'. YNAB wins on methodology but loses on polish — the gap between utilitarian an [https://www.thepennyhoarder.com/budgeting/budgeting-copilot-money-review/ ; https://era.app/articles/era-vs-monarch-vs-copilot-vs-ynab/ ; https://www.engadget.com/apps/best-budgeting-apps-120036303.html]
- Micro-interactions in finance apps should be subtle and professional, paired with haptic feedback for high-stakes actions (payments, transfers): soft vibrations confirm completion, calm success states and reassuring microcopy reduce financial anxiety. iOS-style physics-based motion plus tactile conf [https://www.onething.design/post/top-10-fintech-ux-design-practices-2026 ; https://rosalie24.medium.com/microinteractions-in-mobile-apps-2025-best-practices-c2e6ecd53569 ; https://www.eleken.co/blog-posts/modern-fintech-design-guide]
- Gamification is now a primary fintech retention driver: apps using streaks, milestone celebrations, and micro-rewards (confetti, streak flashes, satisfying audio/haptic cues) report ~47% higher 90-day retention and 40-60% DAU lift; streaks work best on repeatable low-friction actions like logging an [https://www.strivecloud.io/blog/mobile-app-gamification-fintech ; https://www.netguru.com/blog/fintech-gamification ; https://trophy.so/solutions/finance]
- Dark mode support is table stakes in 2026 ('no longer a differentiator — it is expected'); the quality bar is now WCAG-verified contrast in both modes, calm low-saturation dark surfaces for long sessions, and OLED efficiency. Accessibility-first design (WCAG 2.2, scalable fonts, high contrast) is a [https://www.gitnexa.com/blogs/modern-fintech-ui-ux-trends ; https://www.graphiceagle.com/dark-mode-design-ui-tips-for-modern-apps-that-users-will-love/ ; https://www.onething.design/post/top-10-fintech-ux-design-practices-2026]
- Data-viz for personal finance has settled patterns: bar charts for category comparisons (spending by envelope/merchant), line charts for trends (net worth, forecasts), donut for parts-of-whole (allocation) — with direct labels on charts highlighting trends and anomalies so the chart itself guides a [https://merge.rocks/blog/fintech-dashboard-design-or-how-to-make-data-look-pretty ; https://www.eleken.co/blog-posts/modern-fintech-design-guide ; https://www.onething.design/post/top-10-fintech-ux-design-practices-2026]
- Transparency and explainability are the 2026 trust pattern: specific reasoning instead of generic messages ('flagged because usage is 3x your 90-day average', not 'anomaly detected'), previews before automated actions, visible progress instead of abstract loaders, and plain-language microcopy with v [https://outcrowd.medium.com/fintech-design-trends-2026-733bd55314d3 ; https://www.eleken.co/blog-posts/modern-fintech-design-guide]
- For React Native Paper specifically, going beyond Material defaults is done by spreading MD3LightTheme/MD3DarkTheme with a full custom token layer (colors, spacing, typography, motion) while respecting MD3 semantic color roles — overriding tokens without understanding their roles causes accessibilit [https://hemanshum.medium.com/the-ultimate-guide-to-custom-theming-with-react-native-paper-expo-and-expo-router-8eba14adcab3 ; https://oss.callstack.com/react-native-paper/docs/guides/migration-guide-to-5.0/ ; https://blog.logrocket.com/designing-custom-ui-using-react-native-paper/]
  RECOMMENDATIONS:
  - (major) Wire up the installed-but-unused expo-haptics into a semantic haptics service: expo-haptics ~55.0.14 is in package.json but has zero usages in src/. Create src/infrastructure/haptics/ with semantic methods (confirmTransaction, envelopeFunded, milestoneReached, warning, error) mapping to Haptics.notificationAsync/impactAsync intensities, and call it at high-stakes moments: savi
  - (transformative) Make the dashboard action-oriented: a 'next best action' hero, not a data report: The 2026 consensus is dashboards must answer 'where do I stand / what needs attention / what do I do next'. AccountingV2 has unusually rich signal for this: current baby step, debt snowball target, envelopes nearing empty (envelopeWarning/envelopeDanger tokens already exist), meter anomalies, and Ra
  - (major) Build celebration moments for debt payoffs, baby steps, and streaks: Gamification drives ~47% higher 90-day retention, and this app's Dave Ramsey model is inherently milestone-based (7 baby steps, per-debt payoffs, Ramsey Score tiers) but currently lacks celebration mechanics. Add: (1) a full-screen celebration (confetti via react-native-svg or Reanimated particles +
  - (major) Create a chart primitive kit on react-native-svg with direct labels and semantic tokens: react-native-svg 15.15.3 is installed and there's a custom progress ring (CurrentStepHero), but no shared chart components exist. Build src/presentation/components/charts/ with four primitives matching 2026 finance-viz patterns: BarChart (spending by envelope/category), LineChart with area fill (net
  - (moderate) Finish the money-typography rules in CurrencyText: The app already does the hard part — tabular-nums is applied in CurrencyText, KPIRow, EnvelopeTile, BabySteps — and uses a distinctive Fraunces + Plus Jakarta Sans pairing (already beyond Material defaults). Close the remaining gaps per fintech typography guidance: (1) always render trailing zeros /
  - (moderate) Add tonal surface elevation and an OLED true-black option to the dark theme: The dark palette in src/presentation/theme/tokens.ts is well-built (contrast ratios documented, WCAG AA verified) but is flat: surface and background are both #121212 with one surfaceVariant. Add a 3-step dark surface ramp (e.g. #121212 base, #1B1F1E cards, #232928 raised/sheets) mapped to Paper's e
  - (moderate) Make anomaly detection and coaching explainable, with previews before automated actions: 2026's trust pattern is specific reasoning over generic alerts. For the existing meter-reading anomaly detection and coaching features: always show WHY ('Electricity flagged: 42 kWh/day vs your 90-day average of 14') with the comparison sparkline inline, and let the user dismiss with 'this is expect

## React Native + Expo 2026 best practices (Expo SDK 55, New Architecture, lists, storage, animations, charts, OTA) applied to AccountingV2

- Expo SDK 55 (Feb 2026) drops Legacy Architecture entirely — New Architecture is the only option, newArchEnabled removed from app.json. Ships RN 0.83 + React 19.2, Hermes v1 opt-in (perf gains but requires building RN from source), Expo Router v7, expo-brownfield, and an Expo MCP server / AI agent sk [https://expo.dev/changelog/sdk-55]
- SDK 55 introduced Hermes bytecode diffing for EAS Update: devices download binary patches instead of full bundles, cutting OTA download size/time by ~75%. Opt-in in SDK 55, default in SDK 56. Requires latest expo-updates package. [https://expo.dev/blog/ship-smaller-ota-updates-bundle-diffing-comes-to-ota-updates-in-sdk-55 and https://docs.expo.dev/eas-update/bundle-diffing/]
- 2026 EAS Update production playbook: phased rollouts at 1% for 1 hour (catches ~70-80% of crash regressions), then 10% for 4-8 hours, then 50% overnight, then 100%; pair with auto-rollback. Rollback requires republishing the last-known-good bundle as a NEW update — users with the bad bundle cached w [https://www.72technologies.com/blog/eas-update-ota-strategy-channels-rollouts-rollbacks and https://www.72technologies.com/blog/expo-eas-ota-updates-without-bricking-production]
- FlashList v2 (Shopify) is a ground-up rewrite for the New Architecture only: no more estimatedItemSize (measures precisely), JS-only implementation, masonry as a prop, web support, and maintainVisibleContentPosition for feeds that update in real time. 2026 guidance: on SDK 55+, default to FlashList [https://shopify.engineering/flashlist-v2 and https://www.pkgpulse.com/guides/flashlist-vs-flatlist-vs-legendlist-react-native-lists-2026]
- react-native-mmkv is 30x (up to 30-100x in real scenarios) faster than AsyncStorage, fully synchronous (no bridge, no promises), supports AES encryption, and has a documented Zustand persist middleware adapter. 2026 consensus: MMKV for app state/preferences/feature flags/Zustand persistence; expo-se [https://github.com/mrousavy/react-native-mmkv and https://www.pkgpulse.com/guides/react-native-mmkv-vs-async-storage-vs-expo-secure-store-2026]
- Reanimated 4 is New-Architecture-only and is the 2026 default for animations: worklets run on a separate UI thread so animations stay fluid even when the JS thread is blocked, plus a new CSS-style transitions/keyframes API. The 2026 performance playbook explicitly says to move animations off the JS- [https://docs.swmansion.com/react-native-reanimated/docs/guides/compatibility/ and https://www.rapidnative.com/blogs/react-native-performance-optimization-2026-playbook]
- Expo Router v7 vs react-navigation 7 in 2026: Expo Router is a file-based layer built ON TOP of react-navigation (typed routes, automatic deep links, static web rendering). Consensus is it's the default for NEW projects, but established production apps with react-navigation codebases are generally N [https://dev.to/satyasootar/expo-router-vs-react-navigation-which-one-should-you-use-in-2026-40mm and https://docs.expo.dev/router/migrate/from-react-navigation/]
- For charts in 2026, Victory Native XL (built on React Native Skia + Reanimated + Gesture Handler) is the clear leader: GPU-rendered on the Skia canvas, 60-120fps animations even on low-end devices, eliminating the jank of SVG-based chart libraries on large datasets. Handles hundreds of points at 60f [https://github.com/FormidableLabs/victory-native-xl and https://getnerdify.com/blog/charts-react-native]
- React Native Paper remains actively maintained in 2026 (v5.15.3, Callstack) and is still the recommended library when Material Design 3 is the design language; alternatives (Tamagui, gluestack v3, NativeWind) matter only if leaving Material Design. No migration pressure for Paper 5 apps. [https://github.com/callstack/react-native-paper and https://blog.logrocket.com/best-react-native-ui-component-libraries/]
- expo-sqlite in SDK 55 adds a SQLite Inspector DevTools plugin (browse the on-device DB in real time) and a tagged-template SQL API for type-safe parameterized queries — directly useful for debugging a Drizzle/offline-first sync engine. [https://expo.dev/changelog/sdk-55]
- 2026 RN performance priorities, in order: (1) New Architecture + Hermes (done by default), (2) FlashList for lists, (3) kill unnecessary re-renders with memoization, (4) Reanimated 4 worklets for animations, (5) bundle size via Metro tree-shaking and Hermes bytecode. Hermes v1 became RN default in 0 [https://www.rapidnative.com/blogs/react-native-performance-optimization-2026-playbook]
  RECOMMENDATIONS:
  - (transformative) Adopt EAS Update with phased rollouts — currently missing entirely: AccountingV2 has no expo-updates dependency and eas.json contains only build profiles (development/preview/production) with no update channels. For an offline-first finance app where a sync-engine or money-math bug is high-severity, OTA is the single biggest operational win: add expo-updates, map ch
  - (major) Replace AsyncStorage with react-native-mmkv for Zustand persist and flags: AsyncStorage 2.2.0 is used in themeStore (Zustand persist), onboardingFlag, earlyCrashLog, NotificationPreferencesRepository, and slip-upload queueing. MMKV is 30-100x faster and synchronous, which eliminates the async-hydration flash on theme/store rehydration at cold start and lets earlyCrashLog w
  - (major) Swap FlatList for FlashList v2 in the 11 list screens: FlatList appears in 11 screens including DashboardScreen, ForecastScreen, SnowballDashboardScreen, SinkingFundsScreen, SlipQueueScreen, RateHistoryScreen, and EnvelopePickerSheet. Transaction/slip lists grow unbounded over months of budgeting data. FlashList v2 is a near drop-in replacement (same pr
  - (moderate) Introduce Reanimated 4 for celebration/gamification animations: The app animates with the JS-driven core Animated API (CelebrationModal 9 uses, SevenDotPath 11, LoadingSkeletonCard 9, EnvelopeFillBar 4). Ramsey Score celebrations and baby-step confetti moments are exactly when the JS thread is also busy (score recompute, DB writes), so JS-driven animations will
  - (moderate) Use Victory Native XL (RN Skia) for forecasting and meter-reading charts: The app has react-native-svg but no chart library — ForecastScreen and meter-readings (electricity/water/odometer with anomaly detection) are chart-hungry surfaces likely hand-rolled or table-based today. Victory Native XL renders on the GPU via Skia with Reanimated-driven interactions (pan-to-inspe
  - (moderate) Stay on react-navigation 7 — do not migrate to expo-router: Expo Router v7 is a file-based convention layer over the same react-navigation core the app already uses. For a stable ~52k LOC app with a working RootNavigator, typed params, and Detox e2e flows, 2026 consensus is that migration cost exceeds benefit (the wins — file-based routes, automatic web deep
  - (moderate) Enable the SDK 55 SQLite Inspector and tagged-template SQL for sync debugging: The offline-first Drizzle/expo-sqlite sync engine is the app's riskiest subsystem. SDK 55's SQLite Inspector DevTools plugin gives real-time on-device DB browsing (inspect envelope balances, sync queue, tombstones during development), and the new db.sql tagged-template API provides type-safe paramet

## Offline-first sync architectures 2026: PowerSync, ElectricSQL, WatermelonDB, Legend-State, TinyBase, CRDTs vs the hand-rolled SQLite-to-Supabase sync engine in AccountingV2

- PowerSync is the most production-proven Postgres<->SQLite sync engine in 2026 and has a first-party Supabase integration: reads are streamed from Supabase Postgres via WAL logical replication filtered by Sync Streams/Rules (YAML, per-user buckets), auth plugs into Supabase Auth (JWT/JWKS), and RLS s [https://docs.powersync.com/integration-guides/supabase ; https://powersync.com/blog/offline-first-apps-made-simple-supabase-powersync]
- PowerSync works with Expo/React Native but requires native modules, so Expo Continuous Native Generation or a dev build (no Expo Go); there are official Expo+Supabase demos including background sync via expo-background-task. Pricing: free cloud tier (2 GB synced/month, 50 concurrent connections), Pr [https://docs.powersync.com/client-sdks/reference/react-native-and-expo ; https://powersync.com/pricing ; https://marcoapp.io/blog/offline-first-landscape]
- ElectricSQL (post-2024 rewrite) is read-path-only by explicit design: it syncs data out of Postgres to clients but 'does not do write-path sync' and prescribes no offline write solution — you keep building your own optimistic-state/queue for writes. It works with managed Supabase (must disable conne [https://electric-sql.com/docs/guides/writes ; https://electric-sql.com/docs/integrations/deployment/supabase ; https://supabase.com/partners/integrations/electricsql]
- WatermelonDB is client-side only: its sync protocol requires you to hand-build pull/push backend endpoints against Supabase yourself (last_modified > lastPulledAt delta queries, transactional consistency, server-side conflict handling) — i.e., roughly the same backend work AccountingV2 has already d [https://watermelondb.dev/docs/Sync/Backend ; https://marcoapp.io/blog/offline-first-landscape ; https://cssauthor.com/offline-first-tech-stack/]
- Legend-State v3 ships an official syncedSupabase plugin (Supabase co-published a local-first Expo guide with it): incremental sync via changesSince/fieldUpdatedAt mapped to Supabase columns, offline write persistence with retry across sessions, AsyncStorage/MMKV persistence. But it is state-library- [https://supabase.com/blog/local-first-expo-legend-state ; https://legendapp.com/open-source/state/v3/sync/supabase/]
- CRDT routes in 2026: vlcn-io/cr-sqlite is effectively semi-dormant; the newer sqliteai/sqlite-sync extension (v1.0.x, 2026) adds CRDT replication to plain SQLite tables with direct sync to self-hosted PostgreSQL/Supabase and SDKs for React Native/Expo — but it is Elastic License 2.0 (commercial agre [https://github.com/sqliteai/sqlite-sync ; https://github.com/vlcn-io/cr-sqlite ; https://tinybase.org/guides/releases/]
- AccountingV2's hand-rolled engine is already sophisticated on the push side — batched pendingSync queue with backoff, dead-letter queue (10 retries / 7 days), N+1-safe row prefetch, and per-table server-side merge RPCs (server-authoritative conflict resolution, the pattern PowerSync itself recommend [C:\Project\AccountingV2\src\data\sync\SyncOrchestrator.ts ; C:\Project\AccountingV2\src\data\sync\RestoreService.ts (grep for realtime/.channel( matches tests only)]
- 2026 consensus across comparison pieces: for a Postgres backend and a small team, evaluate PowerSync first (most battle-tested, used in apps with millions of users); ElectricSQL(+TanStack DB/PGlite) is the most coherent choice for web apps; hand-rolled sync remains legitimate at either very small or [https://trybuildpilot.com/648-electric-sql-vs-powersync-vs-zero-2026 ; https://marcoapp.io/blog/offline-first-landscape ; https://cssauthor.com/offline-first-tech-stack/]
  RECOMMENDATIONS:
  - (major) Close the incremental-pull gap in the existing engine before considering any migration: The cheapest high-value move: add a per-household, per-table delta pull using an updated_at watermark (server-set via trigger, queried with .gt('updated_at', lastPulledAt)) plus a Supabase Realtime postgres_changes subscription on household-scoped tables to trigger pulls when online. This converts R
  - (transformative) If sync scope keeps growing, migrate to PowerSync — keeping the merge\_\* RPCs as the write path: PowerSync is the only 2026 option that replaces both the pull engine and the queue/retry machinery while staying Supabase-native and keeping server-authoritative writes: define Sync Streams per household (mirroring current household_members scoping), and implement uploadData by calling the existing
  - (moderate) Rule out ElectricSQL, WatermelonDB, TinyBase, and CRDT extensions for this app: ElectricSQL solves only the read path — the pendingSync push engine would remain hand-rolled, so it duplicates rather than replaces effort, and its ecosystem momentum (TanStack DB, PGlite) is web-first. WatermelonDB would force replacing Drizzle with its ORM while still requiring the same hand-built
  - (moderate) Do not adopt Legend-State for sync, even though Supabase promotes it: Legend-State's syncedSupabase plugin is attractive for simple per-user apps (incremental changesSince sync, offline retry), but it resolves conflicts client-side with last-write-wins and has no transactional SQLite store. For shared-household financial data, that is strictly weaker than the current
  - (moderate) Document the sync engine's protocol contract as an ADR before any of the above: The engine's non-obvious invariants — lastAttemptedAt stores next-allowed-retry time not last-attempt time, DLQ thresholds (10 retries/7 days), per-table RPC merge semantics, restore-vs-sync responsibilities — live only in code comments. Since every 2026 alternative was rejected partly because the h

## AI features in personal finance apps (2025-2026) and how to apply them to an offline-first Ramsey-method envelope budgeting app

- The AI finance app market has split into three shapes: chatbot coaches (Cleo), predictive categorization/pattern-detection tools (Copilot Money, Monarch), and fast AI-input tools (text/voice/receipt logging, e.g. Finny). Monarch's 2026 release added an agentic scenario planner ('what happens to our [https://getfinny.app/blog/best-ai-budget-apps-2026 ; https://walletgrower.com/compare/ynab-vs-monarch-vs-copilot]
- Monarch publicly uses a mix of third-party LLMs and in-house models for its AI Assistant, Insights, receipt scanning, and categorization — meaning user transaction text is processed by outside providers. This is a privacy differentiator opportunity for offline-first apps: on-device processing means [https://getfinny.app/blog/best-ai-budget-apps-2026 ; https://developers.google.com/ml-kit/genai]
- Google's ML Kit GenAI APIs expose Gemini Nano on-device (offline, no cloud cost) with Summarization, Proofreading, Rewriting, Image Description, and a Prompt API (alpha, Oct 2025) for custom text/multimodal prompts. Hard constraints: only premium devices (Pixel 9+, Galaxy S25+, newer OnePlus/OPPO/Xi [https://developers.google.com/ml-kit/genai ; https://android-developers.googleblog.com/2025/10/ml-kit-genai-prompt-api-alpha-release.html]
- For React Native specifically, react-native-executorch (Software Mansion, Meta's ExecuTorch) runs LLMs (Qwen 3, Llama 3.2, SmolLM 2), Whisper ASR, and CLIP embeddings on-device with Expo compatibility (model download via expo-file-system/expo-asset); llama.rn runs arbitrary GGUF models. Practical mo [https://github.com/software-mansion/react-native-executorch ; https://docs.swmansion.com/react-native-executorch/ ; https://www.webelight.com/blog/on-device-llms-for-usa-startups-enterprises-designing-smarter-multimodal-mobile-apps]
- OCR benchmarks (OmniAI, 1,000 real documents; Parsli 2026): LLM vision models now match or beat traditional OCR overall and are notably better on handwriting, crumpled/low-quality scans, and odd layouts (Gemini ~94% on scanned invoices vs GPT+OCR 91%); traditional/on-device OCR (ML Kit, Google Visio [https://getomni.ai/blog/ocr-benchmark ; https://parsli.co/blog/llm-ocr-vs-traditional-ocr ; https://aimultiple.com/ocr-accuracy]
- Transaction auto-categorization state of the art: ML systems reach 90-95% auto-categorization under clean taxonomies, but real-world accuracy depends on merchant ambiguity and requires a correction feedback loop and periodic retraining. Research (2025) shows small fine-tuned + calibrated models and [https://neontri.com/blog/ai-transaction-categorization/ ; https://arxiv.org/html/2508.05425v1 ; https://www.vellum.ai/blog/document-data-extraction-llms-vs-ocrs]
- Conversational money coaching is the highest-engagement AI pattern: Cleo 3.0 (2025) added voice, long-term memory, and reasoning, is explicitly 'built for behavior change, not just tracking', claims 20x engagement of traditional banking apps, and passed 1M paid subscribers / $250M ARR growing 2x YoY [https://www.businesswire.com/news/home/20250729690058/en/Cleo-Becomes-the-First-AI-Money-Coach-That-Speaks-Thinks-and-Remembers ; https://web.meetcleo.com/blog/Introducing-cleo-3-0]
- Ramsey Solutions itself relaunched EveryDollar (Jan 2026) with AI-powered personalization rather than a chatbot: personalized money plans, a margin-finding tool, a financial roadmap projecting the debt-free date, streaks, and daily bite-size lessons — while deliberately routing coaching to humans, p [https://lendedu.com/blog/everydollar-review/ ; https://www.ramseysolutions.com/ask-ramsey]
- Forecasting in consumer finance apps is mostly not deep learning: the reliable core is recurring-transaction detection plus rule/statistical projection (payroll on the 25th, rent on the 1st), with ML layered on for anomaly flags and 'next month will be higher' alerts. Deterministic forecasts with pl [https://www.meniga.com/guides/cashflow-forecasting-tools/ ; https://vocal.media/01/how-ai-and-machine-learning-are-transforming-personal-finance-apps]
- VLM/LLM receipt processing has deployment gotchas: content-policy refusals on some image types (e.g. GPT-4o refuses IDs), token-driven cost (~1,500 in / 1,000 out per page), and variable latency — reinforcing that cloud LLM extraction should sit behind a queue with on-device fallback in an offline-f [https://getomni.ai/blog/ocr-benchmark]
  RECOMMENDATIONS:
  - (major) Learning transaction auto-categorization with an on-device correction loop: Add a three-tier categorizer for assigning transactions to envelopes: (1) deterministic user rules and exact merchant-string matches stored in the local Drizzle/SQLite DB; (2) an on-device similarity model — merchant-name embeddings (CLIP/text encoder via react-native-executorch, or a simple TF-IDF/
  - (major) Upgrade slip scanning to hybrid OCR + LLM structured extraction: Keep the existing on-device OCR in src/infrastructure/slipScanning (ML Kit-class, free, real-time, offline) as the always-available path, then add an LLM structured-extraction layer: send the OCR text (not the image — cheaper, more private) to a Supabase edge function that returns JSON {merchant, da
  - (transformative) Ramsey-voice conversational coach grounded in local data: Cleo proved coaching personality drives 20x engagement and real subscription revenue, and Ramsey Solutions ships only a generic web 'Ask Ramsey' — an in-app coach that knows YOUR envelopes, debt snowball order, and Baby Step is an open gap. Implementation: build a compact deterministic 'financial st
  - (major) Envelope depletion forecasting and recurring-bill detection — deterministic, fully offline: The highest-value 'AI' forecast features need no LLM and run 100% offline against local SQLite: detect recurring transactions (same merchant, amount tolerance, period detection over 2-3 cycles), then project per-envelope burn rate to warn 'Groceries will run out around the 22nd at your current pace'
  - (moderate) Weekly Recap: locally computed insights, optionally LLM-narrated: Copy the pattern Monarch validated (Weekly Recap) but compute it offline: a domain-layer job derives facts from SQLite — top overspent envelopes, week-over-week deltas, snowball progress, Ramsey Score change, unusual transactions (reuse the existing anomaly-detection logic on spending, not just mete
  - (moderate) Debt snowball what-if scenario narrator: Match Monarch's 2026 agentic scenario planner at a fraction of the complexity: the snowball math is already pure domain logic, so 'what if I add R500/month?' or 'what if I get a bonus in December?' is deterministic simulation — add a scenario UI that recomputes payoff dates and total interest saved
  - (moderate) Adopt a progressive on-device AI capability layer: Create a small infrastructure module (src/infrastructure/ai) that probes device capability at runtime and routes AI tasks: tier A = ML Kit GenAI / Gemini Nano (Pixel 9+, Galaxy S25+; free, offline; respect foreground-only and battery-quota constraints with exponential backoff on ErrorCode.BUSY); tie

## Gamification & habit formation for finance apps (streaks, loss aversion, social accountability, debt-payoff progress visualization) applied to a Ramsey Baby Steps + 0-100 habit score app

- Lowering the daily bar for streak continuation works dramatically better than demanding full goal completion. Duolingo decoupled streaks from daily goals (one lesson extends the streak; goal progress tracked separately) after finding ~40% of learners active two days in a row with NO streak had the ' [https://blog.duolingo.com/improving-the-streak/]
- Bounded forgiveness is the highest-leverage streak mechanic: streak freezes increase average streak length ~48% (17.19 vs 11.62 days among 7+ day streakers). Duolingo's A/B tests found 2 freezes beat 1, but 3 was no better than 2 — forgiveness must be capped or it substitutes for the behavior. Freez [https://duolingo.deconstructoroffun.com/mechanics/streaks and https://apptitude.io/blog/how-duolingos-streak-mechanic-actually-works/]
- Loss aversion is the most dangerous motivational drive to deploy in finance apps: over-activating it churns users through anxiety. Rigid daily streaks trigger the 'abstinence violation effect' — one missed day leads to rage-quitting the entire habit. Ethical streak design treats life interruptions ( [https://yukaichou.com/gamification-examples/top-10-finance-apps-for-2017-from-an-octalysis-gamification-perspective/ and https://www.professorgame.com/podcast/423/ and https://medium.com/design-bootcamp/streaks-and-daily-rewards-as-habit-forming-systems-dab7f5a34539]
- YNAB's playbook: subtle gamification integrated into the core workflow beats gimmicky game layers. Its 'Age of Money' works as a self-competitive score, savings goals use progress bars, its four rules act as achievement unlocks ('Live on Last Month's Income' = leveling up), and it uses positive rein [https://trophy.so/blog/ynab-gamification-case-study]
- The debt snowball works because of the goal-gradient effect and Amabile's 'progress principle': motivation increases as people get closer to a goal, small wins release dopamine and act as positive reinforcement, and visual feedback (shrinking balances, thermometers) creates emotional connection to p [https://www.ramseysolutions.com/debt/how-the-debt-snowball-method-works and https://choosefi.com/debt-freedom/debt-snowball-method and https://www.moneyfit.org/debt-snowball/]
- Social accountability roughly doubles-to-triples goal success: stickK (Yale behavioral economists) reports adding a referee to a commitment contract doubles success odds, and financial stakes make users 3x more likely to succeed. But couples budgeting apps die a specific death: ~70% abandonment with [https://stickk.zendesk.com/hc/en-us/articles/206109308-What-is-stickK and https://www.thepennyhoarder.com/budgeting/best-budgeting-apps-couples/]
- The fresh start effect (Dai, Milkman, Riis 2014): temporal landmarks — new week/month/year, birthdays, holidays — motivate aspirational behavior by relegating past failures to a closed mental accounting period. Framing a retirement-savings invitation around an upcoming birthday increased savings enr [https://pubsonline.informs.org/doi/10.1287/mnsc.2014.1901 and https://gc-bs.org/articles/temporal-landmarks-behavioral-modification-leveraging-fresh-start-effect-organizational-change/]
- What backfires, cautionary tale: Robinhood's confetti, scratch-card rewards, and celebratory affirmations on trades drew regulatory action (Massachusetts) for nudging frequent transactions that benefited the company, not the user. The line: celebrate outcomes the user chose for themselves (goal reac [https://www.fastcompany.com/90619112/robinhood-gamification-dark-side and https://btlj.org/2025/11/the-gamification-of-investments-a-comparative-approach-between-the-us-and-eu/]
- Magnitude of the opportunity in fintech specifically: gamified experiences show ~48% engagement lift, streak/milestone mechanics lift DAU 40-60% in the first month, gamified apps show ~47% higher 90-day retention, and gamified savings features boost saving habits ~22% with users saving ~20% more — i [https://www.netguru.com/blog/fintech-gamification and https://www.strivecloud.io/blog/mobile-app-gamification-fintech]
- Gamified scores work when the score is legible and actionable: Credit Karma-style dashboards (visible score + visible improvement factors) coincided with the average US credit score rising 22 points over a decade; UK app Chip's WealthScore ties a single number to concrete behaviors. The score must a [https://www.smartico.ai/blog-post/credit-score-improvement-games and https://craftinnovations.global/gamification-in-fintech-examples-ideas/]
  RECOMMENDATIONS:
  - (transformative) Add a logging streak with 2 silent monthly 'grace days': The score already tracks loggingDaysCount (HabitScoreCalculator, src/domain/scoring/RamseyScoreCalculator.ts) but there is no streak surface. Add a daily streak whose bar is deliberately low — 'open the app and log or confirm one transaction' — per Duolingo's decoupling lesson (low bar drove +10.5%
  - (transformative) Make the debt snowball visually roll: per-debt thermometers, a growing snowball, and a live debt-free date: This is the highest-alignment feature for a Ramsey app: the snowball method IS gamification (goal-gradient + small wins). For each debt show a shrinking balance bar with % killed ('82% to destroying Store Card'); show the snowball payment amount growing as debts close ('your snowball is now R3,450/m
  - (major) Make the 0-100 score legible, forgiving, and always actionable: Current HabitScoreCalculator is period-ratio based: meterPoints are 0 until meters are logged and the score can sag early in a period through no fault of the user — a number that drops without an action attached reads as judgment and churns users (loss-aversion overuse). Ship three changes: (1) show
  - (major) Household accountability: weekly shared digest + joint milestones, per-member notification control: The app already has multi-household with invite codes — this is a structural advantage no competitor score-app has, and stickK evidence says a human referee doubles success rates. But the known failure mode is the non-manager partner drowning in transaction pings until they ignore everything (the 70
  - (moderate) Baby Step milestones as fixed, transparent, one-time achievements — no variable rewards: The 7 Baby Steps are a ready-made achievement ladder (YNAB's 'four rules as level-ups' pattern). Add permanent badges for: BS1 emergency fund funded, first debt paid off, each subsequent debt, BS2 complete (debt-free scream moment), 3/6/12 months of logging consistency, first full month with every e
  - (moderate) Fresh-start triggers at temporal landmarks: Use the Dai/Milkman/Riis fresh-start effect (birthday framing lifted savings enrollment 20-30%): trigger goal-setting and recommitment prompts at the start of each budget period, new month, new year, and (opt-in) the user's birthday — 'New month, clean slate: set this month's envelope targets.' Crit
  - (moderate) One anxiety budget: audit every negative signal in the app: Adopt an explicit rule from the fintech research: loss-aversion mechanics get ONE slot. Inventory all red/negative UI (overspent envelopes, dropping score, streak warnings, anomalous meter readings) and ensure at most one fear-based push notification can fire per day, with everything else framed as

## Supabase security & RLS best practices 2026 (multi-tenant/household apps)

- CVE-2025-48757 (disclosed May 2025) exposed 303 endpoints across 170+ Lovable-generated apps (10.3% of 1,645 analyzed): tables were readable by unauthenticated requests using only the public anon key because RLS was missing or insufficient. Root cause: RLS is off by default on new tables and AI code [https://mattpalmer.io/posts/2025/05/CVE-2025-48757/ ; https://vibeappscanner.com/supabase-row-level-security ; https://supabase.com/blog/supabase-security-2025-retro]
- Supabase replaced legacy JWT-based anon/service*role keys with publishable (sb_publishable*...) and secret (sb*secret*...) API keys. Since 1 Nov 2025, new/restored projects no longer get legacy keys. Benefits: secret keys rotate in seconds without invalidating user sessions, multiple independent sec [https://supabase.com/docs/guides/api/api-keys ; https://supabase.com/docs/guides/getting-started/migrating-to-new-api-keys ; https://supabase.com/changelog/29260-upcoming-changes-to-supabase-api-keys]
- The '(select auth.uid())' wrap is the single biggest RLS performance lever: bare auth.uid() (and auth.jwt(), auth.role(), current_setting()) re-evaluates per row; wrapping in a SELECT subquery caches it per query as an InitPlan — documented 100x+ improvements (5ms vs 5s on 100k rows). Other top patt [https://supabase.com/docs/guides/troubleshooting/rls-performance-and-best-practices-Z5Jjwv ; https://makerkit.dev/blog/tutorials/supabase-rls-best-practices ; https://vibeappscanner.com/supabase-row-level-security]
- SECURITY DEFINER function pitfalls: (1) they run with the creator's privileges (typically postgres, which has bypassrls), so every one must contain explicit authorization checks; (2) they must pin 'SET search_path' (usually to public or '') to block search_path hijacking; (3) any function in an expo [https://supabase.com/docs/guides/database/postgres/row-level-security ; https://dev.to/nasheomirro/hiding-utility-functions-in-supabase-4ol1 ; https://makerkit.dev/blog/tutorials/supabase-rls-best-practices]
- Multi-tenant authorization rules: put tenant_id (household_id) on every table and match against membership or a JWT claim; NEVER authorize from user_metadata (user-editable) — only app_metadata (server-set) or database tables. Common CVE-class RLS mistakes: enabling RLS with zero policies (blocks ev [https://makerkit.dev/blog/tutorials/supabase-rls-best-practices ; https://vibeappscanner.com/supabase-row-level-security]
- Edge function security: keep verify_jwt = true (platform validates JWT before your code runs), then still call auth.getUser() and do explicit authorization — a valid JWT is authentication, not authorization. Use two clients: a user-scoped client (forwarding the caller's Authorization header) so RLS [https://supabase.com/docs/guides/functions/auth ; https://supabase.com/docs/guides/auth/rate-limits ; https://supabase.com/docs/guides/functions/development-tips]
- Supabase now ships weekly automated Security Advisors (Splinter linter) that flag missing RLS, SECURITY DEFINER views, functions with mutable search_path, and exposed schemas; plus column-level privileges for PII independent of RLS. Best practice 2026: treat advisor output as CI — run it after every [https://supabase.com/blog/supabase-security-2025-retro ; https://vibeappscanner.com/supabase-row-level-security]
  RECOMMENDATIONS:
  - (major) Wrap auth.uid() in (select ...) and add TO authenticated across all RLS policies: Zero occurrences of the (select auth.uid()) pattern exist across all 19 files in supabase/migrations — e.g. 002_rls_policies.sql uses bare 'user_id = auth.uid()' and per-row 'SELECT household_id FROM user_households WHERE user_id = auth.uid()' subqueries, and 002 has no 'TO authenticated' clauses at
  - (transformative) Add a CI drift test for SECURITY DEFINER merge RPC guards: The repo has ~40 SECURITY DEFINER functions and has already suffered a real guard regression: 012_fix_role_escalation.sql added a role-escalation check to merge_household_member, 016 (CREATE OR REPLACE) silently dropped it, and 017 had to 're-add role escalation guard (from 012)', with 019 tightenin
  - (major) Migrate from legacy anon/service_role keys to publishable/secret keys with asymmetric JWTs: src/data/remote/supabaseClient.ts reads supabaseAnonKey from app.config.ts extra — a legacy JWT-format anon key baked into the shipped Android bundle (extractable from the APK; fine by design, but legacy keys cannot be rotated without invalidating all sessions). Migrate the client to an sb_publishab
  - (major) Consolidate household membership checks into a private-schema helper function: Policies repeat 'household_id IN (SELECT household_id FROM user_households WHERE user_id = auth.uid())' inline in every table policy (002 and later). Create private.is_household_member(hid uuid) as STABLE SECURITY DEFINER with SET search_path, in a NON-exposed schema (public functions are callable b
  - (moderate) Add rate limiting and per-event authorization to notify-event; keep verify_jwt on for all functions: extract-slip already implements the 2026 best-practice pattern (atomic check_and_reserve_slip_slot Postgres counter with per-user 25 and per-household 50 limits returning 429) — extend the same pattern to notify-event, which validates the JWT via auth.getUser() but then uses a service-role client wi
  - (moderate) Run Supabase Security Advisor / db lint in CI and enable RLS-by-default toggle: Supabase's Splinter-based Security Advisors now detect exactly the issue classes present in this repo's history: SECURITY DEFINER functions with mutable search_path (012's merge_household_member lacked SET search_path until superseded by 019), tables without RLS, and policies without TO authenticate
  - (moderate) Add a pgTap adversarial RLS test suite covering all CRUD as anon and cross-household users: RLS failures return empty result sets silently rather than erroring, so unit tests that only check happy paths (as this repo's Deno edge-function tests do) miss isolation bugs. Add a pgTap suite (runs in supabase db test) that: creates two households with two users, then for EVERY table asserts (1)

## Mobile accessibility & Android quality bar: WCAG 2.2 mobile guidance, Google Play 2026 policy/quality requirements, and React Native accessibility APIs for a finance app

- W3C published 'Applying WCAG 2.2 to Mobile Applications' (WCAG2Mobile, Group Draft Note, May 2025) — the first official guidance mapping WCAG 2.2 to native apps. The conformance unit becomes the app 'screen/view' instead of the web page. The criteria it flags as most mobile-critical: 1.3.4 Orientati [https://www.w3.org/TR/wcag2mobile-22/ and https://www.w3.org/WAI/news/2025-05-06/wcag2mobile/]
- WCAG 2.5.8 sets a 24x24 px legal minimum for touch targets, but Google's Android/Material quality bar is 48x48 dp — Accessibility Scanner and Play pre-launch reports flag anything smaller. Contrast requirements: 4.5:1 for normal text, 3:1 for large text and for non-text UI (icons, envelope progress [https://www.levelaccess.com/blog/wcag-for-mobile-apps/ and https://developer.android.com/docs/quality-guidelines/core-app-quality]
- Google Play 2026 quality bar: new apps and updates must target Android 16 (API 36) by August 31, 2026; apps targeting below API 34 become invisible to new users. The April 15, 2026 policy update tightens Accessibility API usage (AccessibilityService is blocked for autonomous-action/automation use), [https://support.google.com/googleplay/android-developer/answer/16926792, https://stora.sh/blog/2026-04-14-android-api-36-august-deadline-what-to-do-now, https://developer.android.com/about/versions/16/features]
- React Native accessibility hinges on a small set of props that map to both TalkBack and VoiceOver: accessibilityLabel (mandatory for icon-only buttons — TalkBack otherwise reads the icon asset/resource name or nothing), accessibilityRole, accessibilityState, accessibilityValue, and grouping via acce [https://reactnative.dev/docs/accessibility, https://reactnativerelay.com/article/react-native-accessibility-guide-building-inclusive-apps-expo, https://www.accessibilitychecker.org/blog/react-native-accessibility/]
- Dynamic type: Android users can scale fonts far beyond 100% (Android 14+ uses non-linear scaling up to 200%). Best practice in React Native is to keep allowFontScaling on (default) and use maxFontSizeMultiplier per-component (never below ~1.2, which 'significantly impacts accessibility') rather than [https://reactnative.dev/docs/text, https://ignitecookbook.com/docs/recipes/AccessibilityFontSizes/, https://github.com/appt-org/accessibility-code-examples/blob/main/text-scale/react-native.md]
- Play Store data-safety: the Data safety form covers 14 data categories with collection/sharing/purpose/encryption/deletion declarations and must match both the privacy policy and the actual binary behavior — mismatches trigger enforcement (2025 report: 80,000+ developer accounts banned). April 2025 [https://support.google.com/googleplay/android-developer/answer/10787469, https://respectlytics.com/blog/google-play-data-safety-guide/, https://support.google.com/googleplay/android-developer/answer/13327111]
- Google Play Financial Services policy: any app with 'financial features' (management of money, including personalized advice) must complete the Financial features declaration in Play Console (Policy > App content). A budgeting/debt-management app with coaching features likely qualifies — and note th [https://support.google.com/googleplay/android-developer/answer/13849271, https://support.google.com/googleplay/android-developer/answer/9876821]
- Codebase audit of C:\Project\AccountingV2: accessibility props (accessibilityLabel/Role/etc.) appear 96 times across only 29 files, concentrated in dashboard, auth, babySteps, slipScanning and shared components — most of the 29 screens (transactions, budgets, envelopes, meters, forecasting, househol [C:\Project\AccountingV2\src\presentation\__tests__\accessibility.test.tsx and grep of C:\Project\AccountingV2\src]
  RECOMMENDATIONS:
  - (transformative) Sweep all 29 screens to full TalkBack coverage with a shared labeling convention: Only ~29 of the presentation files carry any accessibility props today. Do a screen-by-screen pass (transactions, budgets, envelopes, meters, debt snowball, household, sinking funds, settings, forecasting) adding accessibilityRole/accessibilityLabel/accessibilityState to every touchable, and group c
  - (major) Announce dynamic financial state changes to screen readers: AccessibilityInfo is used in only 5 files. Add announceForAccessibility (or accessibilityLiveRegion='polite' on Android) for: envelope balance updates after adding a transaction, sync status changes (OfflineBanner already exists — make it a live region), slip-scan OCR completion, debt-snowball payme
  - (major) Enforce 48dp touch targets and add dragging/gesture alternatives: Zero hitSlop or minHeight:48 styles exist in src/presentation. Add a lint-able shared style (minHeight/minWidth 48, or hitSlop) for all small touchables — envelope tiles, list-row chevrons, stepper dots in SevenDotPath, slip line-item edit icons. Audit any swipe-to-delete or drag interactions (trans
  - (major) Make layouts survive 200% font scale; use maxFontSizeMultiplier instead of disabling scaling: No font-scaling handling exists in the codebase. Test every screen at Android's max font + display size (Android 14+ non-linear scaling to 200%). Fix fixed-height cards (HeroSummaryCard, EnvelopeTile, RamseyScoreBadge) to grow with content; where large amounts would break layout, cap with maxFontSiz
  - (major) Fix contrast and color-only signaling in the budgeting/theming layer: Audit src/presentation/theme tokens against 4.5:1 (text) and 3:1 (non-text UI: envelope progress bars, DebtPayoffBar, BabyStepsBar, chart strokes, over/under-budget red/green) in BOTH light and dark themes — Paper's default md3 tones don't guarantee AA for custom tints. Anywhere red/green conveys ov
  - (moderate) Replace string-matching accessibility tests with rendered a11y-tree assertions + Accessibility Scanner in CI: accessibility.test.tsx asserts source code contains 'accessibilityLabel=...' via fs.readFileSync — it passes even if the prop is on the wrong element or conditionally dropped. Migrate to @testing-library/react-native queries (getByRole, getByLabelText, toHaveAccessibilityValue) and consider eslint-p
  - (transformative) Prepare Play Console compliance pack: data safety, account deletion, financial features declaration, API 36: Before/at launch: (1) complete the Data safety form covering Supabase-synced personal + financial data, FCM tokens, and slip-scan images (OCR of receipts = user financial info; declare collection, encryption in transit, and deletion support); verify no SDK reads ANDROID_ID undeclared. (2) Implement

## Monetization & growth for indie budgeting apps (2025-2026): pricing benchmarks, freemium boundaries, family plans, Play Store ASO, and retention — applied to a household-focused envelope-budgeting app

- 2026 pricing benchmarks cluster at $75-110/year for budgeting apps: YNAB $109/yr ($14.99/mo), Monarch Core $99.99/yr with a new $199/yr Plus tier, EveryDollar Premium $79.99/yr ($17.99/mo), Goodbudget $80/yr, PocketGuard Plus $74.99/yr. YNAB has raised prices repeatedly ($60 one-time -> $50 -> $84 - [https://www.ramseysolutions.com/budgeting/budgeting-apps-comparison ; https://www.fool.com/money/personal-finance/monarch-money-vs-ynab/ ; https://envelopebudgeting.com/articles/ynab-review ; https://costbench.com/software/personal-finance/goodbudget/]
- RevenueCat's State of Subscription Apps 2026 (115k apps, $16B revenue): hard-paywall apps convert 10.7% download-to-paid at D35 vs 2.1% for freemium, and generate $3.09 revenue-per-install at D60 vs $0.38 — but 12-month payer retention is nearly identical (27% vs 28%). North America median D35 downl [https://www.revenuecat.com/blog/growth/subscription-app-trends-benchmarks-2026/ ; https://www.revenuecat.com/state-of-subscription-apps/]
- Trial length matters more than paywall design: 17-32 day trials convert at a 42.5% median vs 25.5% for trials under 4 days (~70% better). 82% of trials start the same day as download, and 55% of short-trial cancellations happen on Day 0. Trial-format paywall screens win 64.5% of A/B tests against vi [https://www.revenuecat.com/blog/growth/subscription-app-trends-benchmarks-2026/ ; https://www.airbridge.io/en/blog/hard-vs-soft-paywalls]
- Android-specific churn trap: 31% of Google Play subscription cancellations are involuntary billing failures (vs 14% on iOS App Store), and ~72% of annual subscribers don't renew into year 2, with 35% of annual cancellations occurring in month 1. Billing-retry/grace-period handling is one of the chea [https://www.revenuecat.com/blog/growth/subscription-app-trends-benchmarks-2026/]
- The freemium boundary that converts in this exact niche: free tier gives manual zero-based/envelope budgeting (the aha moment), premium gates bank sync, insights, and coaching. EveryDollar's Jan 2026 relaunch: free = manual budget; Premium ($79.99/yr) = bank sync, 'Margin Finder' (avg new user 'find [https://www.thepennyhoarder.com/budgeting/everydollar-app-review/ ; https://lendedu.com/blog/everydollar-review/ ; https://www.fincomparelab.com/guides/goodbudget-pricing/]
- Family/household plans are sold as one household subscription, not per-seat: Goodbudget's shared budget (2 devices free, 5 premium) is repeatedly cited as the top pick for couples/families because it's 'a price that works for one household — not per-seat business pricing'. 'Best budget app for coupl [https://costbench.com/best/family-budgeting-apps/ ; https://envelopebudgeting.com/articles/budgeting-apps-for-couples ; https://asomobile.net/en/blog/aso-in-2026-the-complete-guide-to-app-optimization/]
- Google Play ASO shifted in 2025-2026 from install volume to retention, stability, and update frequency as ranking factors; teams that missed this saw organic traffic drop. Winning keyword strategy is long-tail natural-language intent ('budgeting app with bank sync', 'weekly budget planner app', 'bud [https://appfollow.io/blog/google-play-aso-keywords ; https://asomobile.net/en/blog/aso-in-2026-the-complete-guide-to-app-optimization/ ; https://www.mobileaction.co/blog/google-play-store-ranking-factors/]
- Retention benchmarks: finance apps average ~4.2% Day-30 retention, with strong fintech performers at 10-15% (vs ~7% all-category average). The retention driver called out for budgeting specifically: users who see a concrete saved-money number ('you've saved $150') stay; users who only linked an acco [https://uxcam.com/blog/mobile-app-retention-benchmarks/ ; https://www.businessofapps.com/data/finance-app-benchmarks/ ; https://sendbird.com/blog/app-retention-benchmarks-broken-down-by-industry]
- Indie case studies prove non-standard pricing works: Lunch Money (solo dev Jen Yip) moved to pay-what-you-want annual pricing ($40-150/yr) in 2023 — MRR nearly doubled in 10 months, churn and trial conversion improved, and the 37% of users who chose to pay >$100 generate over 60% of revenue; being w [https://lunchmoney.app/blog/we-lowered-our-pricing ; https://www.savingadvice.com/articles/2025/08/24/10148888_best-budgeting-apps.html ; https://www.buildmvpfast.com/blog/602k-revenue-solo-indie-hacker-app-portfolio-breakdown-2026]
- Market context: subscription app supply is up 7x since 2022 (14,700 new subscription apps/month by Jan 2026) and growth is polarizing — top quartile grew 80%+ MRR YoY while the bottom quartile shrank 33%. Median MRR growth is only 5.3%; differentiated positioning (a specific methodology + household [https://www.revenuecat.com/state-of-subscription-apps/ ; https://www.saastr.com/the-top-10-learnings-from-revenuecats-state-of-subscription-apps-how-115000-mobile-apps-deliver-16b-in-revenue-whats-working-whats-quietly-killing-growth/ ; https://vault22.com/]
  RECOMMENDATIONS:
  - (transformative) Price as a single household plan at $59-79/year, undercutting EveryDollar: This app serves the exact EveryDollar/Ramsey audience (envelope budgeting + debt snowball + baby steps), where the price anchor is $79.99/yr and the category ceiling is $109/yr (YNAB). Launch at $59-79/yr (or ~$7.99/mo) as ONE subscription covering the whole household — the multi-household membershi
  - (transformative) Draw the freemium line at: manual solo budgeting free; household sync, OCR, and intelligence paid: Free tier must let a user reach the aha moment — create envelopes, fund them, record transactions, see one full budget month, and start a debt snowball with 1-2 debts (cap ~10-15 envelopes like Goodbudget's 20). Gate behind Premium the features that map to this codebase's differentiators: (1) multi-
  - (major) Use a 30-day full-Premium trial with a trial-format paywall in onboarding: 17-32 day trials convert at 42.5% median vs 25.5% for short trials, and a budget app's value loop is one monthly budget cycle — so offer a ~30-day Premium trial (YNAB uses 34 days for this reason). Show the trial-offer paywall during first-session onboarding (82% of trials start on download day; tri
  - (major) Instrument a quantified 'money found' moment in week 1 to drive retention and conversion: Budgeting-app retention hinges on showing a concrete number early ('you've saved $150' hooks users; EveryDollar markets 'average user finds $3,015 of margin in 15 minutes'). This app already computes Ramsey Score, debt snowball payoff dates, and envelope balances — synthesize them into a single dash
  - (major) Fix Google Play involuntary churn from day one: 31% of Google Play subscription cancellations are involuntary billing failures (vs 14% on iOS). Since this is an Android-first Expo app, configure Play Billing grace periods + account hold, in-app dunning banners when payment fails, and use RevenueCat (or equivalent) for retry orchestration and to g
  - (major) Turn household invite codes into the primary growth loop, and target couple/family long-tail ASO: The multi-household invite-code system is a built-in viral loop: let free users JOIN a Premium household with full functionality (the subscriber pays for the household), so every convert recruits 1-3 more installs who later start their own households. For Play Store ASO, avoid the head term 'budget
  - (moderate) Keep a one-time-purchase or web-checkout escape hatch in the roadmap: Two proven indie alternatives to pure Play-subscription economics: (1) Buckets sells envelope budgeting as a $64 one-time purchase — a 'lifetime' SKU at 2-2.5x the annual price ($120-150) captures subscription-averse Ramsey-audience users who philosophically dislike recurring bills (on-theme for a d
